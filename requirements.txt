# ===== 主程式 + 排班程式 =====
# 版本上限 pin（next-major）：避免新機器或 runtime fallback 抓到破壞性新版。
# 要升級時手動把上限往上調。0.x / date-based 套件不 pin 上限（語意化不適用）。
requests>=2.31.0,<3
beautifulsoup4>=4.12.0,<5
lxml>=5.0.0,<7
selenium>=4.15.0,<5
keyboard>=0.13.5          # 0.x，穩定，不 pin 上限
pyautogui>=0.9.54         # 0.x，不 pin 上限
schedule>=1.2.0,<2
psutil>=5.9.0,<8
Pillow>=10.0.0,<12
pystray>=0.19.5           # 0.x，不 pin 上限
pywin32>=306             # date-based 版號（306/308...），不 pin 上限
sv-ttk>=2.6.0,<3          # Win11 風格 ttk 主題（main.py 用）

# ===== 打卡程式 =====
webdriver-manager>=4.0.0,<5
winotify>=1.1.0,<2

# ===== 開發 / 測試（選裝，installer 不會自動裝）=====
# pyinstaller>=6.0
# pytest>=7.4
