# 未審區域 Code Review 計畫書（2026-07-09）

> **定位**：接續《五程式完整回顧審查優化計畫書_2026-07-08.md》（已全數執行完畢、9 批次上線）。
> 上一輪聚焦「排班／會診／打卡／止掛／縮寫」五程式**核心**；本文件審查**上一輪沒覆蓋到的區域**
> ——約 23,000 行未審 code：UVB 醫療劑量計算、診間即時資料鏈、獨立小程式（座標偵測／OCR）、
> 大量跨程式共用基礎設施、以及 `main.py` 止掛以外的絕大部分。
>
> **分工**：審查＋本計畫書由 **Fable 5** 產出；**修正執行由 Opus 4.8** 依本文件開工（同上一輪模式）。
> **節奏**：審查分 5 階段進行，每階段獨立可跑、可中斷、結果即時落檔，避免單一 session 用量爆掉。
> **審查基準 commit**：`1512ee5`（v2026.07.09.2，上一輪 9 批次全部上線後）。

---

## 0. 給 Opus 4.8 的執行守則（沿用上一輪，硬性）

1. **開工前**：`git fetch origin` + `git status` 確認乾淨且與 origin/main 同步；逐條開工前先重讀該 finding 行號段落確認現況相符（行號以 `1512ee5` 為基準會漂移；不符 → 記錄後跳過回報，不硬修）。
2. **只修本文件列出的項目**；「選配」由執行者裁量。
3. **每批**：`python -m pytest -q` 全綠（基準見開工當下）才進下一批；每個 fix 加回歸測試。
4. **發佈紀律**：修完 → `python .claude/codex.py review --uncommitted` → 認真處理正確性/安全/blocking → 過了才 `python scripts/push_helper.py "<訊息>"`（自動 bump version + 重生 manifest；**絕不手動 git commit+push src**）。
5. **⚠ OneDrive 教訓**（上一輪實際發生兩次靜默還原）：每批修完當日內 push；高風險大檔（`main.py`/`uvb_dose.py`）每個小項改完**立即 commit 到分支**，並 `git diff > %TEMP%\<批次>.patch` 備份；push 後 `git show HEAD:<檔>` 驗證 commit 內容。
6. **⚠ pytest settings 污染陷阱**（上一輪踩到）：pytest 下 `get_settings_dir()` 解析到 `site-packages/pytest/settings/`。任何寫真實 settings 的測試都要隔離到 `tmp`（見背景任務 task_e3f4e471）。
7. 語意慣例：唯讀 fail-open、寫入/資料落地 fail-closed；Tk 只在主緒碰；**醫療計算/寫回（UVB 劑量、打卡）＝不確定就不自動動作，留給醫師**，絕不猜危險值。
8. Windows：檔案 IO 一律 `encoding='utf-8'`；subprocess 對外部指令比照既有 `errors="replace"` 慣例；PowerShell 輸出在繁中機是 cp950（見上一輪 AC-06 教訓，需要中文就強制 UTF-8）。
9. 安全紅線：repo 維持 **public**；`settings/` 與含 PII（員編/GSM/姓名/帳密）檔絕不 commit。

---

## 1. 未審範圍盤點（本輪目標）

上一輪《§5 已確認無問題》清單涵蓋的**不重審**。本輪目標為以下**未審或僅淺觸**區域：

| 群組 | 檔案 | 行數 | 風險 |
|---|---|---|---|
| UVB 醫療計算 | `uvb_dose.py` + `main.py` UVB/Excimer 熱鍵流程(1542-2550) | ~2000+~1000 | **最高**（劑量錯＝病人傷害） |
| 診間即時資料 | `main.py` reg64 診間燈號(非止掛)、`floating_clinic.py`、`clinic_state/history/light_history.py`、`reg64_utils.py`、`appt_utils.py`、`threshold_policy.py`、`master_schedule_cache.py` | ~2500 | 高（顯示錯診間/人數） |
| 獨立小程式/影像 | `coord_detector.py`、`ditto_card_ocr.py`、`pixel_picker.py`、`punch_status.py`(完整)、`hotkey_guardian.py`、`hotkey_scaling.py` | ~1600 | 中高 |
| 跨程式基礎設施 | `updater.py`、`sqlite_cache.py`、`imap_reader.py`、`smtp_mail.py`、`http_client.py`、`single_instance.py`、`bounded_executor.py`、`atomic_io.py`、`config_io.py`、`app_settings.py`、`settings_backup.py`、`cache_cleanup.py`、`cache_state.py`、`deps_installer/runtime.py`、`logging_setup.py`、`paths.py`、`process_launch.py`、`update_policy.py`、`memory_cache.py`、`date_utils.py`、`win32_safe.py`、`tk_exception.py`、`task_gate.py`、`ui_messages.py`、`window_icon.py`、`splash.py`、`notifications.py`、`chrome_options.py` | ~4000 | 中（一 bug 影響全部程式） |
| 主程式其餘＋入口 | `main.py` 止掛/UVB/診間以外（UI 建構、托盤選單、更新接線、啟動流程、F9/F10/F11 其餘熱鍵）、`scheduler.py`、`watchdog_runner.py`、6 個 `.pyw` 入口 | ~9000 | 中 |

---

## 2. 審查階段（每階段一個受控 session 可完成）

> 方法：每階段派 1-3 個平行唯讀審查 agent（general-purpose/sonnet），結構化回傳 findings；
> Fable 抽驗高風險 finding（二層對抗驗證）後彙整成本文件的「§N findings」。
> **驗證等級聲明**：本文件 finding **未經第二層對抗驗證**（除 Fable 抽驗的關鍵 P1 外）。
> Opus 執行每條前**必須先重讀該行號段落確認現況成立**（同上一輪紀律）。

> **額度紀律（2026-07-10 起硬性）**：一次只跑**一個階段**（≤2 個平行 agent）；每階段 agent 回報後
> **立刻把 findings 寫進本文件並手動 commit（docs-only）**，才啟動下一階段——agent 跑失敗/超額中斷
> 時已完成的結果不會遺失。各階段之間可跨 session 接續。

- **階段 1 — UVB 醫療計算（最高風險）**：`uvb_dose.py` 全檔 + `main.py` UVB/Excimer F1-F3 熱鍵流程。→ §3【✅ 審查完成 2026-07-10：**5×P1（實測重現、3 條主審複驗）**＋9×P2＋13×P3，修正批次見 §3E——**全計畫最高優先，Opus 應最先開工 UVB批次1**】
- **階段 2 — 診間即時資料鏈**：reg64 診間燈號輪詢(非止掛)、floating_clinic、clinic_state/history、reg64_utils、appt_utils、threshold_policy、master_schedule_cache（light_history.py 不存在，已併入 clinic_history）。→ §4【✅ 審查完成 2026-07-10：1×P1＋2×P2＋9×P3，修正批次見 §4C，待 Opus 開工】
- **階段 3 — 獨立小程式＋影像/座標/卡號**：coord_detector（點座標偵測程式入口）、punch_status(完整)、hotkey_guardian、hotkey_scaling。ditto_card_ocr/pixel_picker 已於 3B 審過（不重審）。→ §5【✅ 審查完成 2026-07-10：無 P1、1×P2 潛伏＋7×P3，批次見 §5C】
- **階段 4 — 跨程式基礎設施**：§1 表中「跨程式基礎設施」全部（一 bug 影響五程式，優先度僅次於醫療計算）。splash.py 白框殘影已實機回報修正（v2026.07.09.3+v2026.07.10.1），重審時可跳過該檔。→ §6【✅ 審查完成 2026-07-10：1×P1（更新器混版本）＋6×P2＋16×P3，修正批次見 §6G】
- **階段 5 — 主程式其餘＋入口/守護**：main.py 未審部分、scheduler.py、watchdog_runner.py、6 個 .pyw 入口。→ §7【✅ 審查完成 2026-07-10：1×P1（launcher 零兜底）＋4×P2＋P3 若干，批次見 §7C】

**五階段審查已全部完成（2026-07-10）**，合計約 **8×P1＋22×P2＋40+×P3**。修正執行全局順序見 §8。

---

## 3. 階段 1 findings — UVB 醫療計算

> 狀態：**✅ 審查完成（2026-07-10，C/D 均回報）**——**5×P1＋9×P2＋13×P3**，修正批次見 §3E。
> C＝`uvb_dose.py` 全檔（excimer 多段以外）；D＝`main.py` F1-F3 寫回鏈。
> **UC-01/03/04 三條 P1 經主審獨立重跑確認屬實**（850→700、遞減變加量、830>上限800）。
> 已排除重報：excimer 多段（v2026.07.09.4 已修+codex 7 輪）、H4/M2/M4（v2026.07.09.5/.6 已修）。
> 期間實機回報的 excimer 多段劑量 bug 已修正上線，該路徑已深讀。

### 3D. main.py F1-F3 寫回鏈（agent D 回報，2026-07-10）

> D 已交叉讀 uvb_dose 的 `uvb_written_back_ok`/`apply_uncertain_updates` 與 watchdog/interrupt 鏈。無 P1；
> 6×P2 中四條同屬「F12 取消尊重」縫隙（H4/W1/W7 既有機制沒蓋到的縫），修法都是小改動。

#### UD-01 [P2] `_autofill_卡號_from_醫師上次` 吞 SubsystemInterrupted → 卡號 OCR 期間 F12 失效
- `main.py:4366-4367`（外層 `except Exception`）；call site 2559(F2)/2586(F3)。內部走 `_wait_for_window`→`_sleep_interruptible`→`check_stop` 會拋，被外層吞掉照常返回 → F2/F3 續跑 51019 前置（搶前景/切代碼輸入模式）。與 H4 同型破口（H4 只修了 `_f23_pure_excimer_update`）。OCR 開「醫師上次」+截圖耗時數秒＝醫師最可能按 F12 的窗口。
- 修法：`except SubsystemInterrupted: raise` 置於 except Exception 前。信心高。

#### UD-02 [P2] 純 excimer 確認窗按「否」後無 check_stop → F12 後仍寫身份=01、狀態列顯示「操作完成」
- `main.py:2103-2116`（No 分支 fall-through 到 `return _F23_PURE_EXCIMER`）→ caller 2553-2556/2580-2583 設身份。Yes 分支有 check_stop（H4），No 分支沒有；且 interrupt 已清 `_subsystem_running` → 舊 worker 寫身份欄同時醫師可啟動新熱鍵＝兩流程並行操作 HIS（W1 力避的情境）。
- 修法：`_photo_confirm_yesno` 返回後不論 Yes/No 一律 check_stop；`_set_身份_自費` 開頭補 check_stop 更保險。信心高。

#### UD-03 [P2] UVB 已寫回後，51019 階段以例外/F12 收場時 W7 半套警告被繞過
- `main.py:2560-2566`（警告只掛在 `if not ok` 分支）＋13211-13216（wrapper 對例外只改狀態列）。`_script_code_input_adaptive` 內三個 check_stop 拋 SubsystemInterrupted 直接冒泡 → 「UVB 已改 X→Y、請補 51019 或改回」的精準警告不會出現，病歷已改+醫令沒進的半套狀態無人把關。
- 修法：`script_F2/F3_adaptive` 對 51019 階段包 try/except：攔到例外且 `_last_uvb_write` 有本 label 紀錄 → 先 `_show_light_code_incomplete_warning(..., uvb_already_updated=True)` 再 re-raise。信心高。

#### UD-04 [P2] 主路徑寫回處置欄前無最終 check_stop 閘門
- `main.py:2366`（core 直達 UPDATED 的 `_write_tmemo_text`）；同類 2053（excimer 無確認窗寫回）。從 wrapper check_stop 到寫回之間要過「找主視窗+列舉控件+逐一 gettext(各 2.5s 上限)+parse」，HIS 慢時達數秒，期間 F12 不被理會。寫回是全鏈唯一直接改病歷的動作，卻沒有取消閘門。
- 修法：兩個 call site 寫回正前方各加 `check_stop()`（成本近零、fail-closed）。信心高。

#### UD-05 [P2] 療程欄定位純幾何+寫後驗證自我參照 → 版面漂移時把 1/2/3 寫進別的窄欄且驗證照樣過
- `main.py:1655-1685`（`_set_療程_only`）＋3842-3901（`_find_療程_edit_hwnd`：top row 幾何、最左窄欄=療程）。`_set_身份_自費` 已為同型破口補「寫入前正向辨識原值樣式」（3989-3993 註解自承理由），療程欄沒補。HIS 改版/DPI 讓窄欄增減 → "2" 寫進「類別」等欄、read-back 讀同一錯 hwnd → 驗證通過。且身份欄以療程欄為錨（3915）會連帶錯。
- 修法：比照身份欄，寫入前讀原值正向把關（療程原值=空白或 1 位數字）；可加「同排存在第二個窄欄」結構性確認。信心中（需版面漂移，但屬自家已認證的破口類型）。

#### UD-06 [P2] `_wait_for_code_input_focus` 有 previous_focus 分支仍收任意 edit/memo/rich
- `main.py:1464-1469`。M2 只收緊了無前焦點分支；有前焦點時「menu id 漂移(有前例)+0.6s 窗內焦點移到別的輸入欄」→ 51019+Enter 打進病歷欄。
- 修法：兩分支都優先正面辨識 grid 編輯器；至少排除 memo/rich（代碼欄絕非多行控件）。可先加 log 蒐集實機 class 名單再收緊。信心中。

#### UD-07~15 [P3]（摘要）
- **UD-07** 確認窗 Yes 同時帶兩個 skip flag：stale 確認過了連 dose>1500 確認也跳過（2249-2250/2089-2090）。修法：只帶本次對應 flag，回另一種 CONFIRM 再跳窗。
- **UD-08** 寫回後 read-back 空字串時 verify 整段跳過照樣續跑 51019（2375-2376）；HIS 卡頓時「寫回失敗+51019 已下」反向不一致無提示。修法：空 read-back 至少警告或保守中止。
- **UD-09** F1 情境 TOO_CLOSE/PARSE_FAIL/SANITY_FAIL 文案未提醒「51019+療程1 已下」（2291-2300 等）；醫師若不照光可能忘刪醫令。修法：依 strict 分流文案。
- **UD-10** uncertain 行套用失敗靜默 fallback（2361-2363），與醫師剛按「是」的承諾不符。修法：fallback 附警告。
- **UD-11** W7「(B)改回原值」指引在 uncertain 行也被更新時不完整（1875-1876）。修法：rec 記行數、文案追加。
- **UD-12** `_send_chars_to_window` 不檢查 PostMessageW 逐字回傳（1533-1536）：編輯器中途被關仍回成功、代碼欄殘留半截。修法：檢查回傳+IsWindow。
- **UD-13** `_show_uvb_warning` MessageBoxW 未包 `_hotkey_awaiting_user_scope`（1807）→ watchdog 誤報 keep_stuck（純 UX）。
- **UD-14** F1 route 偵測例外→normal 與 core 二次分流不一致：51019 已下+excimer 行已+1、無矛盾警示（2028-2031/2200-2204/2539）。修法：core 回 _F23_PURE_EXCIMER 時跳人工核對警告。
- **UD-15** 純 excimer 寫回無 read-back 驗證（已知設計，列備查；可加全文弱比對）。

**正面觀察（穩健勿動）**：W7 對帳鏈（開頭清空/verify 過才記/比對 label）；`uvb_written_back_ok` 三方向收緊無漏洞；uncertain triplet span 一致性（D 特別驗過無錯位）；`_photo_confirm_yesno` MB_DEFBUTTON2+失敗當否+owner=main_hwnd（確認窗期間 HIS 被 disable，「Yes 後蓋掉醫師手改」實質被封）；H4/M2/M4 修正現況到位；`_set_身份_自費` 是全鏈把關最完整的欄位寫入；卡號 autofill 計費安全設計（除 UD-01）；W1 watchdog token 守衛；F2/F3「全過才 commit 51019」fail-closed 順序。

**已看過無明顯問題**：`_write_tmemo_text`/`_wm_settext/gettext_timeout`、`_send_enter_to_window`、`_show_light_code_incomplete_warning` 三分支、`_f1_pure_excimer`、script_F1/F2/F3 回傳分流、`_record_uvb_write`、update_uvb_in_text round-trip verify、`_resolve_phototherapy_disposition`、跨緒 MessageBox（worker 只用 Win32 無 Tk）。

### 3C. uvb_dose.py 核心（agent C 回報，2026-07-10）

> **全部 finding 皆以實際執行 repo 函式重現**（agent C probe）；UC-01/03/04 另經主審獨立重跑確認屬實。
> 共同主題＝**欄位歸屬**（跨行縫合、日期解析失敗≠無日期、裸數字 each time 無方向、till/until 吃年份、
> 行首括號當次數）。分流/規則引擎/驗證網本身紮實，破口集中在 parser 前段。

#### UC-01 [P1] 跨行欄位縫合：UVB 行缺 MAX 時 borrow 下一行別的治療的 MAX/increase/日期 【主審已重跑屬實】
- `uvb_dose.py:822`（`parse_uvb_line` 的 `_UVB_MAX_RE.search(text, dose_start)` **不限同一行**）＋833-834/841-861/870。
- 主審實測（today=7/10）：`"UVB: 850 mj/cm2 keep\nexcimer 500 mj (10) on (2026/7/8) add 30 MAX 700"` → 寫回 **`UVB: 700 keep`（850 被改成 700！）**＋excimer 行 count/日期被動、excimer 自己的 500 反而沒加。「keep UVB」本應 SILENT_SKIP（v20.17 梁雯琳 case），混合病歷是常態。round-trip verify 用同一 parser 重 parse 同樣縫合＝**驗證共享盲點照樣通過**。`uvb_written_back_ok` 註解(1731-1733)早已點名此問題並改逐行，但主 parse 沒同步。
- 修法：MAX 搜尋上界＝dose 所在行行尾（`text.find("\n", dose_start)`）；同行無 MAX → 維持 SILENT_SKIP/PARSE_FAIL。

#### UC-02 [P1] 日期「存在但解析不出」被當無日期 → 假 first-time 繞過 TOO_CLOSE/decay/stale 全部間隔防線
- `uvb_dose.py:841-851`（strict：第一個日期形狀解析失敗就整個放棄，不試下一個）、944-953（partial 同）、1855-1867。
- agent C 四種輸入實測：裸民國 `on 115/7/9`（昨天！）→ UPDATED +30；點分隔 `(2026.7.9)` 同；7 位病歷號 `(1234567)` 先被 ROC-concat 咬到 ValueError → 放棄後面合法日期；8 位 `(20260708)` 被當 count → **寫回 `(20260709)`（把日期樣數字+1）**。均為劑量/次數已改、行內舊日期留存＝病歷自相矛盾；first-time 無 round-trip verify。
- 修法：(a) `_first_time_update` 前掃 segment 日期殘跡（`\d{2,4}[./-]\d{1,2}[./-]\d{1,2}`、括號 7-8 位純數字），有 → PARSE_FAIL 交醫師；(b) 日期解析失敗改 finditer 試下一個；(c) first-time 補 count sanity。

#### UC-03 [P1] `decrease/reduce/taper N each time` 被當 +N（遞減醫囑方向反轉）【主審已重跑屬實】
- `uvb_dose.py:294`（`_UVB_INCREASE_RE` branch (b)「數字+each time/每次」無方向判斷）。
- 主審實測：`"UVB 800 ... decrease 50 each time, MAX: 1000"` → **寫回 850**（應 750）。變體 reduce/taper/-50/減50 全中。另 first-match-wins：`"UVB: 150 mj/cm2 each time, add 20"` → increase=150（劑量本身）→ 寫回 300（應 170）。
- 修法：branch (b) 檢查數字前 N 字內有無 decrease/reduce/taper/減/降/`-` 即否決；且 branch (b) 只在無 add/increase 關鍵字命中時才採用。

#### UC-04 [P1] `till|until` 無 \b＋可吃日期年份 → 假 MAX 突破醫師上限 【主審已重跑屬實】
- `uvb_dose.py:311`（同組複製於 322-332/359-369）。
- 主審實測：`"UVB 780 ... add 50, treat until 2026/9/1, MAX: 800"` → max=**2026（年份）**→ 寫回 **830 ＞ 醫師上限 800**。另 `still 900` 內含 till → 無 MAX 行被擅自 UPDATED。
- 修法：`till|until` 前加 `\b`；捕獲數字後加 `(?!\s*[/-]\d)` 擋日期；**三處同組 regex 同步改**。

#### UC-05 [P1] 行首編號 `(1)` 被當次數：編號 +1、真次數不動、verify 照樣過
- `uvb_dose.py:856-861`（v20.15 segment 擴到行首的副作用）。實測 `"(1) UVB: 500 (25) on (2026/7/8) add 30, MAX: 800"` → 寫回 `(2) UVB: 530 ... (25)`。條列式處置中文病歷常見。
- 修法：count 先在 dose 之後找，dose 前括號只在其後找不到時才接受（line 841 日期已有同策略先例）。

#### UC-06 [P2] first-time（無日期）路徑跳過全部 sanity：increase>200/dose<50/count>999 直接套用
- `uvb_dose.py:1610-1648/1855-1867`。實測 `add 500` → 一次 +500→1000 靜默寫回（dated 路徑同值 SANITY_FAIL）。與 UC-02 假 first-time 風險加乘。修法：進入前套用同款 sanity。

#### UC-07 [P2] Step C 跨日期「劑量=driver MAX」續行不 decay，8-30 天前段落直接 date→今天
- `uvb_dose.py:2096-2114`。12 天前 `scalp 800(=cap)` → 寫回今天照 800（規則應 ×0.75=600）。修法：cross-date 續行加 days_diff 桶檢查，≥8 天列入 uncertain 走 Yes/No。

#### UC-08 [P2] Step B 附加行無 round-trip＋重複日期時 count +2、行首歷史日期被改
- `uvb_dose.py:2049-2056`＋`format_uvb_line:1099-1104`（`src.find(date_text)` 換第一個）＋Step C 疊加。實測 count 5→7。修法：format 日期替換改用 parse 時的 span；或 Step B 加 mini round-trip 失敗即 SANITY_FAIL。

#### UC-09~12 [P3]（摘要）
- **UC-09** 模組 docstring 劑量規則過時（11-16 漏 15-21 ×0.5 桶）。
- **UC-10** `_TRIPLET_RE`/`_detect_uncertain_triplets` 只認 4 位西元年 → 民國 triplet 漏更新（安全方向但不同步）。
- **UC-11** dose>MAX 矛盾病歷：day-7 保持 900、2-6 天靜默壓回 800 行為不一致 → 建議 CONFIRM_NEEDED。
- **UC-12** UVB 路徑 Step D 丟棄 excimer too_close 訊號（2151，設計取捨列備查）。

**正面觀察（穩健勿動）**：`compute_new_dose` 全邊界實測正確（衰減永不增量、不超上限）；round-trip verify 是真安全網（實測擋下兩種寫壞）；`_CEILING_KEYWORD_BEFORE_RE` 有效；`_resolve_date_match` 民國窗保守；`load_and_apply_uvb_rules` 嚴格型別+整組單調性；`uvb_written_back_ok` 防巧合同值；純 excimer TOO_CLOSE 優先；`apply_uncertain_updates` span 無漂移。

**已看過無明顯問題**：`combine_phototherapy_kinds`、`_replace_uvb_dose`、`_today_in_format`、`strip_stale_phototherapy_segments` 群、`detect_phototherapy_kind`/`_has_dosed_uvb_specific`（D-case 誤入危險路徑的根因在 parse 縫合，非分流）、`_first_fresh_uvb`、`_stale_confirm_result`。

### 3E. 階段 1 修正批次規劃（給 Opus 4.8）——最高優先，先於階段 2 批次

| 批次 | 項目 | 備註 |
|---|---|---|
| **UVB批次1（醫療急件）** | UC-01、UC-04、UC-03 | 三條「寫回錯劑量/突破上限/方向反轉」＝實測重現的實害；同組 regex 三處同步改；每條先加 agent C probe 的重現輸入為回歸測試（紅→綠） |
| **UVB批次2（醫療急件二）** | UC-02、UC-05、UC-06 | 假 first-time 繞過間隔防線＋行首編號＋first-time sanity；UC-02 修法保守側（PARSE_FAIL 交醫師） |
| **UVB批次3（取消尊重）** | UD-01、UD-02、UD-03、UD-04 | 四條 F12 縫隙，修法均為小改動（re-raise / check_stop / W7 包裝） |
| UVB批次4（把關補齊） | UD-05、UD-06、UC-07、UC-08 | 療程欄正向辨識（比照身份欄）、focus 收緊、Step B/C 邊界 |
| UVB批次5（P3 擇修） | UD-07~15、UC-09~12 | 執行者裁量；UC-09 docstring 順手修 |

執行紀律同 §0：**uvb_dose.py 屬最高風險檔**——每個小項改完立即 commit 分支+備份 patch；每批 pytest 全綠（含新增紅→綠回歸測試）→ codex → push_helper；修 UC 群時**絕不可動**「正面觀察」列出的機制。

---

## 3B. 已完成 — 主程式 F 鍵 HIS 自動化＋OCR/pixel_picker（原屬階段 5 範圍，提前完成）

> findings 全文見《未審review_findings_主程式F鍵HIS自動化_2026-07-09.md》。**修正已全數上線**：
> - **H1–H5（P1×5）** → v2026.07.09.5（commit 7b3aa5f）：F9/F10 只對 HIS 行程 #32770 按「是」(require_pid)；
>   同意書 round2/3/4 失敗即中止不送出；`_switch_tab_by_text` 回真實 success（swap 失敗不開立）；
>   純 excimer 確認框後 check_stop＋SubsystemInterrupted re-raise；轉診取樣前 WindowFromPoint 驗遮擋。
> - **M1–M6＋L1/L3/L5** → v2026.07.09.6（commit b1abaa4）：HIS 版本守門（偵測＋警示，刻意不硬停）；
>   代碼輸入 focus 正面辨識 grid 內嵌編輯器；popup watcher (hwnd,class)+身分去重+retry 綁身分；
>   `_replace_edit_text` 前景＋遮擋驗證；precheck 全形正規化；winsdk runtime 安裝改 opt-in＋CREATE_NO_WINDOW；
>   未滿 18 dialog 多鈕防誤點；OCR PHI 暫存 try/finally 保證清除；#32770 熱鍵前景加 HIS 行程把關。
> - L2（pixel_picker 死碼）/L4/L6（刻意行為）按 finding 不動。
> - 回歸測試：tests/test_main_fkey_guards.py（7）＋tests/test_main_ml_guards.py（16）＋tests/test_ditto_card_ocr_cleanup.py（5）。
> - **殘留已知限制**（已文件化於 code）：M3 同 class＋同標題＋同子視窗數的瞬間 hwnd 重用無法區分（Win32 無
>   per-instance id；實際 F11 handlers 各不同 class，幾乎不會發生）；M1 硬停＋醫師對話框待實機確認 title 格式後再補。
> - ⚠ 建議實機抽驗：F9/F10 同意書全流程、切 tab 失敗不誤送、F11 轉診被 Chrome 蓋住不誤判、代碼輸入不 key 錯欄位。

---

## 4. 階段 2 findings — 診間即時資料鏈

> 狀態：**✅ 審查完成（2026-07-10，A/B 均回報）**。
> A＝`floating_clinic.py`＋`main.py` 顯示/輪詢鏈；B＝六個資料/快取 helper 全檔。
> 行號以 2026-07-10 工作樹（v2026.07.10.1 後）為準；除 FC-01 經主審抽驗屬實外，
> 其餘 finding 未經二層對抗驗證，Opus 開工前先重讀該段確認現況成立。

### 4B. helper 層（agent B 回報，2026-07-10）

**總評**：六檔幾乎全是純函式/純轉換，檔案讀寫全部委派 `atomic_io`（tmp+fsync+os.replace、utf-8、corrupt 自動 backup）→ 跨程式原子性/cp950 在這層**無破口**。無 P1。

#### CL-01 [P2] `clinic_int_count` 對 float NaN/inf 直接拋例外（fail-open 破口）
- `reg64_utils.py:163-164`。float 分支 `int(value) if value == int(value) else default` 不在 try 內：`int(nan)`→ValueError、`int(inf)`→OverflowError。`json.load` 預設接受 NaN/Infinity 字面值，快取檔數值欄壞掉重載即可觸發 → 上層計數/顯示中斷而非回保守值。
- 修法：float 分支加 `math.isfinite(value)` 防護，非有限回 default。信心中。

#### CL-02 [P3] `duration_stats` 單一極端值污染平均（離群過濾回退把離群放回）
- `clinic_history.py:14-19`。離群帶以「含離群的均值」定帶：`[300,300,300,36000]`→帶 [4612,18450]→全部被排除→回退**全體**（含 36000）→平均 153 分。顯示「本診平均/估計剩餘」被單一卡住的計時嚴重拉高。
- 修法：回退用中位數，或以中位數為中心裁剪。信心中。

#### CL-03 [P3] `reg64_time_code_from_local_clock` 時段切換早約 1 秒
- `reg64_utils.py:28-32`。`cur <= 12:59:59` 含微秒比較 → 12:59:59.000001 已落下一時段，與註解「13:00 轉下午」不符。修法：改 `cur < 13:00` 半開區間。影響 <1 秒，信心高。

#### CL-04 [P3] `is_near_alert_threshold` 忽略 `is_stopped`（止掛時段仍發「快滿」警示）
- `threshold_policy.py:54-70,91-105`。跳過條件只看「休診/停診」字樣，不看掛號 dict 的 `is_stopped`（main.py:6152-6159 有此欄）→ 已止掛（不會再增號）仍因既有數接近門檻而誤發提醒。
- 修法：dict 且 `is_stopped` → continue。信心中。

#### CL-05 [P3] `is_near_alert_threshold` 無 session 正規化（用語漂移脆弱點）
- `threshold_policy.py:73-105`。門檻 key 用「上午/下午/晚上」，比對直接拿原字串；同 repo 另一套用語「早上」並存。**現況安全**（掛號來源全部產「上午」），但上游改字串會靜默永不觸發。修法：接受 canonical_session 正規化後比對，或加測試守住不變式。信心中。

#### CL-06 [P3] `normalize_clinic_rooms` 全空清單不補預設
- `clinic_state.py:24-45`。5 格全空 → 原樣回傳 → 診間 key 變 `"/時段"` 錯位。需使用者手動清空全部才會發生。修法：逐格 `rooms[i] or DEFAULT[i]`。另 :39-41 legacy 檢查是死碼（可清）。信心中。

#### CL-07 [P3] `all_time_average_text` 對非數字元素拋 TypeError
- `clinic_history.py:234`。`if x > 0` 在 float() 之前，混型（字串）輸入 `"12" > 0` 直接 TypeError 而非回 `-`。修法：先 coerce 再比較。信心低（需混型輸入）。

**已看過無明顯問題**：`appt_utils.py`（合併/去重/dayoff 自洽；去重 key 含 count 屬刻意）、`master_schedule_cache.py`（fetch 失敗保留舊快取、TOCTOU 收斂保守側）。

**正面觀察（穩健，不要動）**：持久化全走 atomic_io（勿在 helper 自造輪子）；coerce 幾乎全面 fail-safe；殘留盤面/跨日/拖班邊界處理正確（`is_residual_stale_closed`/`overrun_effective_time_code`）；`safe_load_json_ex` 的 missing/corrupt/error 三態契約（防毒鎖檔≠損壞，不可用預設覆寫）值得保留。

### 4A. 顯示/輪詢鏈（agent A 回報，2026-07-10）

#### FC-01 [P1] 浮動視窗燈號未正規化 `0`/`--` → 解析失敗顯示成 32pt 大字「0」誤導醫護 【主審已抽驗屬實】
- `floating_clinic.py:141`（room_card_view 只 `strip() or "—"`）＋`main.py:10506`（`_capture_floating_status` 原樣塞 `result["light"]`）。
- 根因鏈（四環節均已驗證）：`fetch_clinic_light_status` regex 抓不到燈號時**預設 `light_num="0"`**（main.py:9453，頁面改版/半頁即發生）→ capture 原樣入 RoomStatus → 浮窗 hero 大字顯示「0」。對照主 UI `update_single_clinic_ui`（main.py:10222-10223）**刻意把 `"0"`/`"--"` 正規化成紅字 `--`**——同一個值主 UI 顯示 `--`、浮窗顯示「0 號」。醫師瞥浮窗會誤以為「才剛開診/看到 0 號」，實為解析失敗。
- 修法：`_capture_floating_status` 組 RoomStatus 前把 `"0"`/`"--"` 收斂成空字串（room_card_view 既有 `or "—"` 會補佔位），與主 UI 對齊。

#### FC-02 [P2] `_persisted_session_overrun_state` 不驗日期 → 跨午夜誤判早診拖班
- `main.py:8442-8454`（被 `_overrun_effective_tc:8438` 呼叫）。直接讀 `_clinic_dynamic_state_cache` 的 `had_any_activity`/`is_ended`，**沒比對 state 的 `date`**；對照主要讀取器 `_get_clinic_dynamic_state:8004` 走 `_clinic_dynamic_state_matches`（含 today 檢查）。程式跨午夜執行且昨日早診「有活動但沒落 is_ended」（當時崩潰等）→ 今日誤判早診仍拖班、把昨天早診當「還在看」。觸發窗較窄。
- 修法：讀到 state 後加 `state.get("date") == self._clinic_dynamic_today_str()`，非今日回 `(False, False)`。

#### FC-03 [P3] `_reg64_room_reachable` 跨緒無鎖（理論 dict-changed-size；呼叫端 try/except 已兜住，最壞一輪不隱藏）
- 寫 `main.py:9614`（bg worker）、讀 `main.py:10482`（主緒迭代 `.items()`）。修法（選配）：讀端 `list(...items())` 快照。

#### FC-04 [P3] 浮窗每 15 秒無條件全量重建卡片 → 週期性閃爍/無謂重繪（拖曳時每 motion 重建一次）
- `floating_clinic.py:504-507`（update_rooms 無 dirty-check）→ `577-612`（_render 全砍重畫）。無洩漏（有 destroy）。修法：比對關鍵欄位相同就跳過 _render；拖曳中只 _reposition。

#### FC-05 [P3] `_ensure_shown` 400ms after 未取消；`exists()` 單邊死亡走裸 destroy 繞過清理（時鐘 after 未取消、geometry 未存檔）
- `floating_clinic.py:371, 630-649, 651-670`。修法：存 after id 並於 destroy() cancel；exists() 清殘窗改走 self.destroy()。

#### FC-06 [P3] 手動高度不還原（_parse_geo 丟高度）；單邊死亡後 tick 停止不自癒（浮窗靜默消失需手動重開）
- `floating_clinic.py:391-399, 614-619`；`main.py:10357-10380`。修法：_parse_geo 讀回高度設 _manual_h；tick 偵測 not exists() 且 mode==floating 時重建一次（防無限迴圈）。

**正面觀察（穩健，不要動）**：背景緒完全不碰 Tk（純字串進 worker、UI 全走 root.after 回主緒——勿改）；`_floating_status_by_room`/`_floating_error_streak` 全主緒讀寫故無鎖正確；`_reg64_public_snapshot`/`_clinic_dynamic_state_cache` 有正確鎖與 rebind 設計；日期驗證做在讀取端（唯一例外=FC-02）；fail-open 徹底（stale cache、streak≥3 且他診可達才隱藏）；ctypes/Win32 細節嚴謹（GA_ROOT/GetWindowLongPtrW 64-bit/SetWindowRgn 所有權——勿簡化）；`_clinic_lights_worker_running` 單 worker 保證。

**已看過無明顯問題**：`_clinic_refresh_seconds`/`_reg64_micro_ttl_seconds`、clinic dynamic state 其餘方法群、`_update_reg64_public_cache`/`_refine_clinic_reg64_for_display`/`_ensure_clinic_room_model_from_disk`/`_start_clinic_lights_polling_once`/`_clinic_tab_first_paint_refresh`/`_reg64_total_for_calendar_cell`、floating_clinic 純函式群。

### 4C. 階段 2 修正批次規劃（給 Opus 4.8）

| 批次 | 項目 | 檔案 | 備註 |
|---|---|---|---|
| 診間批次1（必修） | FC-01(P1)、FC-02(P2)、CL-01(P2) | main.py、floating_clinic.py、reg64_utils.py | 顯示誤導＋fail-open 破口；各加回歸測試 |
| 診間批次2（值得修） | CL-02、CL-04、FC-05、FC-06、CL-06 | clinic_history/threshold_policy/floating_clinic/clinic_state | 顯示品質＋UX 自癒 |
| 診間批次3（選配） | CL-03、CL-05、CL-07、FC-03、FC-04 | 各檔 | 邊界/防禦性/效能，執行者裁量 |

每批依 §0 守則：pytest 全綠 → codex review → push_helper；main.py 項目改完立即備份 patch 到 %TEMP%。

## 5. 階段 3 findings — 獨立小程式＋影像/座標/卡號

> 狀態：**✅ 審查完成（2026-07-10）**——**無 P1**、1×P2（潛伏死路徑）＋7×P3。
> 範圍：`coord_detector.py`/`punch_status.py`(全檔)/`hotkey_guardian.py`/`hotkey_scaling.py`；
> 已交叉核對呼叫端（main.py guardian 迴圈、consult_query 打卡信件消費端）與既有測試。
> ditto_card_ocr/pixel_picker 已於 3B 蓋過不重審。

#### SP-01 [P2·潛伏] hotkey_scaling：`configure_hotkey_scaling` 不驗 target_size → `(0,0)` 使 scale=0.0、所有座標縮到左上角
- `hotkey_scaling.py:48-59`。`not target_size` 擋不住 `(0,0)`（truthy）；實測 scale_x=scale_y=0.0、`_scaled_xy(960,540)`→`(0,0)`；`(None,...)` 還會拋 TypeError。**唯一救贖：現況全 repo 只以 `(False, None, None)` 呼叫**（2026-05-19 已註記縮放邏輯已死）→ 降 P2 潛伏。若日後重新啟用＝F11 全部點到螢幕左上角（HIS 誤點）。
- 修法：target_w/h ≤0（含 int 失敗）一律視同 disabled＋warning；或把啟用路徑整段移除。信心高（已實測）。

#### SP-02~08 [P3]（摘要）
- **SP-02** punch_status：「今日無紀錄」與「Gv_attppre 表消失/改名」不可區分 → portal 再改版時整批誤標「❌未打卡」（假遲到警報；僅信件顯示、不驅動打卡動作故 P3）。修法：JS 回 `{tableExists, rows}` 或消費端 canary（全帳號全 FAIL 時加「可能系統改版」提示）。
- **SP-03** hotkey_guardian：idle 門檻僅 3s＋重啟計數不跨進程 → hook 死因修不好時每輪 ≥4 分的慢速無限重啟循環（非風暴）。修法：idle 提高 30-60s；計數持久化（同日超過 N 次放棄改人工提示）。
- **SP-04** hotkey_scaling 模組級 dict 無鎖、多鍵讀非原子（現況 disabled 無害；重新啟用時的競態）。修法：不可變整包替換。
- **SP-05** coord_detector：keyboard hook 緒直呼 `root.after`（跨緒；關窗瞬間按 F8 可拋例外，實害極低）。
- **SP-06** coord_detector：取色失敗沿用上一位置顏色且 `_last_pos` 照更新 → F8 無聲記錄錯 RGB（下游餵 F11 比色）。修法：失敗顯示「讀取失敗」或不更新 _last_pos 讓 50ms 後自動重試。
- **SP-07** punch_status：登入逾時的 alert 一律標「帳號/密碼錯誤」→ 該字串使 `_is_retryable_punch_error` 判不可重試，暫態問題失去重試。修法：回 alert 實際文字。
- **SP-08** coord_detector 無單例（多開僅重複記錄，無實害，優先度最低）。

**正面觀察（穩健勿動）**：punch_status fail 方向正確（消費端先檢 error 才渲染三態；全 repo 無任何打卡動作依賴本模組；「已打」誤報方向堵得住）；時間窗邊界（07:30/12:40 含端點）是刻意設計且有測試釘住勿動；guardian 探針「死 hook 不可能被誤判活＋不誤殺＋連續 2 次＋idle＋冷卻＋上限」＝無重掛風暴；GetTickCount 32-bit 回繞處理正確；punch_status 韌性層完整（TCP 預檢/預算/cookie 清理/密碼不落 log）；coord_detector DPI 座標系一致性（per-monitor v2 與 main 一致）是「記的座標不點錯」的根基。

**已看過無明顯問題**：guardian 其餘純函式（測試釘住）、punch_status 的 weekday_prefix/_hhmm_to_time/classify/scheduled_today/evaluate_account、query_accounts_today deadline 邏輯（單帳號可超預算 1-2 分屬已註記取捨）。

### 5C. 階段 3 修正批次規劃（給 Opus 4.8）
- **小程式批次1（低量順手修）**：SP-01（防呆或移除死路徑）、SP-02（canary）、SP-07（alert 文字）、SP-06（取色失敗顯示）。
- 其餘 SP-03/04/05/08 選配，執行者裁量。無急件。

## 6. 階段 4 findings — 跨程式基礎設施

> 狀態：**✅ 審查完成（2026-07-10，E/F 均回報）**——1×P1＋6×P2＋16×P3，修正批次見 §6G。
> E＝更新/儲存/併發核心；F＝通訊/環境/UI（`deps_installer/runtime.py` 實為 `deps_installer.py`+`deps_runtime.py` 兩支皆審；splash 已修跳過）。
> 兩 agent 的關鍵疑點均以 Python 實測驗證；finding 未經二層對抗驗證，Opus 開工前先重讀確認。

### 6F. 通訊/環境/UI（agent F 回報，2026-07-10）——無 P1

#### IF-01 [P2] tk_exception class-level patch 簽章錯誤：handler 被呼叫必拋 TypeError、原始例外遺失
- `tk_exception.py:18,43`。3 參數普通函式指派為 class attribute → descriptor 綁定後 instance 呼叫多塞 self=4 引數 → 必炸 TypeError（agent 已實測：instance-attr 路徑正常、class-attr 路徑必炸）。現況四個呼叫端都有傳 root（instance attr 有設）＝**潛伏彈**；docstring 明文支援 `root=None` 只 patch class 的用法，未來第二個 Tk root 或 install(None) 就踩中 → pythonw 下例外完全不可見且不進 log（本模組要修的問題在此路徑原封不動）。
- 修法：`def _report_callback_exception(*args): exc, val, tb = args[-3:]` 相容 3/4 引數；或 class patch 用帶 self 簽章。信心高。

#### IF-02 [P2] smtp_credentials.json 被 BOM/ANSI 存檔後遭 corrupt-rename → SMTP+IMAP 一次全滅
- `smtp_mail.py:143`（`load_credentials`→`safe_load_json` 預設 `backup_on_corrupt=True`）＋`atomic_io.py:117-130`。官方流程=手動記事本編輯貼 App Password；存成 UTF-8 BOM → JSONDecodeError（實測）、存成 ANSI/cp950（from_name 中文）→ UnicodeDecodeError（實測）→ **帳密檔被 rename 成 .corrupt-<ts>**、回 {} → is_configured()=False → 會診收信+寄信同時靜默停擺（CQ-01 型），診間無人看 log。密碼仍在 .corrupt 檔可救、未外洩。
- 修法：讀檔容忍 BOM（utf-8-sig）；credentials 檔改 `safe_load_json_ex` 且 **backup_on_corrupt=False**（corrupt 保留原檔+明確 UI 告警）——「讀壞就把唯一帳密搬走」對 credentials 是錯誤預設。信心高。

#### IF-03 [P2] show_windows_notification 阻塞式 MessageBox 在 health monitor 緒 inline 呼叫 → RAM 自動重啟保險絲失效
- `notifications.py:14`（MessageBoxW 無 timeout）＋觸發鏈 `main.py:14196-14203`（`_ram_restart_warn`）→ `health.py:238`（warn_callback 在監看緒 inline）。診間無人按掉「記憶體偏高」彈窗 → `_health_loop` 卡死不再 tick → consecutive_critical_ram 永遠到不了門檻 → **RAM 失控自動重啟在最需要的無人場景失效**（main 無外層 watchdog 接手）。對照 main.py:4139 另一處已知要包 thread。
- 修法：notifications 提供非阻塞變體（daemon thread）或 MessageBoxTimeoutW；`_ram_restart_warn` 包 thread；docstring 標明勿在監看緒直呼。信心高。

#### IF-04~12 [P3]（摘要）
- **IF-04** imap search 用 sequence number 非 UID（變數卻叫 uid）：並行 client expunge 時可能標錯封已讀（後果有限：觸發信留 UNSEEN 下輪重掃+dedup 擋重複）。修法：全程 `conn.uid(...)`。
- **IF-05** 中文 keyword 的 server-side SEARCH 必失敗（imaplib ascii 編碼先炸，UTF-8 重試 dead code）→ 永遠走全 UNSEEN 逐封 fetch 後備路徑；功能正確但信箱堆積時變慢。修法：刪注定失敗的嘗試或用 literal；掃描加上限。
- **IF-06** http_client：`get_shared_session`/`disable_insecure_warnings` 死碼；`is_internal` 用 netloc.split 有三坑（大小寫/無 scheme/**userinfo 欺騙 `https://10.20.8.47:x@evil.com` 判 internal**——實測）；現況無使用者輸入路徑無現行攻擊面。修法：`urlparse(url).hostname`；死碼刪或補強制 timeout wrapper。
- **IF-07** smtp 對永久性錯誤（RecipientsRefused 等 5xx）照樣 backoff 重試；DATA 後斷線重試可能重複寄（符合寧可多提醒）。修法：5xx 歸入不重試組。
- **IF-08** `use_tls=False` 且非 465 時密碼明文 AUTH 無阻擋（需明確錯誤設定才觸發）。修法：非 localhost 拒絕或警告。
- **IF-09** deps_installer：requirements.txt 缺席時靜默降級裸套件名（無 pin 抓最新版，M6 同型）＋直接裝共機 site-packages。修法：缺席改警告+內建最低 spec 表。
- **IF-10** logging_setup 三邊角：basicConfig 被第三方庫搶先時靜默 no-op；pythonw 下 StreamHandler(None) 殭屍 handler；AV/OneDrive 鎖檔時 rollover 靜默掉 log。多進程共寫風險不存在（五程式各自 log 檔已逐一確認）。
- **IF-11** paths.restart_self 的 os.execv fallback 對含空白路徑參數斷裂（Popen 已失敗後的最後手段，極端路徑）。
- **IF-12** 註解漂移：deps 失敗退出碼註解寫 exit(0) 實為 exit(1)（三處），對 watchdog 成敗語意有差。

**正面觀察（穩健勿動）**：imap_reader 威脅模型消化完整（不汙染全進程 timeout、watchdog 逃生、finally 直砍 socket、CQ-01 教訓已在 consult_query 呼叫端閉環）；smtp rate limiter rollback/密碼不進 log（逐路徑檢過）/成功 log 無病人 PII；deps 雙防線（fork-bomb 守門×2、pythonw 換手、SW_HIDE、裝完驗 import）；win32_safe fail-open 取捨有文件化；chrome_options 無 remote-debugging-port/無共用 user-data-dir；.gitignore 圍堵有效（git ls-files 確認無 settings/log）。

**已看過無明顯問題**：date_utils、ui_messages（drop-oldest 競態皆良性）、window_icon（HICON 有界洩漏不成長）、notifications.show_winotify_toast（契約正確）。

### 6E. 更新/儲存/併發核心（agent E 回報，2026-07-10）

> 14 檔全審＋交叉查證 paths/version/watchdog_core/.gitattributes/五入口呼叫端/manifest；4 項推理以 Python 實測。

#### IE-01 [P1] 下載失敗 backoff 把「失敗」變「靜默跳過」→ 第二輪檢查寫出**混版本**（違反「任一失敗整批不寫」不變量）
- `updater.py:362-367`（`_download_one` backoff 短路 `return None`＝「不需更新」語義）＋394。
- 觸發鏈：首輪檢查檔 X 下載失敗 → 整批正確放棄＋10 分鐘 backoff（OK）→ **同 process 10 分鐘內再跑 check_and_update**（main 啟動檢查 06:55 失敗 → 07:00 排程檢查）→ X 被 backoff 短路跳過、不進 pending_writes 也不進 errors → 其餘檔照寫新 commit → 重啟 → **磁碟 cmuh_common 混版本**。若新檔 import X 的新符號 → ImportError crash loop → watchdog 寫 suspend 旗標又把自癒推遲 1 小時。cmuh_common 五程式共用＝一支混版拖垮全部。
- 修法：backoff 短路改 raise（整批 fail-closed，backoff 期間整批不更新＝「寧可不更也不能壞」）；或回「skipped-but-stale」標記、寫入前有任何 stale-skip 即整批不寫。信心高（機制確定，觸發機率中、後果最重）。

#### IE-02 [P2] 五程式同時 check_and_update 無跨行程互斥：`.bak` 互踩、回滾還原到錯版本、可混 commit
- `updater.py:543-651`＋五呼叫端。main 只有行程內鎖；開機 watchdog 幾乎同時拉起多支程式全部寫同一批檔。壞情境：A/B 釘到不同 commit 交錯 replace＝混 commit；B 的 `copy2(v2→.bak)` 蓋掉 A 的 v1 備份 → A 回滾還原到 v2；rollback/成功清理都會刪掉**另一行程**依賴的 `.bak`。
- 修法：寫入階段包跨行程鎖（named mutex `Local\CMUH_Updater_Write`，single_instance 基建可重用；或 settings/ lockfile 比照 `_restart_history_lock`）；拿不到鎖本輪放棄。信心高（無鎖確定；機率中低、後果高）。

#### IE-03 [P2] `get_auto_update_suspend_until`：暫時性 OSError 會**刪掉有效 suspend 旗標**＋過期判定→刪檔 TOCTOU 誤刪新旗標
- `update_policy.py:47-57`。防毒/OneDrive 短暫鎖檔 → 讀失敗落到 `os.remove` → crash-loop 抑制旗標被刪 → updater 立刻恢復寫檔可能再拉肇事版本。另 A 讀到過期準備 remove 的間隙 watchdog 寫新旗標 → A 刪掉新旗標。
- 修法：OSError 分支直接 return 保守值（甚至視同 suspended）不刪檔；過期旗標可乾脆不刪（過期本不生效，下次 suspend 覆寫）。信心高。

#### IE-04 [P2] updater 寫入階段不重查 suspend 旗標：最長 5 分鐘下載窗內寫下的抑制無效
- `updater.py:438-446`（只開頭查一次）＋57（下載上限 300s）。crash loop 正在發生時 watchdog 寫旗標 → 本輪照樣把肇事新版寫進磁碟。修法：Phase 1 寫暫存前（~574）再查一次，有旗標整批放棄。信心高。

#### IE-05~11 [P3]（摘要）
- **IE-05** manifest 合法 JSON 但非 dict 時 AttributeError 逃出 try（五呼叫端有外層 except 兜住，僅訊息不友善）。修法：`_fetch_manifest` 尾端驗 isinstance dict。
- **IE-06** manifest 條目缺 `sha256` 時**完全不驗證即寫入**（updater.py:308-309 空 SHA 直接接受）——防禦縱深缺口。修法：空 SHA 視為該檔失敗（fail-closed）。
- **IE-07** settings_backup：restore 裸 copy2 非原子（併發讀到半檔會被 safe_load 判 corrupt 搬走）；create 中斷留殘缺快照被當最新。**現況 production 無呼叫端＝潛伏**，接線即升 P2。修法：restore 走 tmp+replace；create 先寫 .tmp-<ts> 目錄完成再 rename。
- **IE-08** 清理規則蓋不到三類殘留：src 子目錄 `.upd.tmp`（Phase1 中斷殘留）、settings `.corrupt-*`、sqlite `.corrupt-<ts>` 三件套 → 無人值守緩慢堆積。修法：tmp 掃描加 src 子目錄；`.corrupt-*` >30 天刪。
- **IE-09** single_instance 三邊界：CreateMutexW 失敗 fail-open（雙開放行——對打卡=重複打卡方向）、is_instance_running 瞬時建 mutex 誤判窗、RLock 抱著睡。修法：打卡類考慮 fail-closed；查詢改 OpenMutexW；重試移出鎖外。
- **IE-10** task_gate 舊式 `release(key)`（不帶 lease）會把別人接手的新 lease 一併釋放 → 打卡/會診 job 罕見並行。修法：呼叫端全面帶 lease；舊式用法記 deprecation。
- **IE-11** `load_doctors_settings`：合法 JSON 但形狀全錯時靜默用預設**覆寫原檔**且無備份（OneDrive 還原舊格式檔可踩）。修法：覆寫前 rename `.invalid-<ts>`（比照 sqlite_cache:105）。

**正面觀察（穩健勿動）**：atomic_io 全檔複核仍成立（三態契約在 config_io/app_settings 寫回路徑被正確遵守——逐路徑驗證）；updater 兩階段批次寫入＋路徑逃逸防護＋反序 rollback 正確；降版防護完整（`parse_version("")=(0,)` 意外但正確的 fail-closed，已實測）；commit-SHA 釘住策略（2026-06-05 修正是對的）；.gitattributes LF＋CRLF normalize 兩端閉環；sqlite WAL+timeout+執行期損壞隔離重建正解；bounded_executor 無洩漏路徑；restart_self spawn 確認存活才退；process_launch list 形式無 shell=True；consult_query mutex 擋退不寫 log 的跨模組協同。

**已看過無明顯問題**：memory_cache、cache_state、config_io（淺 merge 既定）、bounded_executor、process_launch、app_settings（除 IE-11）、cache_cleanup（除 IE-08；活動 log 排除邏輯實測驗證）。14 檔所有檔案 IO 均顯式 utf-8；本批無 subprocess 輸出解析＝無 cp950 風險點。

### 6G. 階段 4 修正批次規劃（給 Opus 4.8）

| 批次 | 項目 | 備註 |
|---|---|---|
| **基建批次1（更新安全鏈）** | IE-01(P1)、IE-02、IE-03、IE-04 | 三者互相放大，一起修；IE-01 先加「同 process 兩輪檢查+單檔暫時失敗」的回歸測試（紅→綠） |
| **基建批次2（通訊/例外）** | IF-01、IF-02、IF-03 | Tk handler 簽章（實測必炸）、credentials corrupt-rename、阻塞通知廢掉 RAM 保險絲 |
| 基建批次3（防禦縱深） | IE-06、IE-11、IF-06(is_internal)、IE-08 | fail-closed 補洞＋清理缺口 |
| 基建批次4（P3 擇修） | IE-05/07/09/10、IF-04/05/07/08/09/10/11/12 | 執行者裁量；IF-12/UC-09 註解漂移順手修 |

## 7. 階段 5 findings — 主程式其餘＋入口/守護

> 狀態：**✅ 審查完成（2026-07-10，G/H 均回報）**——1×P1＋4×P2＋P3 若干，批次見 §7C。
> G＝main.py 未審其餘；H＝scheduler.py/watchdog_runner.py 全檔＋六個 .pyw 入口＋入口防護一致性。

### 7G. main.py 其餘（agent G 回報，2026-07-10）

#### MG-01 [P2] `check_appointment_count` 把「還在被 worker 續寫的活 dict」塞給 UI（漏 deepcopy）
- `main.py:6825`（對照 6679 有 deepcopy 的正確寫法）。訊息送出後 worker 緊接 6827-6844 原地 `_merge_dayoff_overrides` 同一 dict；UI 緒把活物件存進 `all_doctors_data`，`_update_grid_data`（12391/12434）主緒無鎖迭代；`_get_all_doctors_data_snapshot` 持鎖 deepcopy 但 worker 原地合併不持鎖＝鎖形同虛設。偶發 dict-changed-size → 月曆重繪中途炸（UI 半殘）、或畫出合併到一半的休診狀態。休診解析耗時與 UI 輪詢窗重疊機率真實。
- 修法：6825 改 `data=deepcopy(appointments_by_date)`（與 6679 對齊）。信心高。

#### MG-02 [P2] 自動更新「需重啟」→ 2 秒後無條件重啟，不看熱鍵自動化是否進行中
- `main.py:12850-12853`→`_restart_app`(7768)；排程時點 07:00/**13:00/18:00 都在門診時段**。重啟是 spawn+sys.exit，daemon 熱鍵緒在**任意指令中間**被砍（不同於 F12 只在 check_stop 安全點）：`_send_chars_to_window` 打到一半→醫令欄殘碼「510」沒 Enter；F9/F10 同意書半開。設定頁未存編輯也蒸發。
- 修法：need_restart 分支檢查 `_subsystem_running`/距 last_action_time <N 秒，忙碌每 5-10s 重查、閒置才重啟；提示改「將於熱鍵操作結束後重啟」。信心高、機率中低。
- **附帶**：G 確認 main 呼叫端（固定 3 時點+啟動+手動、單飛）**不會放大** IE-01 backoff 混版本。

#### MG-03~05 [P3]
- **MG-03** `save_all_settings`：Treeview 回讀讓純數字醫師代號變 int 寫入 doctors.json（消費端有 str() 自癒）；且 `existing_doctor['notifications']` 缺鍵 KeyError 中止 → r_doctor/threshold 已寫、doctors 沒寫＝**半套儲存且無提示**（8626-8631）。修法：`str(doc_no)`＋`.get('notifications', False)`。
- **MG-04** `_kill_orphan_chromedriver` 名不符實：只殺 `ppid==本行程`，前次崩潰的真孤兒（父已死）永遠清不到 → 崩潰一次殘留 ~150MB headless Chrome（7728-7766）。修法：啟動掃「name=chromedriver 且 ppid 不存在」或按 user-data-dir 標記清。
- **MG-05** F8 全域無守門直打焦點＋`quick_text_f8` 明文存檔（使用者需求刻意，列備查）：建議設定頁註明勿存正式密碼。
- 小雜項（備查）：`_ConnPoolFullAggregator` 首筆 summary 失真；熔斷 log「重啟才會重試」訊息過時（已有 30 分自動重置）；http 位址帶 verify=True no-op；promo image 關閉競態；`_schedule_save_cache` 關閉時最後一筆 debounce 快取丟失（可接受）。

**正面觀察（勿動）**：關閉/重啟序列順序講究；Tk 跨緒紀律抽查全過（背景進 UI 全走 after/ui_queue）；懶載分頁自癒；排程迴圈難被單一例外殺死；F4/F5 共用 M2 已加固路徑無 UD-06 型殘留。
**已看過無明顯問題**：檔首常數/快取/thread-local sessions/circuit breaker/IPv4 patch、東/惠和/惠盛 fetch 三兄弟、`check_appointment_count` 其餘、值班並行查詢、email 雙軌、設定分頁群 UI、`process_ui_queue`、啟動段。

### 7H. 入口/守護（agent H 回報，2026-07-10）

#### EH-01 [P1] 六個 .pyw 啟動器對「模組 import 階段例外」零兜底 → pythonw 靜默死亡、零 log
- 六個 .pyw 皆裸 `runpy.run_path`；excepthook 都裝在目標模組 main() 內，但 src/*.py **模組層** import 先跑（scheduler.py:52-59 roster import 在 setup_logging 之前；main.py 頂部 100+ 行）。半套更新（**與 IE-01 混版本互相放大**）/斷電中斷更新造成的 ImportError/SyntaxError 直接穿出 → 雙擊「什麼都沒發生」、任何 log 無痕跡。autoclock/consult 有 watchdog 接手（緩解）；**主程式/排班/座標偵測完全不可診斷**。
- 修法：六 launcher 統一 `try: runpy.run_path(...) except Exception:` → 寫 `startup_crash.log`（時間戳+traceback）＋MessageBoxW；**不可攔 SystemExit**（deps 取消的 sys.exit 需照常穿出）。信心高。

#### EH-02 [P2] 守護程式「自動以 admin 啟動」文檔與碼漂移：非 admin 雙擊時外層 watchdog 降級＋可能反覆彈 UAC
- `watchdog_runner.py:14` docstring 稱自動提權，實際 runner 與 .pyw 都無 is_admin/run_as_admin。非 admin daemon：對 admin 行程 kill 必 AccessDenied（救不了半死行程）；以非 admin 起 consult → consult 自己彈 UAC，無人在場反覆彈窗、會診空窗。schtasks 路徑（/RL HIGHEST）不受影響。
- 修法：runner 啟動檢 is_admin，非 admin 至少 WARNING（或比照 consult 自動提權）；修 docstring。

#### EH-03 [P2] 排班 `ScheduleApp` 建構失敗只進 log、無可見錯誤（main.py 同情境已修過、scheduler 未跟進）
- `scheduler.py:291` 未包 try；對照 main.py:14159-14177（try+MessageBox+exit(1)）。使用者雙擊 → 視窗閃沒、無提示。修法：比照 main 包 try。

#### EH-04~10 [P3]
- **EH-04** coord_detector 無單例（唯一缺 mutex 的入口；SP-08 已列，此處依「一致性準則」再確認）。
- **EH-05** watchdog.log 多行程 RotatingFileHandler 輪替競態（daemon+--once 並存時偶發掉 log/超 1MB）。修法：--once 改獨立檔名。
- **EH-06** 四支入口 deps 檢查在單例之前 → 首裝/換 Python 雙開會併發 pip（打卡程式 launcher 先取 mutex＝正確範本）。
- **EH-07** 排班自動更新重啟無預告（main 有 2 秒通知、scheduler 直接 SystemExit）→ 編輯中 modal 內容遺失。修法：至少 toast 預告或 idle 偵測。
- **EH-08** scheduler `_run_app` 先 release mutex 再 flush（最長 30s）→ 新舊實例併發碰 roster git repo（git index.lock 會擋、不毀資料，偶發「同步異常」訊息；註解已載明取捨）。可選：更新重啟路徑先 flush 再 restart。
- **EH-09** watchdog_runner `_setup_logging` 在 try 之外：log handler 建構失敗＝外層守護靜默失效（schtasks 每 2 分重複同結局）。修法：包 try、失敗退化 null logging 仍執行 tick。
- **EH-10** schtasks 情境 psutil 缺失會每 2 分彈一次 DependencyInstaller 視窗（`except Exception` 攔不住 SystemExit）；安裝器以絕對路徑釘死 pythonw → Python 升級後四個 ONLOGON＋watchdog task 全滅（文件註明「升級後重跑安裝器」或改 shim）。

**正面觀察（勿動）**：schtasks IgnoreNew+5min 上限正確；watchdog_core 檔案鎖三方互斥已妥善；single_instance 冪等+重試；consult mutex-before-logging 與打卡 launcher 級 mutex 各得其所；**排班對 IE-01 天然免疫**（每行程只跑一輪、`_sha_mismatch_until` 行程內記憶體）；main.py 入口區是五支中最完整的範本；autoclock main() 守門齊全。

### 7C. 階段 5 修正批次規劃（給 Opus 4.8）

| 批次 | 項目 | 備註 |
|---|---|---|
| **入口批次1（必修）** | EH-01(P1)、EH-03、MG-01、MG-02 | launcher 兜底（與 IE-01 修正同批更佳）；deepcopy 一行修；更新重啟守門 |
| 入口批次2（值得修） | EH-02、MG-03、EH-09、EH-05 | 守護降級警告、半套儲存、守護自身兜底 |
| 入口批次3（選配） | MG-04/05、EH-04/06/07/08/10、G 小雜項 | 執行者裁量 |

---

## 8. 修正執行全局順序（給 Opus 4.8）＋完工檢查清單

> **審查全部完成（2026-07-10）**。以下為跨階段合併後的建議開工順序（風險×觸發頻率）；
> 各批次細節見 §3E / §4C / §5C / §6G / §7C。**2026-07-10 起 codex review 一律用 GPT-5.6 Sol**
>（`python .claude/codex.py review --uncommitted`，launcher 已固定 model）。

| 順位 | 批次 | 內容 | 理由 |
|---|---|---|---|
| 1 | UVB批次1（§3E） | UC-01/04/03 | **醫療急件**：實測重現的錯劑量寫回（850→700、突破上限、遞減反轉），門診天天可踩 |
| 2 | UVB批次2（§3E） | UC-02/05/06 | 假 first-time 繞過全部間隔防線 |
| 3 | UVB批次3（§3E） | UD-01~04 | F12 取消尊重四縫隙（小改動） |
| 4 | 基建批次1（§6G）＋EH-01 | IE-01~04＋launcher 兜底 | 更新安全鏈三連環互相放大；EH-01 與 IE-01 同因果鏈（混版本→靜默死亡），同批修 |
| 5 | 診間批次1（§4C） | FC-01/02、CL-01 | 顯示誤導（已抽驗）＋fail-open 破口 |
| 6 | 入口批次1 其餘（§7C） | EH-03、MG-01、MG-02 | 排班可見錯誤、deepcopy 一行、更新重啟守門 |
| 7 | 基建批次2（§6G） | IF-01/02/03 | Tk handler 潛伏彈、credentials 全滅、RAM 保險絲 |
| 8 | UVB批次4→5、診間批次2→3、基建批次3→4、入口批次2→3、小程式批次1（§5C） | 其餘 P2→P3 | 依序；P3 執行者裁量 |

- [x] 5 階段審查全部完成、findings 彙整入本文件（2026-07-10；約 8×P1＋22×P2＋40+×P3）
- [~] 每階段的 P1/P2 findings 由 Opus 4.8 依 §0 守則＋上表順序修正並上線（進度見 §8A）
- [x] 每個 fix 有回歸測試；pytest 全綠；**codex(GPT-5.6 Sol) review 過**；push_helper 發佈（已上線批次皆遵循）
- [ ] UVB/打卡等醫療相關修正，提醒使用者實機抽驗（UVB批次4 已上線，待實機抽驗）

---

## 8A. 執行進度（2026-07-12，Opus 4.8 續作；每批 codex GPT-5.6-sol 外審 + 全套 pytest + push_helper）

> 外審一律走本 repo 的 `tools/codex_review.sh`（gpt-5.6-sol，read-only；審歷史 commit 範圍用 `CODEX_REVIEW_TIP`）。
> **codex MCP 已於 2026-07-10 移除，勿用**（§8 上方所寫 `.claude/codex.py` 為另一台機器的啟動器；本機用 wrapper）。

**✅ 已上線**：
- **v2026.07.12.1** 外審補漏：近 3-4 天未外審 commit 的 codex 深審 → U1/U2 劑量 regex（遞減夾中性字 `decrease dose by N`、點分日期 `until 2026.9.1` 破 MAX）、U4/U5 純 excimer 寫身份/確認後寫回前 F12 閘門。
- **v2026.07.12.2**：U3（=UD-01b 卡號寫回前 F12 閘門）、F1（同意書 popup 只認本 HIS 行程新開、排除 stale）、F3（#32770 取不到 PID → fail-closed）。
- **v2026.07.12.3** 診間批次2/3：CL-02（中位數裁剪）/CL-03（時段半開區間）/CL-04（止掛不誤報快滿）/CL-06（全空回預設＋除死碼）/CL-07（混型不拋）/FC-03（reg64 讀端 list() 快照）/FC-05（浮窗 destroy 取消 ensure_shown after＋exists 走 self.destroy）。CL-05 僅加釘位測試。
- **v2026.07.12.4** UVB批次4：UC-11（原劑量>本行 MAX → 交醫師確認，主行＋同日附加行皆把關）/UD-05（療程欄寫入前正向把關，比照身份欄）/UC-09（docstring 補 15-21×0.5 桶）。
- **v2026.07.12.6** 基建批次3/4：IE-05（manifest 非 dict 攔截）/IE-06（缺 sha256 fail-closed）/IE-08（清理擴及 .upd.tmp+.corrupt-*）/IE-11（doctors.json 退回預設前備份 .invalid，含 PID 防撞名）/IF-05（imap 死碼）/IF-06（is_internal 用 hostname 擋 userinfo 欺騙）/IF-07（只有全 5xx 才不重試，4xx greylisting 仍重試）/IF-08（無 TLS 明文帳密守門，loopback 用 ipaddress）/IF-12（註解 exit 碼）。
- **v2026.07.12.7** 入口批次2/3：EH-02（守護非 admin 警告+修 docstring）/EH-05（--once 獨立 log）/EH-09（_setup_logging 建構失敗退化續跑）/MG-03（醫師代號 str()+notifications .get 兜底）/MG-04（也收父行程已死的真孤兒 chromedriver）。
- **v2026.07.12.8** 小程式批次1+其餘：SP-01（scaling target_size 防呆）/SP-05（coord hook 跨緒安全回呼）/SP-06（取色失敗不更新 _last_pos）/SP-07（登入 alert 回實際文字）/SP-03（自動重啟 idle 門檻 3s→30s）。
- **v2026.07.12.9** UVB批次5 安全子集：UD-08（read-back 空值保守中止）/UD-12（_send_chars 逐字 IsWindow 把關）/UD-13（UVB 警告框納 awaiting scope）。
- **v2026.07.12.12 UC-07 完成**（乾淨 session 依下方規格重做，2026-07-12）：Step C 跨日期 capped（劑量==MAX）續行 >7 天不再靜默 bump；`_detect_uncertain_triplets` 逐 triplet 守衛（緊鄰該 triplet 前方劑量 vs 適用 MAX＝該行 MAX、行內無 MAX 退驅動行 MAX——後者是 Step C 判 capped 的依據，不退則同款段仍經 uncertain-Yes 漏回寫）同步排除、留原樣交醫師。8 新測紅→綠（含 codex 點名的同行混合劑量 primary 500<800+capped 800、行內無 MAX 兩案＋7/8 天邊界＋3 行為保留釘位）＋全套 1438 綠＋codex deep 一輪 APPROVE。**待實機抽驗**：混合日期 UVB 病歷，舊 capped 段應原樣保留、不跳 Yes/No。
- **v2026.07.13.1 UC-08 完成**：(A) `format_uvb_line` 日期改用 parse 時 `date_span` 精確替換且最先換（dose/count 替換會位移；同值日期兩次時 str.find 換錯欄位）；(B) Step C＋uncertain 雙端守衛：triplet 中段夾另一裸日期＝配對歸屬不明 → 不 bump/不納 uncertain（Step A 殘留的重複日期不再把驅動行自己的 count 湊成幽靈 triplet 再 +1；`new_count` 與寫回文字不再不一致）。殘留重複日期原樣保留。7 新測紅→綠＋全套 1462 綠＋codex deep 一輪 APPROVE。**至此 §8A 可執行緩修全數完結**；僅剩 UD-06（需實機 class 名單）、各批次較大取捨項、BY-DESIGN。
- **v2026.07.12.13 UVB批次5 其餘 P3 完成（UD-07/09/10/11/14）**：UD-07 CONFIRM 改迴圈、每次只帶已確認那種 skip flag（既 stale 又超限不再漏另一種確認；同種重複＝保守中止防無限跳窗）；UD-09 `codes_already_placed` 穿 core、F1 六種失敗文案附「51019+療程1 已下」提醒；UD-10 uncertain 套用失敗跳警告點名額外行未更新；UD-11 W7 rec 記 `extra_lines`、(B) 改回原值提醒一併還原；UD-14 F1 route 例外 fallback normal 後 core 二次分流=純 excimer 跳人工核對警告——codex 第一輪抓到 TOO_CLOSE 回裸 False 丟失分流結論（CONFIRMED P2）→ 新增 falsy `_F23_PURE_EXCIMER_ABORTED` 哨符（F2/F3 零改動 fail-closed）、兩種結局都警告。12 新測＋全套 1455 綠＋codex deep 兩輪 APPROVE。**至此 UVB 批次全數完結**；剩餘緩修僅 UC-08（獨立 session）、UD-06（需實機 class 名單）、各批次較大取捨項、BY-DESIGN。

**✅ 5 個主批次全部上線（v.3~v.9,含診間/UVB批次4/基建3-4/入口2-3/小程式）。剩餘為刻意緩修（勿盲改）**：
- ~~**UC-07**~~ ✅ **已完成（v2026.07.12.12，見上）**。原記錄留供追溯：（dose parser Step C 跨日期續行不 decay）：**2026-07-12 已重現**。重現輸入(today=2026/7/12)：
  `"UVB 800 mj/cm2 (10) on (2026/7/8) fixed at 800\nUVB 800 mj/cm2 (5) on (2026/6/28) fixed at 800"`
  → 第二行(6/28,14 天前、dose==MAX)被 bump 成今天仍寫 800,獨立算應 ×0.75→600。
  **嘗試修正後 codex 連抓兩層更深問題(故 2026-07-12 還原、不 ship 半套劑量修)**:①只擋 Step C 即時
  bump 不夠 —— 被跳過的段會被 `_detect_uncertain_triplets` 撿進 `uncertain_other_triplets`,醫師按
  Yes 經 `apply_uncertain_updates` 仍 kept-dose bump(不重算劑量)寫回未衰退值;②在 uncertain 端補
  守衛時,**不可 parse 整行**(同一行 primary 500<800 + 續行 800 capped 時會誤判)—— 須**逐 triplet**
  評估「緊鄰該 triplet 前方的劑量」對其 MAX(比照 Step C 既有的 `continuation_m`/`guard_dose_m`)。
  **完整修正規格**:Step C 與 `_detect_uncertain_triplets` 兩處都用「該 triplet 的 capped(dose>=MAX)
  且跨日期 >7 天」判定 → 一律不 bump/不納入 uncertain,留原樣交醫師。回歸測試需含**同一行混合劑量**
  (primary 非 capped + 續行 capped)案例。
- **UC-08**（format_uvb_line src.find 換錯 + Step B count+2）：**2026-07-12 已重現**。輸入：
  `"UVB 500 mj/cm2 (10) 2026/7/8 done, next on (2026/7/8) add 30, MAX: 800"` → count 10→**12**(+2)、
  行內兩處日期都被改。屬 **P2 資料一致性(count/日期),非劑量錯**;修 `format_uvb_line`(所有寫回
  共用、blast radius 最大)+ Step B round-trip,建議獨立 focused session 做。
- **UD-06**：focus lenient 分支收緊需先蒐集實機 focus class 名單（§3D 修法自承；貿改會讓代碼輸入整批停擺）。
- ~~**UVB批次5 其餘 P3**~~ ✅ **已完成（v2026.07.12.13，見上）**：UD-07、UD-09、UD-10、UD-11、UD-14 全數上線。
- **各批次內較大/取捨項**：IE-07（settings_backup 原子化，無 prod 呼叫端潛伏）、IE-09（single_instance fail-open→closed 只對打卡通道、建議僅加警告）、IF-04（imap sequence→UID 改寫較大）、IF-09/IF-10/IF-11（deps fallback/logging stderr-None/execv 邊角）、EH-06（單例先於 deps，涉 4 支入口重排）、EH-10（schtasks psutil 缺失彈窗，需 mode-aware+.ps1 shim）、SP-02（表消失 canary，需 JS+消費端）、SP-03(b)（重啟計數持久化）、SP-04（scaling dict 整包替換，現 disabled）、FC-04（浮窗 dirty-check，誤判會顯示過期燈號）、FC-06（浮窗手動高度+自癒，涉 settings schema）。
- **BY-DESIGN（不改）**：UC-10（民國 triplet 為安全方向）、UC-12（Step D 丟 excimer too_close）、UD-15（純 excimer 無 read-back）、MG-05（F8 明文,刻意）、EH-08（scheduler release-before-flush 取捨已載明）、IE-10（呼叫端全帶 lease、已 DRIFTED）。

> 每條緩修在 §3~§7 各節都有 4 個平行驗證 agent（2026-07-12）對現況 HEAD 的精確定位＋最小修法＋測試approach＋風險；接手時先重讀該段確認現況成立。醫療劑量/寫回相關（UVB/打卡）修正**務必實機抽驗**。
