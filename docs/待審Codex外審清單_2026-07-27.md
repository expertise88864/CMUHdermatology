# 待補 Codex 外審清單(2026-07-27 起)

> **狀態:未審上線。** 2026-07-27 Codex(GPT-5.6-sol)用量額度用盡
> (`You've hit your usage limit ... try again at Aug 2nd, 2026 3:17 AM`),
> 使用者定案:**先 commit + push,標記未審,等額度回復後一次補審。**
> 這覆寫了 `~/.claude/CLAUDE.md` 平時的「必須 APPROVE 才 push」實務閘門。

## 最後一個通過外審的 commit(補審的 base-ref)

```
f3561d603602d7512c49e1dedee659eb7c80db49
fix(clinic): 完成人數實測值不再被時長樣本閘門丟掉;SQLite 暫時鎖競爭不再隔離歷史快取
v2026.07.27.3 — Codex targeted 第 2 輪 APPROVE
```

**補審時的指令(額度回復後執行):**

```bash
bash tools/codex_review.sh targeted f3561d603602d7512c49e1dedee659eb7c80db49 <ctx-file>
```

`f3561d6` 之後的所有 commit 都要一次審完;審到 `APPROVE` 為止,
中途 `REQUEST_CHANGES` 一律先驗證 finding(CONFIRMED / REJECTED / UNCERTAIN)
再修 CONFIRMED 的,然後 `tools/codex_review.sh resume <ctx>` 續審。
**審完並 APPROVE 之後,把本檔刪除或改寫成「已補審」。**

> ⚠ **這批 diff 很大(排班引擎 + 門診統計兩個互不相干的領域)。**
> 補審時建議**分兩個 ctx 檔、跑兩輪 targeted**,不要塞成一個 —— 混在一起
> 容易讓外審抓不到重點。排班那批動到求解引擎與套用防線,值得用 `deep`。

---

## 待審 commit 一覽(`f3561d6..`)

| commit | 領域 | 一句話 |
|---|---|---|
| `f9daca6` | 排班 | 切片室改「排好排滿·同梯次數一致」 |
| `c467f18` | 排班 + 工具 | 修 OneDrive 吃掉的 solve_day.py;push_helper 加 SHA256 還原偵測 |
| `b25b602` | 排班 | 反「整天放假」:每人每天盡量至少半天有事 |
| `c378538` | 排班 | 手動指定連休段拆段求解 + 週六切片右鍵指定 |
| `915f39e` | 排班 | 套用路徑也要認拆段(accept_solution / quick_validate) |
| `36ef556` | 門診統計 | 同輪多位病人完成的間隔整批平分(詳見下節) |
| `36ef556`+ | 排班 | 週五值班↔週六切片連動 + 班數平衡權重提升(詳見下節) |
| `5d75267` | 設定 | 設定頁「還原預設設定」+ 預設值單一事實來源(詳見下節) |
| `f0b1…` | 稽核/隱私 | 回讀不符告警加上病人定位資訊(詳見下節) |
| `76f…` | 金絲雀 | 校正版本 1150720 → 1150722(使用者實測)+ 告警補「預期寫入」 |
| 本次 | 擴充性 | HIS 契約單一宣告處 + 告警去重原語 + 擴充規約文件(詳見下節) |

排班那 5 個 commit 由另一個 session 完成,commit message 內已有各自的實測結果與
測試清單,補審時直接讀 `git log` 即可;本檔只詳述門診統計這批。

---

## 待審變更

### 門診完成間隔分攤 + 提權參數一致性

| 檔案 | 變更 |
|---|---|
| `src/cmuh_common/clinic_state.py` | 新增純函式 `apply_newly_completed()` |
| `src/main.py` | 該處 34 行內聯邏輯改為 4 行呼叫;import 新函式 |
| `src/cmuh_common/platform_win.py` | `_admin_relaunch_params` 加 frozen 模式分支 |
| `tests/test_completion_batch_pace_2026_07_27.py` | 新增 9 項 |

**1. 同一輪多位病人完成時,間隔只有第一位拿到(高估平均看診時間)**

`newly_completed = current_completed_set - tracker['last_completed_set']` 是**集合**,
門診動態輪詢間隔 `CLINIC_LIGHT_REFRESH_SECONDS = 60` 秒,同一輪出現 2 位以上完成是常態。
原本的迴圈逐筆算 `doctor_pace = current_timestamp - tracker['last_valid_completion_time']`,
而且**在迴圈內**就把 `last_valid_completion_time` 更新成 `current_timestamp` →
集合迭代到的第一位拿到完整 300 秒,同批其餘拿到 0。

那些 0 會被 `duration_stats` 的中位數帶裁掉(所以平均不會直接歸零),但結果是
「兩人共用 300 秒」被記成**一筆 300** 而不是**兩筆 150** → 平均看診時間、
以及用它乘上候診人數的「預估剩餘」被系統性高估。

同一段還有第二個問題:`first_valid_skipped` 只跳過集合迭代到的第一位 ——
同批其餘拿著**同一個無效基準**算出的 0 照樣進了樣本。基準無效就該整批跳過。

修法:抽成 `clinic_state.apply_newly_completed(tracker, newly_completed, ts)`
純函式(與該檔既有的 `new_clinic_tracker` / `restore_tracker_from_state` 同層級),
先分類照光/有效,再把同一段間隔**平分**給整批有效完成;`first_valid_skipped`
未設時整批不取樣;`< 3600` 的休息/斷線門檻語意維持不變(對整段原始間隔判定)。

**2. `platform_win._admin_relaunch_params` 在 .exe 模式會把自己的路徑當引數傳給自己**

`run_as_admin` 用 `ShellExecuteW(None, "runas", sys.executable, params, ...)`,
params 原本是 `list2cmdline(sys.argv)` —— frozen(PyInstaller)模式下 `sys.executable`
就是程式本身、`argv[0]` 也是同一個 exe,於是變成
`app.exe "C:\...\app.exe" --background`。
同套件的 `paths.restart_self` 早就分兩種模式處理(frozen 時 `[sys.executable] + args`),
這裡沿用同一條規則。

⚠ **補審時請特別看這一項**:目前 repo 沒有 `.spec` 也沒有打包腳本,實務上跑 `.pyw`,
所以這是「與既有模式對齊的一致性修正」而非實機故障。若外審認為不該動提權路徑,可以撤回。

**本機驗證(已完成)**

- `ruff check src tests` → All checks passed
- `pyright`(clinic_state.py / platform_win.py / main.py)→ 0 errors, 0 warnings
- `python -m pytest -q` → **2117 passed, 6 skipped**(已與排班那 5 個 commit 合併後重跑)

**已寫好的 ctx 檔(補審時可直接沿用/合併)**

`scratchpad/ctx_p4b.txt`(session dea4e6b2 的 scratchpad;若已清掉,依本檔重寫即可)

---

### 週五值班↔週六切片連動 + 班數平衡權重提升

| 檔案 | 變更 |
|---|---|
| `src/cmuh_common/roster/rules.py` | 新增 `FridayBiopsyLinkRule`;`DutyCountBalanceRule` 權重 1 → 1500 |
| `src/cmuh_common/roster/saturday_biopsy.py` | 次數平手時優先挑已值週五者 |
| `tests/test_roster_friday_biopsy_link_2026_07_27.py` | 新增 16 項 |

**使用者需求(2026-07-27 原話)**

1. 「若R排班 盡量禮拜六早上切片的人 也同樣安排禮拜五值班,但是要在排班允許的
   情況下(最後條件,可以選擇的話盡量禮拜六早上切片的人選也值班禮拜五)」
2. 「原本設定就是盡量值班那周六日的人要切片(R2/R3) 若是R1值班則是安排R2/R3值班,
   但是R2/R3週六切片次數要平均分配 盡量讓R2/R3一整年下來週六切片次數一樣」
3. 「在R/VS值班, 若點數允許的情況下, 也盡量要安排班數接近或一致」

第 2 點查證後**既有實作已完全滿足**(值班連動 → 否則次數平衡,`counts` 永久累計
於 `biopsy.json`,`rollback_biopsy`/`settle_biopsy` 同月重排不重複計)。未改動,
只補了一支 12 個月的迴歸測試釘住「全年全距 ≤1」。

**做了什麼**

切片人選有兩種來路,所以連動要兩側各做一半:
- **值班側** `FridayBiopsyLinkRule`(新):週六排到 R2/R3 時(→ 值班連動使他成為
  切片者),獎勵同一人也值週五。
- **切片側** `assign_saturday_biopsy`:週六值班是 R1 → 走次數平衡,此時
  **次數平手**才優先挑已值週五者(取代原本的「與上次不同者優先」輪替;
  輪替本來就只是次數平衡的弱化版,全距仍由 `run` 保證 ≤1)。

**權重帶(這是最需要外審看的部分)**

```
點數公平            10,000 / 0.01 點        ← 絕對優先,以下全部加起來都不該超過
5 連值           10,000,000
4 連值            3,000,000
週末上限 >2       ~4-10 點級(動態)
─────────────────────────────────────
班數全距 1 單位        1,500   ← 由 1 提升(原本連 3 連值罰則都輸,幾乎不生效)
週五連動 1 個            600
3 連值 1 扇窗            500
```

- 用**獎勵連上**而非**懲罰沒連上**:後者會讓求解器改把 R1 排進週六來躲罰則
  (成本 0),反而破壞使用者更想要的「週末由 R2/R3 值」。
- ⚠ **誠實揭露的取捨**:每月最多 5 個週六 → 連動總獎勵上限 3,000 > 班數全距
  1 單位 1,500,亦即「3 個以上的週五連動」可以換掉 1 天的班數差距。
  要完全支配需 `RANGE_WEIGHT > 3,000`,但那會讓班數項在全距較大時突破點數
  步進,反而危及最高優先的點數公平。取捨後選擇「單一連動絕不換班數」。
  **若外審或使用者認為這個取捨不對,改 `RANGE_WEIGHT` / `LINK_WEIGHT` 即可,
  兩個常數都有測試釘住相對大小。**
- ⚠ **班數平衡權重 1 → 1500 是行為變更**:它現在也壓過 3 連值罰則(500)。
  依據是使用者第 3 點把班數一致列為明確要求,而 2026-07-13 已定案
  「3 天勉強可接受」。實測 2026/8 基準情境下解不變(班數 10/10/11、
  點數 13/14/14、週末 2/4/4 全部相同)。

**本機驗證**

- `ruff check src tests` → All checks passed
- `pyright`(rules.py / saturday_biopsy.py)→ 0 errors, 0 warnings
- `python -m pytest -q` → **2133 passed, 6 skipped**(6 個是 tk 環境 skip)
- 排班相關全跑:367 passed
- 端到端 A/B:關閉本規則 0 個週六連上、啟用後 ≥2 個,且點數/班數/週末分布
  **完全相同** —— 這正是使用者說的「排班允許的情況下」

---

### 設定頁「還原預設設定」+ 預設值單一事實來源

| 檔案 | 變更 |
|---|---|
| `src/cmuh_common/settings_defaults.py` | **新檔**:原廠預設宣告 + 還原邏輯 |
| `src/cmuh_common/app_settings.py` | 門檻載入改用統一預設;新增 `clear_load_failed` |
| `src/main.py` | 設定頁新增「還原預設設定…」按鈕與對話框;`_reload_settings_into_ui` |
| `tests/test_settings_defaults_2026_07_27.py` | 新增 18 項 |

**使用者需求**:「設定新增一個返回預設設定功能(可以直接載入設定頁面預設的設定,
如原本預設醫師代號/原本預設止掛提醒email等等) 針對程式後續擴充性進行完整深度優化處理」、
「止掛提醒email 預設新增 lai.i.chang.58@gmail.com」。

**擴充性(這是需求的重點,請外審評估規約是否站得住)**

原本「預設值」散在四處:`threshold_policy.DEFAULT_THRESHOLDS`、`app_settings` 的
數個 `DEFAULT_*`、`main.py` 中段的 `F8_QUICK_TEXT_DEFAULT`、以及設定頁 UI 裡零星的
`.get(key, False)`。新增一個設定要同時改預設常數、載入器、`save_all_settings`、
UI 建構四處,沒有任何機制保證一致。新規約:

- 新增**設定鍵** → 只加進對應的 `DEFAULT_*` dict。載入(merge)、還原預設、摘要自動涵蓋。
- 新增**設定檔** → 在 `SETTINGS_GROUPS` 加一個 `SettingsGroup`。設定頁對話框自動長出。
- 測試 `test_group_registry_is_the_single_source_for_ui` 釘住 UI 必須用迴圈跑群組,不可寫死。

**⚠ 請外審特別看的兩點**

1. **`load_threshold_settings` 的順序**:把完整預設合進來之後,原本的舊格式推導
   (`if "notify_dnd_end_time" not in data`)會全部失效 —— 舊機器的檔案往往只有
   `notify_dnd_*_hour`,勿擾時段會被悄悄換成預設。已改成「先對原始檔案內容推導、
   最後才補預設」,並加測試 `test_legacy_dnd_hours_still_derive_times`。
   **這個回歸是既有測試抓到的,不是我先想到的 —— 請確認沒有同類漏網。**
2. **預設收件人不可讓「刻意清空」復活**:merge 語意是 `base.update(file)`,檔案裡
   `[]` 是有鍵 → 不會被預設蓋。與 smtp_mail 帳密快取同一個坑,已加測試釘住。

**破壞性動作的安全設計**

- 還原前一律把原檔另存 `.before-reset-<ts>-<pid>`;**備份失敗就不覆蓋**(沒有退路的
  破壞性寫入不可接受)。
- 刻意**不套用**`save_all_settings` 的「本次讀不到就拒絕存檔」守衛:那道守衛防的是
  【無意間】覆蓋,而這裡是使用者明確要求覆蓋且已備份。硬套會讓「檔案壞掉想重置」
  永遠救不回來。
- 只對**成功寫入**的群組呼叫 `clear_load_failed`(跑 `report.restored` 而非使用者
  勾選的清單),有測試釘住。

**本機驗證**:ruff 全清、pyright 0 errors、`pytest -q` → **2156 passed**。

---

### 回讀不符告警加上病人定位資訊(★請外審重點看隱私邊界★)

| 檔案 | 變更 |
|---|---|
| `src/cmuh_common/patient_locator.py` | **新檔**:橫幅白名單解析 + 獨立定位索引檔 |
| `src/main.py` | `_sample_patient_locator`;定位資訊經佇列獨立欄位送到告警 |
| `tests/test_patient_locator_2026_07_28.py` | 新增 17 項 |
| `tests/test_action_ledger_2026_07_17.py`、`test_audit_health_2026_07_19.py` | 佇列契約由 4 → 5 欄,更新既有測試並補 2 項 |

**使用者需求**:收到「回讀不符:F1 UVB 劑量 / 回讀 dose=800 count=21」告警,
「但是沒有紀錄該病人診間/診號/或是病歷號 這樣我沒辦法查詢是哪個病人有錯誤」。

**資料來源(使用者提供實機截圖)**:HIS 主視窗**標題列只有版本號**
(`…作業(海青灣)-- V.1150722.01`),病人資訊在上方一條獨立橫幅:

```
1150728 早上 103診 113號 -呂冠愷(24994923)女 42歲1月 (0730623) #C0024322
```

**★隱私邊界(這是本批最需要外審的部分)★**

- 只擷取**白名單**:民國日期、時段、診間、診號、病歷號、就診序號。
  **姓名、生日、性別、年齡一律不取、不回傳、不落檔。**
- `parse_banner` 只回白名單 dict,**呼叫端拿不到橫幅原文** —— 原文只存在區域變數。
- `ALLOWED_FIELDS` 是機械性防線(新增欄位必須先進清單),有測試釘住 `name`/`birthday` 不在其中。
- 橫幅裡有兩組括號數字:`(24994923)`=病歷號、`(0730623)`=生日。正則刻意取「診號之後
  緊接的第一組」,取錯就等於把生日當病歷號寄出去 —— 有專門測試。
- **hash-chain 稽核帳本維持「不存病人明文識別」(2026-07-17 定案)不動**:定位資訊
  走佇列的**獨立第 5 欄**,不進 `fields`,有測試釘住 `dict(fields), ts, locator)`。

**採樣時機與識別方式**

- 只在 `outcome == mismatch`(罕見、流程已中止)時採樣 —— 列舉子視窗有成本,不放進正常路徑。
- 在**入列當下**(熱鍵緒)採,不在背景緒 —— 背景緒稍後才採可能已經換病人。
- 用**內容過濾**識別橫幅(同時要求 民國日期 + N診 + N號),不依賴 class 名稱,
  與 `_find_disposition_memo` 同一套思路。找不到就誠實回報,不猜。

**獨立定位索引檔**(使用者定案:除了寄信也要另存)

`settings/patient_locator_index.jsonl`(已 gitignore),保留 30 天。
**寫入在告警信去重之前** —— 告警信「同功能同日只寄一次」,同一天第二個出問題的病人
本來完全查不到,SMTP 掛掉時連第一個都會遺失。索引本身**沒有任何去重**,有測試釘住。

**本機驗證**:ruff 全清、pyright 0 errors、`pytest -q` → **2175 passed**。

---

### 金絲雀校正到 V.1150722.01 + 告警補「預期寫入」

| 檔案 | 變更 |
|---|---|
| `src/main.py` | `_HIS_CALIBRATED_VERSION` 1150720 → **1150722**;mismatch 告警加 `expected` |
| `tests/test_his_write_canary_2026_07_16.py` 等 4 支 | 既有守門測試同步到新校正版本 |

**依據**:使用者 2026-07-28 實機回報「**我已確定目前版本可以使用所有熱鍵功能 V.1150722.01**」。
院方在 7/22 改版(前一次校正是 7/20 的 1150720),但這次**選單 id 未位移**
(`MENU_ID_同意書` 仍 670、`MENU_ID_代碼輸入` 仍 219),所以只需把版本守門的隱性基線跟上。
不跟上的話,每一台**沒有自訂基線檔**的機器都會持續收到「疑似改版」通知。

⚠ **補審請確認**:`_HIS_CALIBRATED_VERSION` 只是「無基線檔時的隱性基線」
(`_his_write_baseline_fp`)。已經按過設定頁「重新校正」的機器有 `settings/` 底下的
基線檔,那份會勝出 —— 那些機器仍需手動再按一次「重新校正」。
**刻意不做「自動把舊基線檔升級成新校正版本」** —— 基線檔是使用者的明確校正,
靜默覆蓋會讓那個功能失去意義(既有註解把兩條路徑寫成平行選項,沒有自動升級)。

**同時修一個診斷缺口**:mismatch 告警原本只印「回讀到什麼」
(實機案例:`回讀 dose=800 count=21`),**沒印「本來要寫什麼」** —— 少了預期值就分不出
是「寫錯」還是「讀錯/根本沒寫進去」。信裡改成 `預期寫入:` / `實際回讀:` 兩行。
預期值取自帳本的 `value` 欄,依 `_record_his_action` 的契約必為非 PII(醫令代碼/劑量/療程數)。

**本機驗證**:ruff 全清、pyright 0 errors、`pytest -q` → **2179 passed**。

---

### 擴充性:HIS 契約單一宣告處 + 告警去重原語

| 檔案 | 變更 |
|---|---|
| `src/cmuh_common/his_contract.py` | **新檔**:校正版本 + 選單 id + 校正沿革(含憑據) |
| `src/cmuh_common/alert_dedupe.py` | **新檔**:`AlertDeduper`(claim/release + send_once)、`DailyOnce` |
| `src/main.py` | 選單 id / 版本改為別名;稽核健康與回讀不符改用共用去重器 |
| `docs/擴充規約_2026-07-28.md` | **新檔**:六條規約 + 每條的由來 |
| `tests/test_extensibility_primitives_2026_07_28.py` | 新增 16 項 |
| 既有 4 支測試 | 改讀單一宣告處 / 改用去重器公開介面 |

**兩項都是從今天的實際摩擦抽出來的,不是憑感覺重構。**

**1. HIS 契約散在三處 + 測試硬編碼版本**
校正版本在 `main.py:1502`、選單 id 在 2244 / 5096 / 5913(相隔數千行),4 支測試各自
寫死 `"1150720"` → 2026-07-28 校正到 1150722 實際動到 **5 個檔**。收攏後「下次改版 =
改一支檔」,而且 `CALIBRATION_HISTORY` 強制寫下**憑據**(改版本沒補歷史 →
`current_calibration()` 丟例外)。
★選單 id 的**數值**仍以字面值宣告並由測試字面值釘死★ —— 那是「打到別的選單 =
寫錯病歷」的安全防線,不可退化成 `assert X == X`。

**2. 告警去重樣板被重寫四次(8+ 個模組級全域)**
「inflight / 寄成功才終局去重 / 失敗下次重試」寫錯的三種方式各自對應:堆執行緒、
**告警永久滅音**(codex P1 抓過)、從此不再重試。而「永久滅音」看起來跟「一切正常」
一模一樣。抽成 `AlertDeduper`,已遷移稽核健康與回讀不符兩條。

⚠ **刻意未遷移**:HIS 改版通知那條多了「跨重啟持久化 + 寄成功但寫檔失敗要補寫」,
有 8 個全域。**Codex 額度用盡期間不做高風險重構** —— `persist_probe` 介面已預留,
留待下一輪。請外審評估這個取捨。

⚠ **請外審特別看**:`claim`/`release` 兩段式。原本 `_notify_audit_mismatch` 是
**在 spawn 之前同步佔 inflight**,若改成在背景緒裡才佔,兩次呼叫之間會各堆一條 60 秒
逾時的寄信緒。遷移時保留了這個順序,並用一支併發測試釘住。

**本機驗證**:ruff 全清、pyright 0 errors、`pytest -q` → **2194 passed**。

---

## 附:同日已完成外審的部分(不需補審)

| 版本 | 內容 | 外審結果 |
|---|---|---|
| v2026.07.27.1 | F9/F10 popup 關閉判定改用可見性(緊急修) | deep,APPROVE |
| v2026.07.27.2 | 隱藏桌面 systemftp 殘留 → 用行程 handle 收掉 | deep,APPROVE(1 輪) |
| v2026.07.27.3 | 完成人數實測值保存 + SQLite 鎖競爭不隔離快取 | targeted,APPROVE(2 輪) |
