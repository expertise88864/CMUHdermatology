# 待補審清單 — 2026-08-04（Codex 額度耗盡）

**狀態：待補審。** 額度恢復時間 **2026-08-08 11:39**。
使用者定案（2026-08-04）：先 push、標記未審，額度回來一次審核 —— 沿用
2026-07-31 那批的處理方式（見 `docs/待補審清單_2026-07-31.md`、
`docs/補審完成報告_2026-08-02.md`）。

> 規約要求未審 commit 的標題加 `[未經codex審查·待補審]` 前綴。
> **2026-07-31 那批漏了這件事**，這次在 push 前補上了。
> 補審完成前請勿刪除本檔。

## 待審 commit

| commit | base-ref | 主題 |
| --- | --- | --- |
| `b45bc7a` | `feaed5f` | BDE 重開機倒數期間使用者回來就取消（外審 P1-06） |
| `4885fb1` | `a6a3d6c` | PID 檔改存可驗證身分，擋住 PID 重用（外審 P1-07） |
| （批次 P 第一刀） | `975f6aa` | 只能終止自己開的 systemftp（外審 P1-01） |

`push_helper` 另外會產生 release commit（只有 `version.py` 與 `manifest.json`，
無邏輯），不需要審。

補審指令 —— **兩批一起審**（使用者定案：額度回來一次審核）：

```bash
bash tools/codex_review.sh deep feaed5f docs/待補審清單_2026-08-04.md
```

用 `deep`：P1-06 決定的是**診間電腦會不會在有人正在使用時被強制重開**，
P1-07 決定的是**watchdog 會不會強殺無關的自家程式**（`taskkill /F /T`），
兩者都是臨床安全，不是一般實作。

> 補審前先 `git fetch` 確認遠端狀態；若 base-ref 已不是 `feaed5f`，
> 改用當時實際的前一個 commit。

---

## 這次改了什麼

外部 review（基準 `3fb95e5` → `feaed5f`，v2026.08.04.4）列出 P1-06：
「BDE 自動重開機倒數期間，使用者回來操作不會取消」。逐條查證屬實：

- `_bde_reboot_watch_loop` 下達 `shutdown /r /t 60` 之後，只有
  `_bde_reboot_cancel.wait(timeout=55.0)`
- 該 event 只由「HIS 恢復」或「程式退出」設置
- `_user_idle_seconds()` 只用在**進入條件**（閒置滿 30 分），倒數期間不再檢查
- → 醫師在這 60 秒內回座打字，機器照樣重開

三個新東西：

1. `_user_idle_seconds_or_none()` —— 查不出來回 `None`，不用 `0.0` 冒充
   「剛剛有輸入」。舊的 `_user_idle_seconds()` 保留為相容包裝，語意不變。
2. `_await_reboot_countdown(total)` → `cancelled` / `user_back` /
   `idle_unknown` / `elapsed`。
3. `_abort_reboot_if_needed(outcome, *, rollback, run=None)` —— 依原因決定要不要
   `shutdown /a`。

---

## 可疑處與取捨（請重點檢視這幾條）

### 1. 用「閒置秒數倒退」偵測使用者回來，而不是設門檻值

閒置秒數在沒有新輸入時是**單調成長**的（每過一秒就多一秒），所以「比執行中的
峰值低超過容差」就是有人動了鍵盤或滑鼠。真的有輸入時是從 1800+ 秒直接掉回 0，
差距極大；容差 `2.0` 秒只用來吸收取樣抖動。

**可疑點**：如果 `GetLastInputInfo` 在某些情況下不是單調的（例如 session
切換、RDP 接管、GetTickCount 回繞邊界），這個判準會誤判成「使用者回來」而
不重開。誤判的方向是安全的（不重開），但會讓自動修復失效。

### 2. `elapsed` 必須讓機器重開 —— 這是刻意的

只守「該取消要取消」會把 fail-open 修成 fail-closed：自動修復**永遠不會發生**，
BDE 壞了就一直壞著。所以取消原因抽成 `_COUNTDOWN_ABORT_REASONS`，
**沒列在表裡的原因就是「讓它重開」**，並有一支測試專門斷言
`"elapsed" not in _COUNTDOWN_ABORT_REASONS`。

### 3. `idle_unknown` 取消重開

查不出閒置時間 → 保守當作可能有人在。代價是 `GetLastInputInfo` 若持續失敗，
自動修復就不會發生。這個方向與該檔案既有的
「查詢失敗回 0 ＝ 當作剛有輸入 ＝ 寧可不重開」一致。

### 4. `shutdown /a` 失敗只記 critical，沒有寄信

失敗代表「該取消卻取消不掉」，機器仍會重開。沒有加寄信的理由：機器距離重開
只剩約 50 秒，寄信執行緒不一定跑得完，而 log 是留得住的紀錄。
**這是取捨，不是遺漏** —— 若審查認為該補寄信（或改用同步短逾時寄送），可以提。

### 5. 改動了既有測試（請確認我沒有削弱它）

`tests/test_consult_persistent_session_2026_08_03.py::`
`test_recovery_during_countdown_aborts_the_shutdown` 原本是**掃原始碼字串**：
在 `_bde_reboot_watch_loop` 的原始碼裡找 `_bde_reboot_cancel.wait(timeout=55.0)`，
再檢查其後的文字。取消動作抽成函式後那個字串不在該函式裡了 —— 測試會
`ValueError` 而不是紅在「性質沒了」。**掃原始碼的守衛，程式碼一搬家就失效。**

改成直接驗它原本要的三個性質，搬到哪裡都測得到：

- 取消要用 `shutdown /a`
- 取消成功要回滾時間戳
- `/a` 失敗不得假裝取消了（時間戳也不可以被清掉）

---

## 本機驗證（已跑，Codex 不需重跑）

- ruff `src scripts tests`：All checks passed
- pyright：0 errors, 0 warnings
- `scripts/type_debt.py`：沒有新增型別債
- pytest：**3500 passed, 2 skipped**
- 逐層覆蓋率門檻、skip 守衛：都通過

### 突變驗證：8 個全部轉紅

| 突變 | 意義 |
| --- | --- |
| watch loop 改回只等取消令 | ＝ bug 原樣 |
| 倒數期間不再讀閒置 | 守衛拿掉 |
| 查不出來當成 0 | 冒充「剛有輸入」 |
| 容差開到極大 | 真的有輸入也偵測不到 |
| 容差歸零 | 抖動被誤判成使用者回來 |
| 三種原因共用同一句說法 | 事後無從分辨機器為什麼沒修好 |
| **把 `elapsed` 也列成取消原因** | **自動修復永遠不發生** |
| 取消失敗仍回報成功 | 措辭不誠實 |

前三輪自我檢查中有三個突變曾經沒轉紅（接線沒被測到、掃原始碼的斷言被同函式
另一個 `return None` 餵飽、抖動測試根本沒碰到容差），都已補強後才提交。

---

## 同一份外審的其他項目（**尚未施工**，不在本次待審範圍）

全部已逐條對照程式碼查證為 CONFIRMED：

| 項目 | 內容 |
| --- | --- |
| P1-01 | `our_pids = (_systemftp_pids() - before) \| {our_pid}` 全機 PID 差集當 ownership |
| P1-02 | borrowed session 仍會對醫師的 HIS 視窗輸入帳密、按登入、點病人 |
| P1-03 | 固定 `sleep(1.8)` 後即截圖，載入中的空／部分清單被當成有效資料 |
| P1-04 | `_consult_signature_from_roster()` 回傳病歷號 `set`，同病人第二張會診被吸收＝**漏寄** |
| P1-05 | 分不出首次安裝與基準損毀（程式碼註解自承） |
| P1-07 | PID file 只比對 `proc.name()`，擋不住被自家另一支 pythonw 重用 |
| P1-08 | 每輪都無條件落地完整 PHI 截圖，與有無新會診無關 |

對應待辦：#54（P1-07）、#55（P1-01/02）、#56（P1-03/04/05）、#57（P1-08 + P2 群）。

---

# 批次 O：PID 檔改存可驗證身分（外審 P1-07）

## 問題

`src/cmuh_common/pidfile.py` 的 docstring 三處宣稱「PID 會被作業系統重用，不驗就
可能誤殺別人」，而實際驗證只有：

```python
proc.name().lower() in ("python.exe", "pythonw.exe", "python", "pythonw")
```

那只能濾掉**非 Python** 的行程。**這台機器上六支 CMUH 程式全都是 pythonw.exe**，
所以 stale PID 檔的 PID 被自家另一支程式重用時，驗證照樣通過。

而 `read_verified_pid` 的結果會走到
`watchdog_core._find_pids_holding_mutex` → `kill_pid` → `taskkill /F /T /PID`。
**這不是「找不到而不動作」，是強殺無關的自家程式連同它的整棵行程樹。**
宣稱擋住了，實際沒有。

## 修法

PID 檔改存 JSON：`schema` / `app_id` / `pid` / `create_time` / `executable`。

驗證分成兩級：

| 級別 | 欄位 | 不符時 |
| --- | --- | --- |
| 必要 | schema、app_id、pid 活著、**create_time 相符**、name 是 python 系 | 不採用 |
| 加強 | executable 路徑 | 讀得到才比；**讀不到不否決** |

核心是 **create_time**：被重用的 PID 必然屬於「後來才建立」的行程，建立時間不可能
與當初記下的相同。

## 為什麼是 create_time 而不是別的（實測，不是推理）

本機 397 個行程、非提權 Python：

| 屬性 | 成功 | 失敗 |
| --- | ---: | ---: |
| `create_time()` | 397 | **0** |
| `name()` | 397 | 0 |
| `exe()` | 396 | 1 |
| `cmdline()` | 393 | 4 |

`create_time()` 一次都沒失敗 —— 需要的判準剛好是最可靠的那個。
`cmdline()` 失敗 4 次，正好印證這個模組當初存在的理由。

## 請重點檢視的取捨

### 1. `exe()` 只做加強，讀不到不否決
實測會失敗（1/397）。若當成必要條件，就會在讀不到的機器上退回那條壞掉的 cmdline
路徑 —— **把這個功能修回它本來要解決的問題**。身分已由 pid + create_time 決定，
exe 是縱深防禦。**這是刻意的 fail-open，請確認我沒有開太大。**

### 2. 沒有加 nonce（外審有建議）
nonce 寫在檔案裡**無從與活著的行程對照**（讀不到別人的行程記憶體），不會增加任何
鑑別力。與其多一個看起來嚴謹、實際是裝飾的欄位，不如三個真的驗得到的檢查。

### 3. 舊格式（純數字）一律不採用
沒有 create_time，無從驗身分 → 退回 cmdline 路徑（＝這個模組出現以前的行為：
可能找不到，但不會殺錯）。程式下次啟動就自報成新格式，所以只是升級後到重啟前的
短暫狀態。**代價是這段期間半死救援失效**，有 log 說明。

### 4. 容差 `0.05` 秒
只是不想讓浮點表示的最後一位決定「要不要強殺一支程式」。真的被重用時兩者的建立
時間差好幾秒以上。兩個方向各有測試（開到極大＝重用偵測不到；歸零＝同一行程被誤判）。

## 改動既有測試（請確認我沒有削弱它）

`test_dead_pid_is_rejected` 與 `test_non_python_pid_is_rejected` 原本寫的是舊格式
純數字檔。新版把舊格式一律擋掉，於是這兩支會**因為錯的理由**通過 —— 名義上測
「行程已死」「不是 python」，實際只測到「舊格式被擋」。改用新格式 helper 寫檔，
讓它們真的走到各自宣稱的那條路。

## 本機驗證

- ruff / pyright / 型別債：全綠
- pytest：**3510 passed, 2 skipped**
- 覆蓋率、skip 守衛：通過
- 突變驗證 **9 個全部轉紅**：回到只比對 name（＝bug 原樣）、建立時間不比、沒有
  建立時間也放行、容差極大／歸零、exe 讀不到就否決、app_id 不比、
  **write 不記建立時間**（實機救援會靜默失效）、舊格式合成一份「已驗過」的紀錄。

> 其中「write 不記建立時間」一開始沒轉紅 —— 原本只斷言 `read_raw_pid` 讀得回
> PID，而那不涉及任何身分欄位。補了「寫出來的紀錄餵給驗證器要通過」的往返測試。

---

# 批次 P（第一刀）：只能終止自己開的 systemftp（外審 P1-01）

## 問題

`our_pids` 來自【全機 PID 差集】：`(_systemftp_pids() - before) | {our_pid}`
（`src/consult_query.py` 兩處）。冷啟動最多等 120 秒，醫師在這段期間手動打開
住院系統，他那個行程就落進差集。之後：

```python
def _terminate_session_process(sess):
    """...不可能誤殺使用者的醫囑系統。"""   ← 宣稱
    close_pids(sess.our_pids)               ← 對【全機差集】送 WM_CLOSE
    ...TerminateProcess(sess.hproc)         ← 這半段才真的安全（靠 handle）
```

docstring 描述的是 handle 那半段，前面那行吃的是差集。**又是宣稱對不上實作。**

## 修法：分清楚兩件事

| | 判準 | 弄錯的後果 |
| --- | --- | --- |
| 找視窗的候選集 | 可以寬鬆（維持現狀，本刀不動） | 找不到 → 無傷害 |
| **可以終止的集合** | **必須封閉** | **關掉別人的程式** |

新增 `_verified_owned_pids(root_pid, candidates)`：只留 root 自己與可驗證的後代
（`psutil.Process(root).children(recursive=True)`）。終止端改用它。

另外 SW_HIDE 後備模式原本 `subprocess.Popen(...)` **把物件整個丟掉**，那條路因此
沒有任何直接的所有權事實 —— 與 2026-07-27 事故同一條教訓（spawn 要留 handle）。
改成留住並以其 pid 當 root。

## 請重點檢視的取捨

### 1. 驗不出後代時只認 root（可能留孤兒）
root 已結束、或 psutil 不可用時，只終止 root。代價是它的後代可能變孤兒；但孤兒
有既有清理機制（`_kill_systemftp` 的 before 快照），而**關掉醫師的 HIS 沒有任何
補救**。★寧可留孤兒，不可誤關★ —— 請確認這個方向沒有被我開得太寬。

### 2. `borrowed` 開關本來就不足以封閉所有權
它只擋「啟動前就存在」的行程。醫師在冷啟動那 120 秒內**新開**的不在 `before`
裡，borrowed=False，舊邏輯照樣會關掉它。`before` 快照只認得「更早以前」。

### 3. 本刀【沒有】改的部分（刻意）
- `our_pids` 本身仍是差集（找視窗用），沒有換成 process tree
- P1-02 的「找不到屬於自己的登入視窗就中止、不借用」**尚未做**

理由：我無法在開發機上確認 systemftp 的真實行程拓撲（登入視窗的 PID 究竟是
`_spawned_pid` 本身、它的後代、還是由服務另外生出來的兄弟行程）。若拓撲假設錯
而我把找視窗那側也改成只認 process tree，會讓冷啟動在診間機器上**完全找不到
登入視窗**。所以這一刀只封閉「終止」這個會造成傷害的方向，並加上實機證據 log：

> `[session] 收尾時排除 N 個不屬於本次啟動的 systemftp(pid=[...])`

那行出現就代表差集確實含有不屬於我們的行程 —— 是把找視窗那側也收緊的依據。
只記 PID 與數量，不記視窗標題（非病人資料）。

## 本機驗證
- ruff / pyright / 型別債：全綠
- pytest：**3523 passed, 2 skipped**；覆蓋率、skip 守衛通過
- 突變驗證 **10 個全部轉紅**：終止端回到整個差集（＝bug 原樣）、驗證直接放行、
  驗不出後代時 fail-open、**連自己的後代也不收（反方向，會累積孤兒）**、
  root 不保證留下、排除的行程不留證據、SW_HIDE 不傳 root_pid、SW_HIDE 不留 Popen。

> 其中兩個一開始沒轉紅：**接線沒被測到**（測試都直接呼叫
> `_verified_owned_pids`，把終止端改回舊寫法照樣全綠）與 `keep.add(root_pid)`
> 在原測試輸入下是多餘的（候選集本來就含 root）。都補了測試。

---

# 批次 Q（第一刀）：會診識別改成 request-level（外審 P1-04）

## 問題
`_consult_signature_from_roster()` 回傳【病歷號集合】。它只能回答「這位病人在不在
清單上」，回答不了「這是不是同一位病人的**另一張**會診」——因為
`{"12345678", "12345678"} == {"12345678"}`。同一病人的第二張會診被吸收掉 →
★漏寄★，臨床上最糟的方向。

## 修法
識別改成 `病歷號|日期|時間`（`_consult_id_from_row`）。缺日期/時間時退回只用病歷號
（＝舊行為），鑑別力只會變好或持平。

### ★不採納外審建議把病房/床號放進識別★
審查建議 `chart + 會診時間 + ward/bed + 申請科別`。**病房/床號不採納**：病人轉床
很常見，而轉床**不是新的會診** —— 放進識別會在每次轉床多寄一封（誤寄）。真正屬於
「這張會診單」的是**申請時間**：轉床時不變，新開一張時必然不同。有測試守這一條。

## ★升級相容：最危險的地方★
舊基準 `consult_notified.json` 只有 `charts`（病歷號）。若拿新識別直接對它做集合
相減，**升級當下每一張既有會診都會變成「新的」→ 對團隊整份重寄**。

作法：
- 讀到沒有 `ids` 的舊檔 → 記 `_notified_is_legacy=True`
- `_new_consult_ids()` 在 legacy 時降級成**病歷號粒度**比對一輪
- `_save_notified()` 一寫就是新格式，旗標關掉，之後恢復完整鑑別力
- **檔案同時保留 `charts`**：診間電腦不是同時更新的，新版寫的檔可能被還沒更新的
  舊版讀到；舊版只認得 `charts`，少了它會整份重寄

## 我自己改壞又被既有測試抓回來的地方
第一版把「解析不到結構的列」也改成只取第一個病歷號。實機上會有**整塊文字被當成
一列**傳進來的情況（`test_poll_first_startup_builds_baseline_silently` 的 fixture
就是），只取第一個會把後面的會診整個丟掉 —— 那是漏寄，比識別粒度不夠更嚴重。
已還原成舊行為（掃出該列全部病歷號）。

## 又一支被重構打敗的守衛
`test_poll_no_new_path_prunes_baseline` 錨在整串
`_new = _poll_sig - _load_notified()`。右手邊換掉之後 `str.index` 直接 ValueError
—— 紅在「找不到錨點」而不是「性質沒了」。錨點改成 `_new = `（只綁變數名）。

## 本機驗證
- ruff / pyright / 型別債：全綠
- pytest：**3536 passed, 2 skipped**；覆蓋率通過
- 突變驗證 **8 個全部轉紅**：識別回到只用病歷號（＝bug 原樣）、把床號放進識別
  （轉床多寄）、poll 回到直接集合相減（升級整份重寄）、升級相容關掉、
  **反方向：升級那輪把真的新會診也吞掉**、存檔不再寫 charts（舊版讀不到）、
  存檔後不關 legacy 旗標、解析不到的列只取第一個病歷號。

## 本刀未做（留待下一刀）
- P1-03 roster readiness（`sleep(1.8)` 後即截圖，載入中的空／部分清單被當成有效資料）
- P1-05 baseline 損毀 vs 首次安裝的狀態分流

---

# 實機診斷：session 判死要說出哪一個成因

## 背景（不是外審項目，是實機 log 發現的）
使用者 2026-08-04 提供診間 `consult_query.log`。**「常駐登入」從未生效**：
13:44→14:29 共 15 輪，每一輪都是

```
[session] 已退回主畫面,session 續留
（3 分鐘後）
[session] 收掉 systemftp(pid=XXXX,現任):session 已死(行程結束或主畫面不見了)
[session] 常駐 session 已建立(pid=YYYY)
```

即每 3 分鐘冷啟動一次、**重新送一次帳密**。而 `_cold_start_session_impl` 的
docstring 明文寫著「3 分鐘 keepalive 節奏絕不可把同一組帳密每 3 分鐘送一次」——
實際行為與設計意圖直接相反，**有帳號鎖定風險**。

## 為什麼這次只加診斷、不改判定
`_session_is_alive` 有兩個條件，而舊訊息把它們寫成同一句
（`行程結束或主畫面不見了`）——**45 分鐘的 log 判斷不出是哪一個在觸發**，
而兩者的修法完全不同。

推測（★尚未證實★）：同一份 log 裡每輪 spawn 的 pid 都不同，真正持有視窗的是
`10928`——我們從未啟動過它。若 systemftp 是啟動器型行程（起來、把工作交給既有
實例、自己結束），`sess.hproc` 就會立刻 signaled → 永遠判「行程結束」。

★不憑推測動刀★（批次 P 已經因為同一個理由留手一次）。本次把成因拆成三種
可分辨的訊息，下一輪診間 log 會直接說出答案：

- `我們啟動的 systemftp 行程已結束`
- `行程還在,但找不到主畫面視窗`
- `無法查詢 systemftp 行程狀態`（★查不到 ≠ 已結束★，措辭鐵律）

## 同一份 log 也證實了 P1-01
`our_pids` 每輪都含 `10928`，且 13:16 有一行

```
[cleanup] pid 10928 已非 systemftp(PID 重用?),略過
```

混進來的 PID **連 systemftp 都不是**。沒釀成災難只因為 cleanup 那條路有守衛；
`_terminate_session_process` 的 `close_pids` 沒有 —— 那正是 v2026.08.04.10
（批次 P 第一刀）修掉的東西。**實機證據支持該修法。**

## 改動既有測試
`test_idle_session_reuse_takes_the_lease` 只 patch `_session_is_alive`。接縫換成
`_session_death_reason` 之後注入不到，會掉進真實路徑冷啟動，紅在
`KeyError: 'username'` 而不是「租約沒拿到」。已補上新接縫的注入。

## 本機驗證
- ruff / pyright / 型別債：全綠
- pytest：**3542 passed, 2 skipped**；覆蓋率通過
- 突變驗證 4 個全部轉紅：兩個成因共用同一句（＝實機 log 判斷不出來的原樣）、
  查不到狀態說成行程結束、**反方向把活著的 session 也判死**、
  `_acquire_session` 不取用原因（診斷沒接上）。

---

# 第 2 輪外審的回應（基準 c41182b / v2026.08.04.10）

> 注意：那一輪審的是 `c41182b`，**在批次 Q 與 session 診斷之前**。
> 它列的 **P1-07（會診 identity 仍只是病歷號 set）已於 v2026.08.04.12 修掉**
> （commit `3fff2cc`），不是未施工項目。

## 已修（本次）

### P1-04：PID file 的 NaN 可繞過建立時間驗證 —— CONFIRMED（實測）
```
json.loads('{"create_time": NaN}')   → 預設【接受】
abs(1780000000.5 - nan) > 0.05       → False   ← 不符也攔不下來
```
整個防重用驗證被一個 token 繞過。這是我今天寫的程式碼裡的真 bug。
改用 `_finite_float()`（回值或 None，不回 bool —— 回 bool 的話 pyright 無法
透過函式呼叫收窄型別，`float(want)` 會多出型別債，實測 147→149）。
`bool` 一併擋掉（它是 `int` 的子類）。另加第二道 `parse_constant`，讓含
NaN/Infinity 的檔案連「被解析成 dict」都不會發生。

### P1-01 盲區 A：初始取樣競態 —— CONFIRMED，且審查的修法比我的好
第一版用【相對】判準（比執行中的峰值倒退超過容差）。那需要一個乾淨的 baseline，
而 baseline 是進入倒數之後才取的第一個樣本 —— 使用者若在「決定重開」與「第一個
樣本」之間回來，baseline 本身就是低的，之後單調上升永遠看不到倒退。

改用【絕對】判準：自動重開機的前提就是「已閒置滿 30 分鐘」，該前提在倒數期間
必須持續成立。任何一次量到低於門檻就是有人動過 —— 不需要 baseline、不需要容差，
初始取樣競態自然消失。`_IDLE_REGRESSION_TOLERANCE_SEC` 一併刪除。

### P1-01 盲區 B：60 秒只監測 55 秒 —— 部分接受
**不照建議改成監測滿 60 秒**：那樣就沒有時間執行 `shutdown /a` 了。留餘裕是
取捨。但我的註解寫「一直驗到最後一刻」**是不實的宣稱**（實作根本沒有那個性質）
—— 已改，並把餘裕具名為 `_REBOOT_CANCEL_MARGIN_SEC` 讓它是明示的設計。

## 尚未施工（已記待辦，需要更多依據）
| 項目 | 為什麼還沒做 |
| --- | --- |
| P1-02 `_kill_systemftp()` 全機差集 | 需要與 P1-03 一起改（同一條 SW_HIDE 路徑） |
| P1-03 SW_HIDE stealth/borrowed | 實機 log 顯示登入視窗屬於我們**未曾啟動**的行程（10928）；拓撲未明前不動找視窗那側 |
| P1-05 kill 前 TOCTOU | 要把裸 PID 換成 handle-based terminate，改動面較大 |
| P1-06 roster readiness | 批次 Q 第二刀 |
| P1-08 baseline 損毀分流 | 批次 Q 第二刀 |
| P1-09 PHI 截圖 | 批次 R |
| P2-01 退出與倒數 thread 重複 `/a` | 已理解，待與 reboot state machine 一起做 |

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（**各自看 exit code，不接 pipe**）
- pytest：**3549 passed, 2 skipped**；覆蓋率通過
- 突變驗證 8 個全部轉紅，含反方向（門檻設到極大＝永遠不重開、正常紀錄被擋掉）
  與「parse_constant 不拒收」（第二道防線要真的承重，改用【別的欄位】帶 NaN 才測得到）

---

# P2-05：IMAP debug 不可以把信件主旨原文寫進 log（實機證實）

## 實機證據
診間 `consult_query.log`（2026-08-04）中這行出現 **3850 次**：

```
（最近未讀主旨樣本…）：'Microsoft Outlook 測試郵件' | '安全性快訊' | …
```

那個信箱收到的**任何**信件主旨都會被寫進 log。這份 log 撈到的剛好無害，但機制
是確認的 —— 只要收到含病人姓名或床號的信，那些字就進到一個**沒有 Email 保存
政策**的檔案。

## 修法
`imap_reader._subject_fingerprint()`：主旨 → `len=N sha=xxxxxxxx`（SHA-256 前 8 碼）。
收集點與呼叫端的訊息一併改。

**診斷價值幾乎沒有損失**：這行的用途是「確認我的觸發信有沒有進收件匣」，而真正
回答那件事的是 `matched` 的數字。指紋仍能區分「還是原來那幾封沒動」與「有新信
進來了」。**要看主旨請直接開那個信箱 —— 使用者本來就讀得到，log 不需要複製一份。**
訊息文字也一起改掉（不再宣稱在給「主旨樣本」）。

## 未一併處理（刻意）
`imap_reader.py` 另有一行記錄**陳舊觸發信**的主旨與寄件人。那是使用者自己寄的
觸發信、主旨依定義含自訂關鍵字，風險等級不同；且「哪一封被忽略」是有價值的診斷。
留著，但已知悉。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（各自看 exit code）
- pytest：**3556 passed, 2 skipped**；覆蓋率通過
- 突變驗證 5 個全部轉紅：指紋回傳原文（＝bug 原樣）、指紋夾帶主旨前幾個字、
  **反方向所有指紋都一樣（log 失去資訊）**、收集時仍存原文、呼叫端改回輸出原文。

> 「收集時仍存原文」一開始沒轉紅 —— 測試驗了指紋函式與呼叫端格式化，卻沒有
> 任何一支確認 `check_trigger` 真的把指紋放進 samples。★本 session 第四次
> 同一個形狀（接線沒被測到）★，已補 AST 檢查。

---

# 批次 Q 第二刀（上半）：基準遺失／損毀不得靜默吞掉（外審 P1-05）

## 問題
只要基準沒建立起來，第一輪 poll 就一律 `_save_notified(目前清單)` 然後 return、
不寄任何信。對真正的首次安裝那是對的（避免每次重裝收全清單），但它分不出：

    真正首次安裝 / 檔案被刪 / JSON 損壞 / 防毒鎖住讀不到

後三種會把當下**所有**未回覆會診靜默標成「已通知」——那些會診從此不會有人收到，
而且沒有任何跡象。★無聲漏寄★

## 修法
- 用既有的 `safe_load_json_ex()`（**前人已經做好**，回 ok/missing/corrupt/error，
  不重造）記下讀取狀態。
- 加「建立過基準」標記檔區分首次安裝與其他三種。
- 其他三種 → **fail-open**：當作全部都是新的寄出去一次、信裡註明請人工核對、
  另寄一封系統告警給開發者。寧可多寄一封讓人核對，不可以無聲漏掉會診。

## 兩個順序上的坑（都是實作時踩到、由測試抓回來的）

### 1. 標記必須在「存檔成功之後」才寫
我第一版寫在 `atomic_write_json` **之前**。寫檔失敗卻留下標記 → 下次啟動看到
「標記在、基準檔不在」→ 判成遺失 → **白白對團隊重寄整份清單並發告警**。
那是自己製造的假警報。

### 2. 標記不可以在啟動時寫
啟動時寫的話，真正的首次安裝在第一輪 poll 就已經有標記，會被判成遺失而寄整份 ——
首次安裝寄整份正是當初要避免的事。所以只在成功存基準之後才寫。

### 3. `corrupt` 不需要標記也不算首次安裝
內容壞掉本身就證明那個檔存在過。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（各自看 exit code）
- pytest：**3566 passed, 2 skipped**；覆蓋率、skip 守衛通過
- 突變驗證 **8 個全部轉紅**，含**反方向**的「首次安裝也被當成遺失（會寄整份清單）」
  與「標記寫在存檔之前（造成假警報）」。

## 本刀未做
P1-03 roster readiness（`sleep(1.8)` 後即截圖）留在下一刀 —— 它要引入
`RosterSnapshot` 型別契約與穩定性判定，改動面比這半刀大得多，混在一起不好審。

---

# 批次 R（第一刀）：沒有新會診的輪次不落地 PHI 截圖（外審 P1-08）

## 問題
`_query_cycle` 以前**無條件** `img.save()`，而且發生在解析 roster **之前** ——
跟有沒有新會診毫無關係。常駐模式 3 分鐘一輪 ＝ 每小時 20 張「沒寄出去、也沒有
臨床用途」的完整病人畫面躺在磁碟上。

## 修法
截圖先留在記憶體（`capture_window_image` 的回傳值直接往上傳），只有走到「這一輪
真的要寄信」那一行時才呼叫 `_materialize_shot()` 落地。沒有新會診的輪次在更上面
就 `return` 了，磁碟上不會多出任何東西。

兩個擷取點（隱藏桌面 `_query_cycle`、後備 `_run_with_sw_hide`）都改。

## 與外審建議的差異（刻意，請檢視）
外審建議「寄完 `finally` 刪掉」。這裡改成**落地到既有的 `consult_shots/` 並沿用
既有 TTL**：

- 那張圖**本來就已經寄給臨床收件人**了，留在本機不會擴大暴露面
- 出事時「當時畫面長怎樣」是最有用的線索
- 保留期本來就有上限（TTL + 數量後備），不會無限累積

真正該消滅的是「**沒寄出去卻留著**」的那 20 張/小時 —— 那個已經消滅了。
若審查認為連已寄出的也不該留，可以再收緊。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（各自看 exit code）
- pytest：**3572 passed, 2 skipped**；覆蓋率、skip 守衛通過
- 突變驗證 5 個全部轉紅，含**反方向**的「落地函式什麼都不寫」（附件會壞掉）。

## 兩個自己踩到的坑
1. **測試被自己的註解餵飽**：第一版用 `"img.save(" not in src` 掃原始碼，而我在
   那兩個函式裡寫的說明註解本身就含 `img.save()` 這幾個字 → 測試紅在自己的散文
   上。改用 AST 只看真正的呼叫。
2. **`# noqa` 寫成裝飾**：`# noqa: PLC0415（中文說明）` 是無效指令，ruff 會警告
   「directive 無效」——那個抑制根本沒有生效。說明文字移到上一行。

---

# ★實機定案★ 常駐登入從未常駐：liveness 訊號用錯了東西

## 證據（不是推理）
加了可分辨診斷之後的診間 log（`consult_query (3).log`，2026-08-03 15:06 →
2026-08-04 16:56）：

```
21 次  session 已死：我們啟動的 systemftp 行程已結束
 0 次  行程還在,但找不到主畫面視窗
```

**100% 集中在同一個成因。** 也就是說 `systemftp.exe` 是**啟動器型行程** ——
起來、把工作交給既有實例、自己立刻結束。`sess.hproc` 握的是那個啟動器的 handle，
它必然馬上 signaled，於是 `_session_is_alive` 每一輪都說「死了」。

後果：整份 log 共 **151 次冷啟動 = 151 次送出帳密**。而
`_schedule_jobs` 自己的註解寫著

> ★SW_HIDE 後備=每輪【完整登入】★ 絕不可套 3 分鐘節奏——那是每小時把同一組
> 帳密送出 20 次，**正是登入鎖定門檻的來源**。

常駐路徑本來就是為了避免這件事，結果它一直在做這件事。

## 修法
權威訊號改成**主畫面視窗**：session 能不能用，取決於主畫面在不在，不取決於當初
spawn 的那個啟動器活著沒有。行程 handle 降級成「主畫面不在時，用來說明是哪一種
不在」。

```
主畫面在                    → ""（可用）
主畫面不在 + 行程已結束      → "…行程已結束,主畫面也不在"
主畫面不在 + 行程還在        → "行程還在,但找不到主畫面視窗"
主畫面不在 + 行程狀態查不到  → "無法查詢…,且主畫面不在"
```

## 改動既有測試（★這裡要特別檢視★）
`test_a_dead_process_says_so` 原本斷言「我們 spawn 的行程結束了就判死」——
**那個期望本身就是 bug**。已改寫成
`test_a_dead_launcher_with_a_live_main_window_is_still_usable`，並在 docstring
裡寫明它原本斷言相反的事、以及為什麼那是錯的。

> 我很清楚「改測試讓它過」是最容易自欺的動作。這裡的依據是**實機 log 的 21/21**，
> 不是我覺得應該這樣。若審查認為該保留舊語意，請直接推翻。

## 未一併解決（誠實說明）
主畫面所屬的行程**不是我們啟動的那一個**（log 中長期存在的 `10928`）。這個修正
讓我們不再每 3 分鐘重新登入它，但**沒有回答「那個實例是不是醫師自己的」** ——
那是外審 P1-02/P1-03（SW_HIDE borrowed / stealth）仍未關閉的部分。
修正前後都在驅動同一個實例，只是頻率從「每 3 分鐘重新登入」降為「持續重用」。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（各自看 exit code）
- pytest：**3573 passed, 2 skipped**；覆蓋率通過
- 突變驗證 4 個全部轉紅，含**反方向**的「主畫面不在也當成活著」（會拿死 session
  一直查）。

---

# 批次 Q 第二刀（下半）：roster 要等它不再變動（外審 P1-03）

## 問題
`_query_cycle` 只 `time.sleep(1.8)` 就當清單載入完了。Delphi 視窗是先建立、資料
再逐步填進去的，那一秒八**不保證**看到的是最終狀態：

- 還沒載入 → 空清單被當成「成功且真的沒有病人」→ 基準被剪成空
  → 下一輪所有既有會診都變「新」→ **對團隊重寄整份清單**
- 載入到一半 → partial roster 被存成基準 → 還沒出現的病人此後不算新 → **漏寄**

**固定睡多久都治不了**（慢的機器仍會失手）。要看的是「內容有沒有還在變」。

## 修法
`_await_stable_roster()`：連續 3 次讀到完全相同才算穩定（每 0.25 秒取樣，逾時 6 秒）。

**空清單也要通過同一道關**：「找不到 radio」與「真的沒有病人」在畫面上長得一模
一樣，唯一分得出來的線索就是它穩不穩定。

## 為什麼不做 `RosterSnapshot` 全套狀態機
外審建議 VALID_NONEMPTY / VALID_EMPTY / LOADING / UNSTABLE / SCHEMA_INVALID /
TIMEOUT 六態，以及「空清單必須有明確的 UI marker」。

**那個 marker 我不知道長什麼樣** —— 那是實機知識，開發機上沒有 HIS 可以量。
批次 P 已經因為同一個理由留手過一次；憑猜測寫一個 marker 判定，猜錯就是把
「真的沒病人」也判成 LOADING，每輪 fail-open 寄信 = 天天洗信箱。

所以這一刀只做**穩定性**這個不需要實機知識的判準，而且**接上既有通道**：
不穩定 → `roster_texts = None` → 走既有的「無法判斷 → fail-open 照寄、
**不更新基準**」那條路。呼叫端一行都不用改。

顯示用與判斷用分開：看到什麼照樣附在信裡，但「有沒有新會診」不拿一份還在變的
清單去判斷。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（各自看 exit code）
- pytest：**3581 passed, 2 skipped**；覆蓋率、skip 守衛通過
- 突變驗證 **6 個全部轉紅**，四個方向都守：
  回到單次讀取（漏寄/誤寄）、不穩定也當有效、**穩定也回報判斷不了（每輪重寄）**、
  只讀一次就算穩定、**永遠不算穩定（沒病人時天天洗信箱）**、逾時卻回報穩定。

---

# 第 3 輪外審的回應（基準 c41182b → 4c093f9 / v2026.08.04.25）

## ★P1-05：teardown 關不掉真正的 HIS —— 這是我造成的迴歸★

**外審抓對了，而且這是本輪最重要的一條。** 推理鏈：

1. 實機證實 systemftp 是啟動器型行程 → 我們 spawn 的 root **立刻結束**
2. `_verified_owned_pids(已死的 root, ...)` → `psutil.Process(root)` 失敗 →
   except → 只保留 `{已死的 root}`
3. `close_pids({已死的 pid})` → **什麼都關不到**
4. `WaitForSingleObject(hproc)` 早已 signaled → `still=False` → **不執行 Terminate**

淨結果：teardown 只清掉 Python 這邊的參照，真正的 HIS UI 還留在隱藏桌面**登入中**。
「6 小時定期重啟」「休息時段收掉」「接管舊 worker」全都只是說說而已。

**這是批次 P（只終止驗證過的自有行程）換來的代價** —— 我把「可能誤關醫師的」換成了
「關不掉自己的」，而當時沒有察覺。

### 修法（本輪做的）
行程層面關不掉，就從**視窗層面**關：`_close_session_windows()` 對確切的主畫面送
`WM_CLOSE` 並**回讀確認它真的不見了**。★送了不回讀就是開迴路★——這個檔案反覆踩過
那個坑。關不掉就明確 log「這個 session 仍然登入中，請人工確認」，不假裝收乾淨。

### 本輪【沒有】做的（同意外審，但需要更大的一刀）
外審要求 session 保存 exact `main_hwnd / main_pid / create_time / generation`，
並在無法證明 ownership 時進入 `UNMANAGED_SESSION`。同意方向，但那是重做 session
identity 的一整批，不適合塞在這裡。本輪先把「關不掉」這個**當下就在發生**的問題補上。

## P1-01：空清單在 0.5 秒就被判成 valid empty —— 外審抓對了
`_ROSTER_SETTLE_READS=3` × `0.25s` = **0.5 秒**。而【空】剛好也是「還沒開始載入」的
樣子，所以 0.5 秒的「穩定空清單」會被當成「今天真的沒有會診」→ 基準剪成空 →
下一輪整份重寄。**與舊版固定睡 1.8 秒是同一個根本問題。**

外審也正確指出我那支「2 秒後才有 radio」的測試**沒有真的模擬時間**（`sleep` 是
no-op、也沒有可控時鐘），所以測不到這件事。

### 修法
`_ROSTER_EMPTY_MIN_OBSERVE = 3.0`：**空**清單要觀察滿 3 秒才算數；**有內容**的不必
多等（資料都出現了，不可能是「還沒載入」——多等會把每輪拖長 3 秒，而常駐節奏還要
維持 keepalive）。測試改用可控時鐘，並補上外審點名的情境（前 1.5 秒空、之後才出現）。

## 尚未處理（已理解，排入後續）
| 項目 | 說明 |
| --- | --- |
| P1-02 snapshot generation | roster/截圖/HWND/memo 不是同一輪的樹 —— 同意，屬 session identity 那一批 |
| P1-03 截圖在 readiness 之前 | 同上，一起改比較不會來回動同一段 |
| P1-04 identity 仍受 regex 限制 | 英文姓名、同分鐘兩張、文字去重砍掉不同 HWND |
| P1-06 `_kill_systemftp` 全機差集 | **仍在**，且繞過 `_verified_owned_pids` |
| P1-07 SW_HIDE stealth/borrowed | 仍會隱藏/借用非自有 HIS |
| P1-08 寄信失敗仍留截圖 | `_materialize_shot` 在 send 之前 |
| P2 群 | 第一位 memo readiness、`_return_to_main` 驗證、TOCTOU、`/a` 重複執行、stale trigger 原文、IMAP HMAC |
| 五、baseline 與 marker 分兩份檔 | 同意合併成單一 schema 檔 |

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（各自看 exit code）
- pytest：**3590 passed, 2 skipped**；覆蓋率、skip 守衛通過
- 突變驗證 **6 個全部轉紅**：teardown 不關主畫面（＝迴歸原樣）、送了不回讀、
  關不掉卻回報成功、列舉失敗當成已關閉、空清單回到 0.5 秒、
  **反方向：有內容也多等 3 秒（每輪被拖長）**。

---

# P1-06：移除全機 PID 差集的 `taskkill`（外審第 3 輪）

## 問題
`_kill_systemftp(before_pids)` 算出 `目前 systemftp PID − 任務開始前快照`，對整個
差集執行 `taskkill /F`。**它完全繞過這幾輪建立的 ownership 驗證。**

「任務期間新出現」不等於「是我們的」——醫師在本次任務**執行中**手動開的住院系統
也不在 before 快照裡。而它會在任何可重試錯誤前執行，**包括寄信失敗**：

```
會診查完 → 醫師手動開 HIS → SMTP timeout → 醫師的 HIS 被 taskkill /F
```

函式自己的舊 docstring 也承認了這個窄窗（「使用者若『恰好在本任務進行中』才手動開
systemftp，會被納入」）。

## 修法
改成收掉**我們自己的 session**（`_session_close` → `_close_session_windows`：對確切
主畫面送 `WM_CLOSE` 並回讀確認），差集只留作證據 log。

`consult_query.py` 內的 `taskkill` 呼叫**歸零**。

### 為什麼不保留一份「僅供對照」的舊實作
我原本留了 `_kill_systemftp_DISABLED`，隨即刪掉 —— 那等於在程式碼裡留一把上膛的槍，
日後有人誤呼叫就回到原樣。git 歷史已經留著原版。

### 取捨（請檢視）
真的清不掉時，下一次 attempt 可能撞上院方「最多兩個」上限而**明確失敗**。
這是刻意的：**前者看得見、修得了；後者醫師只會看到自己的系統忽然消失。**
孤兒會不會累積，靠新增的證據 log 觀察。

## 改動既有測試
`test_kill_systemftp_only_new_orphans` 原本斷言「只殺任務期間新增的」。它守的性質是
「絕不碰任務前就存在的實例」——現在守得更強：**什麼都不殺**。已改寫並在 docstring
說明原本斷言什麼、為什麼那個判準不夠。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（各自看 exit code）
- pytest：**3596 passed, 2 skipped**；覆蓋率、skip 守衛通過
- 突變驗證 4 個全部轉紅：把 taskkill 加回來、不收自己的 session、差集不留證據、
  沒有快照就整個跳過。

> 我的第一版測試斷言「整個檔案不得出現 taskkill 字串」——結果被
> `_kill_systemftp` 自己的 docstring（「絕不再 taskkill /IM 全機掃殺」）餵飽而紅。
> ★本 session 第三次「掃原始碼的斷言被自己的散文餵飽」★。判準改成看實際的
> **呼叫引數**，不是看有沒有提到。

---

# ★自查 P0★ 我一小時前的修法會關掉醫師的住院系統

## 問題（v2026.08.04.27 引進，本檔上面那一節就是它）
`_close_session_windows()` 第一版用這個條件找主畫面：

```python
find_windows(MAIN_CLASS, pids=sess.our_pids)
```

`sess.our_pids` 就是 `(_systemftp_pids() - before) | {our_pid}` —— **全機 PID 差集**，
正是批次 P 認定不可信、因而不准拿來做終止授權的那個集合。實機 log 也證明它會混進
外來 PID：

```
[cleanup] pid 10928 已非 systemftp(PID 重用?),略過
[session] 收尾時排除 1 個不屬於本次啟動的 systemftp(pid=[4364])
```

只要醫師的 HIS 主畫面 PID 落進那個集合，收尾就會對它送 `WM_CLOSE`。
**我用「視窗層面」繞過了「行程層面」的 ownership 驗證，等於自己開了後門** ——
把批次 P 擋掉的傷害從另一扇門放回來。

## 修法：關「我們確切登入的那一個」
`_wait_main_window_after_login()` **本來就回傳**登入後的主畫面 hwnd，只是冷啟動那行
把回傳值丟掉了。接住它、存進 `_PersistentSession.main_hwnd`，收尾只關那一個。

身分依據從「PID 猜測」變成「**我們自己登入了它**」——不需要任何列舉。

### `main_hwnd is None` 的語意是【從未登入成功】，不是【不知道】
`_wait_main_window_after_login()` 只有 `return mains[0]` 一條成功出口，失敗一律拋
例外。所以成功建立的 session 必然帶著 hwnd；只有冷啟動失敗那條路徑上、為了收掉
行程而臨時建的 session 才是 None —— 那時本來就沒有我們的主畫面可關。
**這個不變式由 `test_the_session_records_the_window_it_logged_into` 守著**，
不是假設。

### 順帶：`_window_is_gone` 不可以只看 `IsWindow`
Delphi 表單關閉後常常只是 Hide、handle 仍有效（2026-07-27 診間事故的教訓）。
兩個都看。

## 自查 P1-A：控制項樹是 T0 的、roster 是 Tn 的
```python
children = enum_children(...)                 # T0
roster_texts, _ = _await_stable_roster(...)   # T0…Tn（內部重讀）
radios = _find_patient_radios(children)       # ← 又用 T0 的樹
```
清單在 T0→Tn 之間長出來時：信裡列 4 位、逐病人內文只抓 T0 的 1 位；若 T0 連一顆
radio 都還沒有，更會掉進舊式格線點選的後備路徑。

**這是我加穩定性判定（v2026.08.04.25）時引進的不一致**——改之前 roster 與 radios
同源（可能太早，但一致）。修法：`children` 移到清單穩定之後再取。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增（各自看 exit code）
- pytest：**3604 passed, 2 skipped**；覆蓋率、skip 守衛通過
- 突變驗證 **7 個全部轉紅**：★回到用 our_pids 找視窗（＝P0 原樣）★、冷啟動不接住
  hwnd、hwnd 沒傳進 session、送了不回讀、關不掉卻回報成功、只看 IsWindow、
  ★控制項樹不在穩定後重取★。

> 最後那個一開始沒轉紅 —— 我改了 P1-A 卻沒寫測試。補了「T0 只有 1 位、穩定後
> 4 位，radios 必須是 4 位」的行為測試才紅。

---

# v2026.08.05.2 — 止掛提醒對象變更：移除張廖年峰、加入沈冠宇（功能，非外審修正）

**使用者定案（2026-08-05）**
> 刪除 張廖年峰止掛提醒（設定頁面等等），但是保留整個止掛邏輯（仍保留原本陳駿升醫師）。
> 新增 沈冠宇醫師 止掛提醒：一早上／下午、三下午都先不預設止掛人數，但是三晚上預設 100 人。

## 改了什麼
| 位置 | 變更 |
|---|---|
| `threshold_policy.DEFAULT_THRESHOLDS` | 移除 4 個 `chang_*`；加入 `shen_wed_night: 100`（**只有這一個**） |
| `threshold_policy._DOCTOR_THRESHOLD_KEYS` | 移除「張廖年峰」；加入「沈冠宇」四個診次（一早／一午／三午／三晚） |
| `settings_defaults` | `alert_chang_enabled` → `alert_shen_enabled`，且**預設 True** |
| `main.py` | 開關變數／影子變數×2／存檔／載入／設定頁那一排／比門檻分支／遠期掃描，共 8 處 |
| `main.py` `save_all_settings` | 門檻存檔改走新的 `normalize_threshold_entry` |

「保留整個止掛邏輯」= `build_doctor_threshold_map` / `is_near_alert_threshold` /
遠期背景掃描 / notify_key 去重 / 寄信路徑全部沒動，只換了門檻表的內容與接線指向。

## 可疑點 1：「不預設」不可以變成 0（本批最危險的一處）
`save_all_settings` 原本是
```python
try: self.threshold_settings[key] = int(var.get())
except (ValueError, TypeError):
    self.threshold_settings[key] = DEFAULT_THRESHOLDS.get(key, 0)   # ← 沒有預設的鍵 → 0
```
沈冠宇那三格【刻意沒有預設】，使用者只要按一次「儲存」就會被寫成 **0**。
門檻 0 時 `is_near_alert_threshold` 是 `count >= 0 - margin(10)` → **恆真**，
「還沒設定」會變成「每一診都提醒」——方向完全相反。

修法：抽出 `normalize_threshold_entry(cfg_key, raw)`，語意宣告成
**空字串一律存 `""`**（`build_doctor_threshold_map` 的 `int("")` 會落到 except → 跳過該診次）。

**這一項的取捨（請特別看）**：清空一格現在等於**停用該診次**，
包含陳駿升那些有預設值的格子（舊行為是清空會被預設值填回去）。
選一致語意是因為「我明明清掉了它還在提醒」而且看不出原因，比較糟。

## 可疑點 2：`alert_shen_enabled` 預設 True（我自己決定的）
使用者要的是「新增提醒」，不是「新增一個要自己去勾的選項」；而且舊機器的
`threshold_settings.json` 裡只有 `alert_chang_enabled`，新鍵缺席 → 會走預設值。
若預設 False，這個功能上線後**完全不會動**直到使用者自己去設定頁勾。
影響面很小：四個診次裡只有三晚有門檻，其餘三個沒填數字前不會提醒。

## 可疑點 3：舊設定檔的殘留鍵
`alert_chang_enabled` / `chang_*` 會留在使用者的 json 裡當死鍵。沒有清理程式碼 ——
它們不再被任何地方讀取，`build_doctor_threshold_map("張廖年峰", {...})` 已回 `{}`
（有測試釘住「殘留值不得讓他復活」）。刻意不寫遷移：多一段會動設定檔的程式碼，
風險大於留幾個沒人讀的鍵。

## 刻意**不**動的張廖年峰
`app_settings.DEFAULT_DOCTOR_SETTINGS`（醫師名單／通知）與
`main.REFRESH_QUERY_BATCH_1`（查詢批次）——那是別的功能，不是止掛。

## 順帶修：日期守衛把今天當成未來
`test_review_markers_do_not_claim_a_future_date` 是 2026-08-01 寫的，把字面值
`"2026-08-05"` 當成「未來」。時間走到 8/5，當天的正常標記被自己的守衛擋下，
而真正寫到未來的日期（8/6、9/1…）反而照樣過。改成跟 `date.today()` 比。
（守衛判準要宣告式，不是某個當時剛好在未來的字面值。）

## 本機驗證
- ruff 0 / pyright 0 errors；型別債 147 不變（+1/−1 是同一處診斷的訊息字面值變了，逐行核對過才刷基線）
- pytest **3618 passed, 2 skipped**
- 突變驗證 **13 個全部轉紅**：留空回退成 0、留空回退成預設值、存檔那格改回 `int()`、
  三晚預設值被改掉、三晚沒有預設、一早也給了預設、張廖門檻復活、
  ★比門檻分支還指著舊醫師★、遠期掃描沒接上、設定頁那一排沒長出來、
  提醒預設關閉、影子變數沒跟著改、陳駿升被順手改掉
  ＋ 日期守衛塞一個 2027-01-01 也會紅

> 「留空回退成預設值」一開始**沒有**轉紅：沈冠宇那三格本來就沒預設，兩種寫法在
> 它們身上結果相同，要用陳駿升的鍵才分得開。補了那條測試才紅。
> 「比門檻分支還指著舊醫師」也是後補的 —— 只數開關讀取次數的測試抓不到名字寫錯。

---

# v2026.08.05.3 — 批次S：session 確切身分（外審第 4 輪 P1-01/02/03/04/05）

## 實機證據（2026-08-05 診間 log，11 小時）
先講**好消息**：常駐登入是真的常駐了 —— 99 次續留 / 3 次冷啟動 / **0 次判死 / 0 個 ERROR**，
三次冷啟動全是設計內的（6 小時定期重啟、休息時段收掉、程式結束更新）。

**但這份 log 也把 P1-05 從理論變成事實**：
```
23:40:17 登入視窗 hwnd=396344 pid=[8036, 16276]      ← 登入當下就含外來 pid
06:02:49 登入視窗 hwnd=7673614 pid=[17772, 18748]
10:00:36 登入視窗 hwnd=7673614 pid=[18748, 20940]
→ 三次收尾三次都要「排除 1 個不屬於本次啟動的 systemftp」(15056 / 8036 / 18748)
```
`our_pids` 被污染是**常態，不是邊界情況**（3/3）。而判活寫的是
`find_windows(MAIN_CLASS, pids=sess.our_pids)` —— 醫師自己那個住院系統的主畫面還開著，
就會回答「我們的 session 還活著」。

> ★我一開始把這份 log 讀錯了★ 我看到三次 teardown 都沒有「主畫面已關閉」也沒有
> 「關不掉」，就說新的收尾在實機上靜默 no-op。**錯的**：log 裡只有一個啟動橫幅
> （09:57:33 v2026.08.05.1），在那之前跑的全是舊版。新的 `main_hwnd` 收尾在實機上
> **還沒被執行過任何一次**。這份 log 對它一句話都沒說。

## 修法

### P1-01 / P1-02：一個述詞回答兩個相反的問題
```python
_window_is_gone(hwnd)   # 「不見了嗎」——隱藏算、例外也算
```
判活問「還能用嗎」→ 不知道就當不能用（重登，代價：多一次登入）。
收尾問「關掉了嗎」→ 不知道就當**沒關掉**（告警，代價：多一次人工確認）。
同一支述詞必然有一邊在錯的方向 —— 收尾這邊把「隱藏」和「API 例外」都算成
「已經關掉了」，於是 HIS 還登入著、程式回報成功、還把參照丟掉。

拆成 `_window_alive`（只看 IsWindow，例外→False）與 `_window_destroyed`
（只看 not IsWindow，例外→**False**）。**兩者刻意不是互為反面**：對「不知道」
兩個都回 False，有一支測試專門釘這件事。

`_window_alive` 也**不看可見性**：`_stealth()` 的 SW_HIDE 後備模式是我們自己把視窗
藏起來的，把它讀成「死了」＝每輪重登＝每 3 分鐘重送帳密（8/4 才修掉的實機故障）。

> ★這個檔案的舊測試把 P1-01/P1-02 釘成了通過條件★
> `test_a_hidden_but_alive_window_is_also_gone` / `test_an_api_failure_counts_as_gone`
> —— 名字就寫著缺陷的內容，綠燈綠了一整天。

### P1-04：hwnd 值會被回收
登入當下記下 `(main_pid, main_class)`；送 WM_CLOSE 前比對。對不上就**不關**並回報失敗
（那極可能就是醫師自己開的住院系統 —— 同一支程式、同一個 class）。
身分不明（舊 session／查不到）一律當「不是」：不知道不可以當成是。

### P1-05：判活改問我們登入的那一個
`sess.main_hwnd` + 身分指紋，不再問 `our_pids`。死因也從 3 種細分成 5 種
（從未登入成功／hwnd 被回收／行程與視窗都不在／查不到行程狀態／視窗不在）。
`_session_is_alive` 不再需要「呼叫緒必須在隱藏桌面上」——
IsWindow / GetWindowThreadProcessId / GetClassName 都不受呼叫緒桌面影響。
（舊 docstring 的那句限制已一併更新，不留下對不上的宣稱。）

### P1-03：關不掉不可以連參照一起丟
`_session_close` 先 `_psession = None` 再去關。關失敗時唯一的參照已經沒了，而
底下每一條後路對啟動器型的 systemftp 都無效（hproc 早已 signaled、
`_verified_owned_pids` 只剩死 root、`_kill_systemftp` 已不再 taskkill）。
→ 新增 `_unclosed_sessions` 掛帳，`_acquire_session` 每輪重試，並告警。
**上限只防爆、不丟棄**：丟掉任何一筆就等於回到「沒人認得它」。

## 沒有做的（連同理由）
* **P1-06（modal 擋住收尾）**：Delphi 被 modal 擋住時 WM_CLOSE 會不會被吞，我沒有
  實機證據，不憑猜測改。P1-03 落地之後這一項不需要猜 —— 關不掉就掛帳＋重試＋告警。
* **P1-07/08/09（清單就緒、原子快照、截圖時機）**：屬於另一組（roster），下一批。
* **P1-10（寄信失敗殺掉健康的 session）**：已定位在重試分支的 `_kill_systemftp`，
  獨立且小，下一批。
* **P1-11**：需要外審原文再確認一次才分類，不先猜。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3638 passed, 2 skipped**
- 突變驗證 **12 個全部轉紅**：收尾把隱藏當關掉、例外當關掉、兩述詞寫成互為反面、
  判活又看可見性、判活退回問 our_pids、送 WM_CLOSE 前不驗身分、身分不明當成「是」、
  session 沒記下身分、關不掉不掛帳、掛了帳沒人重試、重試失敗就丟掉、超過上限擠掉舊的

---

# v2026.08.05.4 — P1-10：寄信失敗不可以收掉健康的 HIS 登入

## 問題
`_kill_systemftp` 現在做的事是 `_session_close(...)` —— 收掉常駐登入（taskkill 那一半
已在 8/4 移除）。但它掛在**所有**可重試錯誤上，包括組信／截圖落地／SMTP／Outlook：

```
會診查完（HIS 一切正常）→ SMTP timeout → 收掉登入
→ 下一次 attempt 冷啟動 → ★再送一次帳密★
```

帳密重送正是這幾批一路在防的事（8/4 實機：每 3 分鐘送一次），而這裡的失敗
**根本不在 HIS 那一側**。函式自己的註解早就寫著這個劇本（「會診查完 → 醫師手動開
HIS → SMTP timeout → 醫師的 HIS 被強殺」），只是當時只修了 taskkill 那一半。

## 修法
每個 attempt 開頭 `his_stage_done = False`，`run_consult_flow(...)` 回來的下一行設 True。
重試分支：做完了 → 保留登入、只重試寄信；沒做完 → 照舊重置（HIS 卡死時**必須**重置，
否則下一次 attempt 撞上同一個壞掉的實例）。

## 測試踩到的坑（記下來，這個形狀又出現了）
第一版的接線測試寫成「`_kill_systemftp` 必須在 else 那一側」。
突變驗證把守衛改成 `if not his_stage_done: 保留 else: 殺` —— **方向整個相反** ——
測試照樣全綠（kill 確實在 else 側）。比對形狀擋不住這種改法。
改成把守衛條件在給定旗標值下**實際求值一次**（`_kill_runs_when(True/False)`），
`if X` 與 `if not X`、以及兩個分支對調，就都騙不過去了。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3643 passed, 2 skipped**
- 突變驗證 **6 個全部轉紅**：守衛拿掉、方向反轉(`if not X`)、兩個分支對調、
  HIS 失敗也不重置、旗標沒有每個 attempt 重設、旗標立在 flow 之前(永遠 True)

---

# v2026.08.05.5 — 批次T：清單就緒（外審第 4 輪 P1-07 / P1-08 / P1-09）

三項是同一件事的三個切面：**「病人清單」這份資料在哪一個時刻被定下來**。

## P1-07：有內容的清單也需要最短觀察窗
上一版的規則是「連續三次讀到相同（0.5 秒）就算穩定」，只有**空清單**額外要求觀察
3 秒。理由寫在註解裡：「有內容就不必等，資料都出現了，不可能是【還沒載入】」。

★那句話只對【完全沒載入】成立，對【載到一半】完全不成立★
Delphi 是逐列填的。4 位病人填出 2 位、然後停頓超過 0.5 秒（慢機器／後端慢很常見），
就會拿 2 位那份當成最終清單存進基準 → **另外 2 位此後永遠不算「新」→ 漏寄**。
而「漏寄」正是這個檔案第 1243 行自己列出的失敗模式。

→ 有內容也要 `_ROSTER_MIN_OBSERVE = 1.5` 秒。

**這 1.5 秒不是白等**：它取代了截圖前那個固定的 `time.sleep(1.8)`（見 P1-09），
整輪的實際耗時反而**略短**。

> ★這個檔案的舊測試把 P1-07 釘成了通過條件★
> `test_a_nonempty_roster_does_not_wait_the_extra_window`，理由寫著
> 「有內容就不必等」。連理由都一起釘進去了。

## P1-08：清單文字與 radio 控制項來自兩次列舉
穩定判定讀一次、擷取前再讀一次。中間長出一位病人 → 信裡列 N 位、逐病人內文卻是
N±1 位，基準也只存到其中一份。

★修法是「結構上不可能」而不是「事後再比對一次」★
`_read_roster_snapshot()` 一次列舉同時產出 children / radios / texts，
三者必然同一時刻。`_await_stable_roster` 回傳的 `_RosterSnapshot` 帶著它們，
`_extract_consult_text` 直接用、不再自己列舉。

`_RosterSnapshot` 刻意是**四元組**：舊寫法 `texts, stable = ...` 會當場 ValueError，
不會靜默地只拿到一半而把 children/radios 留在別的時間點。

## P1-09：截圖排在固定 1.8 秒之後、清單穩定之前
信裡的清單是 Tn 的、附圖是 T0+1.8s 的 —— 醫師收到兩份互相矛盾的證據。

★截圖不能挪到擷取之後★：擷取會逐位點選病人 radio，點完畫面顯示的是最後一位的
內容，不再是「打開時的原始清單畫面」。所以順序必須是
**先等穩定 → 再截圖 → 再逐位擷取**，`_capture_with_settled_roster()` 就是這個順序。

截圖後再回讀一次：期間又變了 → 這份快照不代表圖上那一刻 → 標成 unstable，
走既有 fail-open 通道。**回讀失敗 ≠ 清單變了**（讀不到就不因此否定快照，
否則列舉一失敗就變成每輪 fail-open 寄信）。

兩條路徑（常駐 session 的 `_query_cycle`、建不出隱藏桌面時的 `_run_with_sw_hide`）
都改了，有一支測試專門確認**兩條**都改（只修一條的話另一條照樣拍到半份清單）。

## 測試踩到的坑（同一個形狀第四次）
接線測試第一版寫 `assert "time.sleep(1.8)" not in src` —— 而我在**同一段程式碼的
註解裡**解釋了「固定的 time.sleep(1.8) 已被取代」。測試被自己的說明文字餵飽，當場
誤紅。改成用 AST 找真正的 `sleep(1.8)` **呼叫**。
（同一支測試也抓到我把第二條路徑的函式名寫錯 —— 它堅持要檢查到 2 條。）

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3652 passed, 2 skipped**
- 突變驗證 **11 個全部轉紅**：有內容跳過觀察窗、觀察窗縮到比三次讀取還短、
  觀察窗套錯對象（空↔非空對調）、快照內部列舉兩次、擷取自己重新列舉、
  快照退化成二元組、先截圖再等穩定、截圖後不回讀、回讀失敗就否定快照、
  `_query_cycle` 退回原樣、`_run_with_sw_hide` 退回原樣

---

# v2026.08.05.6 — 批次U＋V：操作面確切身分 ＋ UNMANAGED 閘門（外審第 5 輪）

## 批次U / P1-01 —— ★本輪最危險的一項，而且是我上一批親手漏掉的★
批次S 把【判活】與【收尾】改成用 `sess.main_hwnd`，卻把**真正下命令**那一段留在原地：
```python
mains = find_windows(MAIN_CLASS, pids=sess.our_pids)
main_hwnd = mains[0]                       # ← 列舉順序決定的「第一個」
PostMessage(main_hwnd, WM_COMMAND, cmd_id)
```
而同一批 commit 自己引用的實機證據就是「`our_pids` 每一次登入都混著醫師的 systemftp」。
淨結果：**用確切身分確認自己的 session 健康，下一行卻從污染的集合挑出醫師正在用的
HIS 並對它送出「我的會診清單」命令** —— 醫師畫面會自己跳出會診單，我們還會把他螢幕上
的病人清單擷取下來寄給整組人。

修法：
* 送命令前先 `_session_death_reason(sess)`，不通過就停手（不退回列舉找一個來用）。
* `main_hwnd = sess.main_hwnd`，候選集不再參與決定。
* 會診視窗綁定本次命令：記下命令送出【前】既有的會診視窗，之後只認
  **新出現**且屬於 `sess.main_pid`（我們登入的那個行程）的那一個。
* `_return_to_main` 只觀察 `consult_hwnd` 這一個 handle
  （舊判準「污染集合裡看不到任何會診視窗」兩個方向都會錯：醫師自己開著一張，
  我們這張明明關掉了卻永遠等不到「都沒有」→ 誤判退場失敗 → 收掉健康的 session、
  下一輪重送帳密）。
* P2-02：會診單收掉後還要確認主畫面**真的回到可操作**（`IsWindowEnabled`），
  否則不可以宣稱「session 續留」。

★`_consult_form_dismissed` 看可見性是【對的】★，與 `_window_destroyed` 相反：
Delphi modal 關閉後只是 Hide，所以對「這張表單還擋在畫面上嗎」來說看不見就是答案；
而對「HIS session 關掉了嗎」看不見【不能】當成關掉。同一個 API、兩個不同問題。

## 批次V / P1-02 —— 掛帳只記帳，沒有真的擋住
上一批只呼叫 `_retry_unclosed_sessions()` 而**把回傳值丟掉**。於是掛帳機制只做到
「我知道有一個關不掉的 session」，做不到它自己註解宣稱的「不先收就會同時有兩個
登入中的 HIS」。

→ `_ensure_no_unmanaged_sessions()`：帳上有殘留就 raise `UnmanagedSessionError`，
本輪不建立新登入；列為 fatal（不進 backoff 重試）。

**為什麼是 fail-closed（停掉查詢）**：照常再登 → 登入中的 session 逐次累積、沒有人
知道，而 systemftp 本來就有「最多兩個」上限 → 最後照樣全面失敗，只是無聲；
停掉查詢 → 會寄信告警、每輪重試關閉、關掉後自動恢復。
後者是看得見、會自己好的失敗；前者是看不見、只會更壞的失敗。

## P1-03 —— 上限會丟掉「新來的那一筆」
註解寫「上限只是防爆，不丟掉任何一筆」，程式碼丟掉的卻是**第 9 筆**；
測試也只驗了「舊的 8 筆沒被擠掉」，缺陷剛好落在兩者之間。
→ 移除上限（有了 P1-02 的閘門，這個清單不可能因為我們自己再開 session 而增長）。

## P2-04 —— 告警只有 log
註解一路寫「掛帳＋告警」，實際上只有 `logging.error` —— 使用者看不到隱藏桌面、
也不會去翻 log。→ 走開發者告警信，6 小時節流。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3674 passed, 2 skipped**
- 突變驗證 **13 個全部轉紅**：主畫面退回 `mains[0]`、送命令前不驗身分、
  會診視窗退回 `hits[0]`、忽略 generation、會診視窗用寬鬆 PID 集合、
  `_return_to_main` 退回全域列舉、不檢查主畫面可操作、
  「查不到」當成已退場、閘門忽略殘留數、閘門沒被呼叫、
  重試出錯就放行、上限重新丟棄新項目、告警降級成只寫 log

---

# v2026.08.05.7 — 批次Z：止掛設定安全性（外審第 5 輪 P2-09 / P2-10 / P2-11）

三件事屬於同一類：**程式替使用者做了他沒有要求、也看不見的決定**。

## P2-09 —— 只堵了一半
上一批防的是「空字串被存成 0」，卻放行使用者**直接輸入**的 `0` / `-1`。
`is_near_alert_threshold` 是 `count >= 門檻 - margin(10)`，門檻 0 或負數 → 恆真。
註解自己已經寫明「0 會讓提醒恆真」，那就不該只擋其中一條進入路徑。

→ `MIN_THRESHOLD = 20` / `MAX_THRESHOLD = 400`。
**下限不是 1 而是 20**：門檻 ≤ margin(10) 時第一位病人就「接近門檻」——
「還沒滿」與「快滿了」再也分不開。上限純粹是打字防呆（多按一個 0）。

## P2-10 —— 打錯字會靜默改掉設定
舊行為：有原廠值的鍵 → 悄悄變回原廠值；沒有的 → 悄悄變成空（＝關掉那個診次）。
```
原本自訂 88 → 手滑打成 8O → 存檔後變成 59，使用者不會知道
原本自訂 100 → 打錯 → 這個診次的提醒被關掉，使用者不會知道
```
→ `validate_threshold_entry()` 回 `(值, 錯誤訊息)`；有錯就**拒絕存檔**、保留原值、
在對話框裡逐格說明是哪一格錯在哪裡。

★驗證排在【寫任何檔案之前】★：第一版把它放在 `r_doctor_settings.json` 寫入之後，
「拒絕存檔」其實會變成「存了一半」。有一支測試釘住這個順序。

## P2-11 —— ★推翻我自己前一批的決定★
我把 `alert_shen_enabled` 原廠預設設成 `True`，理由是「使用者要的是新增提醒，
不是新增一個要自己去勾的選項」。**那是錯的** —— `main.py` 同一段既有註解已經定案：

> 「多台電腦同時跑時，若有人達到止掛門檻會重複寄信 → 預設關。」

換醫師之後，全院每一台診間機的設定檔都沒有這個新鍵 → 全部走原廠預設 → 全部開始寄。
我在寫下那個 True 的時候，那句註解就在上面三行。

但單純改成預設關也不對：原本負責寄信的那台也會靜悄悄不寄，功能等於沒上線。

→ 真正的資訊在舊鍵裡：**這台原本有沒有在做止掛提醒**。
`load_threshold_settings` 在「檔案裡沒有新鍵」時由
`alert_chang_enabled or alert_chen_enabled` 推導（與既有那幾條舊格式推導同樣的
`not in data` 語意 —— 使用者一旦自己勾過就永遠以他的選擇為準）。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3691 passed, 2 skipped**
- 突變驗證 **13 個全部轉紅**：0/負數放行、下限低於 margin、打錯字靜默回退、
  留空被當成錯誤、存檔完全不驗、★驗了卻不中止★、驗證排到寫檔之後、
  拒絕存檔不 return、拒絕存檔不告訴使用者、繼承被拿掉、繼承蓋掉使用者的明確選擇、
  原廠預設改回開

> 「驗了卻不中止」(`if bad_thresholds:` → `if False:`) 第一輪**沒有**轉紅：
> 錯誤照收集、然後什麼都沒發生（那一格被靜默跳過）。補了「拒絕分支必須 return
> 而且必須告訴使用者」才紅。

---

# v2026.08.05.8 — 批次W：查詢與寄送分開（外審第 5 輪 P1-04 / P2-05）

## 上一批只修了一半
批次 P1-10 讓「寄信失敗不再收掉 HIS session」，log 也寫成「只重試寄信」。
但 retry loop 的下一次 attempt 仍然從頭執行 `run_consult_flow(...)`：
再開一次會診畫面、再擷取一次 roster、再逐位點選病人、再落地一張截圖、
再組一封信 —— 而第二次查到的清單**可能已經不一樣**。

那句 log 與行為不符：它不是「只重試寄信」，只是「重新查詢時不先登出」。

而且 SMTP 有一種很常見的失敗形狀：**伺服器已經收下，只是回應逾時**。
第二次 attempt 用新的 Message-ID 再寄一次 → 收件人收到兩封內容還不一樣的信。

## 修法
```
his_result = None      # 迴圈【外】
delivery   = None
for attempt in ...:
    if his_result is None:            # HIS 只查一次
        his_result = run_consult_flow(...)
    if delivery is None:              # payload 只組一次(含截圖落地)
        delivery = _DeliveryArtifact(..., message_id=_new_message_id())
    send(delivery)                    # 重試 = 重送同一封
```
* `_DeliveryArtifact` 是 `frozen=True` 的 dataclass —— 「組好之後不再變」由型別
  系統保證，不是靠紀律。
* Message-ID 由呼叫端帶入（`send_mail` / `_build_message` 新增選用參數），
  重試沿用同一個；沒指定時仍照舊自己產一個（少了它會被當垃圾信）。
* 截圖只落地一次：舊寫法每個 attempt 都 `_materialize_shot`，三次 attempt
  就是三張病人畫面躺在磁碟上。

## P2-05：沒寄出去的截圖要刪掉
`_materialize_shot` 的既有取捨是「已經寄給臨床收件人的圖留著當線索」——
**那個理由只對【寄成功】成立**。整輪放棄時那張圖沒有人看過、也沒有臨床用途，
留著純粹是多出來的 PHI 暴露面。→ 放棄那一條路徑呼叫 `_discard_undelivered_shot`。
★寄成功的圖仍然留著★（出事時「當時畫面長怎樣」是最有用的線索），有一支測試
釘住「成功路徑上不可以刪」。

## 連帶修好的測試（都是同一個病灶：測試綁在字面寫法上）
* `test_consult_decision_logic` 的 `send_via_smtp` stub 少了 `message_id` 參數
  → 24 個測試一次全紅。**測試要用生產的呼叫形狀**。
* `test_task_gate_supersede` 用 `src.index("send_via_smtp(shot, subject")` 找位置
  → 參數一改就 ValueError。它要守的不變量是【順序】，與參數怎麼寫無關 → 改用 AST。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3710 passed, 2 skipped**
- 突變驗證 **11 個全部轉紅**：每個 attempt 重查 HIS、守衛方向反轉、
  `his_result` 在迴圈內重設、payload 每輪重組、寄送用重算的區域變數、
  Message-ID 沒傳到 send_mail、builder 忽略指定的 Message-ID、artifact 不 frozen、
  未送達截圖保留、刪除變成 no-op、成功路徑也刪圖

---

# v2026.08.05.9 — 批次X：快照三態 ＋ 觀察窗從最後變動起算（外審第 5 輪 P1-06 / P1-07）

## P1-06 —— 「可以寄」與「可以更新基準」是兩個問題
以前共用 `roster_texts is None` 一個通道，中間缺了一格：

| 狀態 | 可寄信 | 可更新基準 |
|---|---|---|
| 確認過沒變 | 是 | 是 |
| 確定變過了 | 是（走既有 fail-open 註記） | 否 |
| **無法確認** | 是 | **否** ← 這一格以前被當成第一格 |

★這是我上一批親手寫下的洞★：截圖後回讀若拋例外，上一版把 `after` 冒充成
`snap.texts`，於是這份**從未被確認過**的快照照樣去更新「已通知」基準。
期間真的多出一位病人的話，他會被記成「已經通知過」而從此不再算新的 ——
漏寄，而且沒有任何跡象。**而我當時還寫了一支測試把這個行為釘成通過條件**
（`test_a_readback_failure_does_not_invalidate_the_snapshot`）。

修法：`_RosterTexts(list)` 帶 `baseline_eligible` 旗標、`_RosterSnapshot.as_unverified()`、
存基準那一處改問 `_may_update_baseline()`。
用 list 子類別是刻意的：所有既有消費端（迭代、`len`、`is None` 判斷、簽章計算）
完全不受影響，只有真正要決定「能不能更新基準」的那一處會看旗標。

**代價說清楚**：不更新基準 = 下一輪這些會診仍算「新的」→ 會再寄一次。
選這個方向是因為「重複寄」是看得見、有界的；「漏寄」是看不見的。
一旦某一輪回讀成功，基準就會前進，重複自然停止。

## P1-07 —— 觀察窗從【最後一次變動】起算
從「進函式」起算的話：清單在第 1.4 秒才長出最後一位時，第 1.5 秒就已經滿足
「總共觀察了 1.5 秒」—— 實際上只安靜了 0.1 秒。

★誠實說明它不能做到什麼★：清單若在觀察窗**之後**才繼續載入（外審舉的例子：
2.1 秒才出現第 3、4 位），任何以時間為判準的做法都會失手 —— 那需要 HIS 自己的
「查詢完成」訊號（loading 指示、列數標籤、dataset 狀態），目前沒有找到。
**這是一道防線，不是「roster readiness 已解決」。**

> 我第一版的測試寫成「2.1 秒才長出來也要接住」—— 那是在宣稱程式有它沒有的性質，
> 而且當場就紅了。改成量真正變了的東西：**變動之後還觀察了幾次**。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3721 passed, 2 skipped**
- 突變驗證 **7 個全部轉紅**：回讀失敗假裝沒變、unverified 仍可更新基準、
  存檔處忽略資格檢查、資格函式永遠回 yes、unverified 被降級成 unstable（會變成
  每輪 fail-open 寄信）、觀察窗改回從進函式起算、變動不重新計時

---

# v2026.08.05.10 — 批次Y：借用醫師的 HIS ＋ 文字去重（外審第 5 輪 P1-08 / P1-09）

## P1-08 —— ★兩條路徑不一樣，只有一條要 fail-closed★
外審建議「移除 borrowed mode」。我只對**其中一條**這樣做，理由如下。

**SW_HIDE 後備模式（`_run_with_sw_hide`）→ 改成 fail-closed**
它跑在【使用者自己的桌面】上，`pick = fresh or cands` 撿到的那個既有登入視窗
極可能就是醫師剛打開、正要自己登入的住院系統。接下來我們會：
```
把自動化帳密打進去 → 把他的視窗移到螢幕外 → 開會診單
→ 擷取全院病人資料 → 最後試著還原
```
「收尾不關掉它」只把傷害縮小到「不關窗」，前面那一串照做不誤。
→ 只接受本次啟動後才出現的登入視窗；沒有就放棄本輪並說明原因。
第二道門：若選中的 pid 竟然落在 before（pid 被回收）→ 一樣 raise，不是印 warning。

**隱藏桌面（`_cold_start_session_impl`）→ 維持可重用**
`find_windows` 只列舉【呼叫緒所在桌面】，而那條路徑已經 SetThreadDesktop 到我們
自己建立的隱藏桌面。醫師的住院系統在互動桌面，**不可能**出現在那裡 —— 撿到的
必然是我們前幾輪留下的孤兒，重用它反而是對的。一起改成 fail-closed 只會讓
「有孤兒佔位時永遠查不到」。有一支測試釘住這條**不可以**被一起改掉。

**行為變化（使用者會感受到）**：建不出隱藏桌面的機器上，若畫面上已有一個
不屬於本次啟動的登入視窗，該輪查詢會失敗（既有告警機制會通知）。
這是刻意的：寧可本輪查不到，也不要動醫師的住院系統。

## P1-09 —— 去重的依據是【控制項】不是【文字】
```python
if ... or t in seen:   # ← 兩個不同的 radio，文字相同就丟掉第二個
```
顯示文字相同不代表是同一張會診單：同一位病人、同一分鐘由不同科別開的兩張會診，
清單上就是兩列一模一樣的字。在任何識別邏輯看到它之前就丟掉第二列 = ★漏寄★，
而且丟在最上游，下游怎麼修都救不回來。
去重原本要擋的是「同一個控制項被列舉兩次」→ 用 `hwnd` 判斷才對，而且更精準。

> ★測試又一次把缺陷釘成通過條件★：舊 fixture 用兩個不同 hwnd、相同文字的
> radio，斷言只留一個，註解還寫著「同文字 → 去重」。

## 沒有做的（連同理由）
**多重集合／occurrence baseline**：外審建議把 row position/occurrence 放進識別。
我不做，因為它會把一個**罕見**的漏寄換成一個**常見**的誤寄 ——
occurrence 是位置相依的，任何一張會診被回覆而離開清單，後面每一筆的序號都會位移
→ 全部被當成「新的」→ 對團隊重寄整份清單。
現況：兩張同病歷號、同分鐘的會診仍會在 `set` 裡收斂成一筆。真正的解法是 HIS 自己的
會診單號，我在控制項樹裡沒有找到。**這一項維持已知缺口，不用更糟的東西去補。**

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3725 passed, 2 skipped**
- 突變驗證 **6 個全部轉紅**：後備模式重新借用、借用只寫 log 不擋、
  隱藏桌面被一起改成 fail-closed、去重改回文字、完全不去重

---

# v2026.08.05.11 — ★實機事故修復★ modal 擋住主畫面 → 會診查詢全停

## 事故
2026-08-05 14:52 起，診間電腦的會診查詢**完全停擺**，使用者重開機也一樣，
只收到我的告警信。這是**我當天三個改動疊在一起**造成的，每一個單獨看都有道理。

```
14:52:28,451 [consult-extract] 擷取完成:清單 0 位
14:52:28,762 收掉 systemftp(現任):會診單已收掉,但主畫面沒回到可操作狀態:
             主畫面被 modal 對話框擋住(disabled)
14:52:31,765 ★主畫面關不掉★(hwnd=207298)
14:52:31,765 ★關不掉,先掛帳待重試★(帳上共 1 個)
之後每一輪   仍有 1 個無法確認關閉的住院系統登入 → 本輪不建立新登入
```

### 三個環節
1. **`_main_ready_for_next_cycle`（P2-02）只取樣一次**
   Delphi 的 modal form 是【先 Hide、後把 owner 重新 enable】，而
   `_consult_form_dismissed` 把「看不見」就算退場 —— 我剛好卡在那個縫裡問。
   log 上兩行相隔 **311 毫秒**。
2. **判定不可操作就收掉 session**
   但 ★disabled 的視窗不會處理 WM_CLOSE★ —— 在這個狀態下收尾【必然失敗】。
   「收尾關不掉」與「被 modal 擋住」根本是同一件事的兩個症狀，我卻把前者
   當成獨立故障。
3. **掛帳閘門（P1-02）無限期 fail-closed**
   我寫它的時候給的理由是「停掉查詢會寄信告警、關掉後自動恢復」——
   ★那個理由預設了『遲早關得掉』，而這裡永遠關不掉★。

## 修法
* `_main_ready_for_next_cycle` 改成**輪詢**等主畫面重新 enable（上限 6 秒），
  不再取樣一次就定生死。
* 不可操作時**先按掉 modal**（`_dismiss_blocking_modals`，只找 `sess.main_pid`
  的通知視窗，不動醫師的），恢復就留著 session；按了還是不行才收掉。
* `_close_session_windows` 送 WM_CLOSE **之前先按掉 modal** —— 否則那個視窗
  是 disabled 的，訊息根本不會被處理。
* **閘門加上限**（15 分鐘）：超過就放行並持續告警。
  臨床上「查詢完全停擺」比「隱藏桌面多一個登入中的 session」嚴重得多，
  而且後者本來就有 systemftp「最多兩個」的自然上限會讓它自己浮現。

## 教訓（寫給下一次的我）
* **fail-closed 之前先問「這個狀況一定會解除嗎」**。我當時只論證了「停掉查詢
  比累積 session 好」，沒論證前提。前提不成立時，fail-closed 就是無聲的全停。
* **把觀察升級成破壞性動作要很小心**。P2-02 本來只是「多說一句話」的診斷，
  我讓它去收 session，於是一個診斷變成了故障源。
* **一次上九個版本、沒有任何一版在實機跑過**，是這次三個改動能疊在一起的原因。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3735 passed, 2 skipped**
- 突變驗證 **7 個全部轉紅**：readiness 改回取樣一次、退場不先按 modal、
  關窗不先按 modal、按 modal 用寬鬆 PID 集合、閘門改回無限期、
  閘門上限改成荒謬值、閘門計時不歸零

---

# v2026.08.05.12 — ★事故第二層★ 真正擋路的是 `TFMTimeOut_1`，程式從來不認得它

## v.11 修好了什麼、沒修好什麼
v.11 之後掛帳/閘門那一段乾淨了（`關不掉` / `掛帳` / `無法確認關閉` 全部 0 次）。
但機器仍然查不到會診 —— 換成登入失敗。而診斷傾印把答案直接寫出來了：

```
TFrmLogin(vis=1,en=0)      ← 我們的登入視窗，被擋住
TFMTimeOut_1(vis=1,en=1)   ← ★唯一 enabled 的，它才是擋路的那個★
TFMShowMessage(vis=1,en=0) ← 通知視窗，它自己也被擋住
TFMNewMain(vis=1,en=0)     ← 主畫面，被擋住
```

程式對著【disabled 的】`TFMShowMessage` 按了 6 次「確認」——
**disabled 的視窗根本不會處理點擊** —— 整整 120 秒的登入預算就這樣燒光，
最後回報「登入沒有完成」並進入 15 分鐘冷卻。
`TFMTimeOut_1` 這個 class 程式從來沒聽過，所以從頭到尾沒碰它。

這也回頭解釋了 14:52 那次：v.11 的 `_dismiss_blocking_modals` 只找 `NOTICE_CLASS`，
**它其實也救不了那次事故**（只是閘門上限把傷害擋住了）。

## 修法：判準改用 Win32 的正規訊號，不再猜 class
modal 會把其他視窗 disable，而它自己是 enabled 的。所以：

    可見 + enabled + 不是內容視窗（主畫面/登入/會診單）＝ 它就是擋路的那個

不管它叫什麼名字。`_blocking_dialogs()` / `_dismiss_blocking_modals()` 改成這樣，
並接到三個地方：等主畫面的迴圈、退回主畫面、關閉 session 之前。

★絕不盲按★：只按 `確認/確定/OK/是/繼續` 字樣的按鈕；不認得的對話框只把
**class 與所有按鈕字樣**記進 log（讓下一次知道它長什麼樣），不出手。
在醫院系統上亂點按鈕的代價，比多停一輪查詢大得多。

順帶：通知視窗自己被擋住（disabled）時就不要再按它了 —— 那 6 次點擊是純粹的浪費。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3743 passed, 2 skipped**
- 突變驗證 **8 個全部轉紅**：判準改回比對 class（會漏掉 TFMTimeOut_1）、
  把 disabled 的對話框也當成擋路的（就是燒掉 120 秒那個誤判）、
  把內容視窗當成對話框（會去點主畫面）、盲按不認得的按鈕、
  登入迴圈不先清擋路的、確認了可不可操作卻沒據此跳過（兩種寫法）

> 兩支測試在這一輪被字面比對誤傷/放行：
> `"not notice" not in body` 被新的 `not notice_actionable` 誤中（改用字界比對）；
> 「有沒有呼叫 IsWindowEnabled」擋不住「呼叫了但不用結果」（改用 AST 求值守衛條件）。

---

# v2026.08.05.13 — 事故收尾：`TApplication` 誤判成對話框（log 雜訊）

## 實機確認 v.12 有效（2026-08-05 18:01 起）
```
18:01:54 [session] 常駐 session 已建立(pid=17288)
18:02:04 [session] 已按掉擋路的對話框 class=TFMShowMessage(按鈕=確認)
18:02:04 [session] 已退回主畫面,session 續留    ← 之後每輪都是這一行(共 7 次)
```
`★主畫面關不掉★` / `掛帳` / `無法確認關閉` / `登入沒有完成` / `登入冷卻中`
**全部 0 次**。服務恢復。

## 這一版修的是診斷雜訊
v.12 的 log 出現兩行：
```
[session] 有擋路的對話框但沒有可按的按鈕 —— class=TApplication 按鈕=[]
```
`TApplication` 是 **Delphi 的隱形擁有者視窗**：每個 Delphi 程式都有一個，
有 WS_VISIBLE、永遠 enabled、而且沒有任何按鈕 —— 它完全符合我的
「可見 + enabled + 不是內容視窗」判準，但它根本不是對話框。

實害只有雜訊（沒有按鈕，本來就不會被按），但 **log 是我們唯一的實機診斷管道**，
而那個迴圈每 0.4 秒跑一次 —— 2026-07-29 那次 1,568 行幾乎全是同一句的教訓還在。

→ `TApplication` 併入 `_CONTENT_CLASSES`；「不認得的對話框」警告改成
**同一個 class 只講一次**。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3746 passed, 2 skipped**
- 突變驗證 **3 個全部轉紅**：TApplication 又被當成對話框、警告不再節流、
  連真的對話框也一起排除掉

---

# v2026.08.05.14 — 批次AA：PID 回收（P1-05 / P2-06）＋ 基準遺失告警節流（自查 P1-B）

## P1-05 與 P2-06 是同一件事的兩面
Windows 會把結束行程的 PID 發給新的行程。只比對 PID —— 甚至「PID + 執行檔名稱」
—— 都擋不住「同一個 PID 換了一個行程」，而在這台機器上那個新行程很可能就是
**醫師自己開的同一支住院系統**（實機三次登入三次都有外來的 systemftp）。

* **P1-05**：session 的主畫面身分（hwnd + pid + class）全對得上，底下的行程仍可能換人
  → `_PersistentSession` 多記一個 `main_proc_started`（行程建立時間）。
* **P2-06**：`_validated_systemftp_pids` 驗完之後，`WM_CLOSE` 與 `terminate` 都是
  【之後】才發生的。舊版只在 terminate 前補驗**名稱** —— 而回收給同一支程式時
  名稱一樣。→ 記下指紋，兩個動作之前各重驗一次。
  ★WM_CLOSE 也是關閉★，所以被回收的 PID 連「拿去找視窗」的集合都不會進去
  （這件事 `close_pids` 自己的既有註解就寫著）。

## ★這個訊號必須是選用的★（今天事故的教訓直接套用）
讀不到建立時間（權限／psutil 不可用）時一律**不採用這個訊號**，不是判定「不一樣」。
把「讀不到」當成「不是同一個」→ 每一輪都判死 → 每 3 分鐘重送帳密 ——
那正是 2026-08-04 才修掉的實機故障，也是今天事故的共同病灶：
**一個無法成功的檢查被接上破壞性動作**。
所以：只有【當初記到了、現在也讀得到、而且不同】才算換了行程。
查不到就回 `None`，★絕不編一個值出來★（編出來的 0.0 會讓「兩邊都讀不到」
變成假的「時間一樣」＝假的「同一個行程」）。

## 自查 P1-B：基準遺失告警沒有節流
這支函式被呼叫的位置在【重試迴圈裡面】：
* 同一輪：寄信失敗會重試 3 次 → 最多 3 封
* 跨輪：基準要等到「寄信成功」才會被重建 → 寄不出去就每 3 分鐘再發現一次

一次磁碟事故可以變成整天每 3 分鐘一封。改成 6 小時冷卻（與 `_note_job_failure`、
掛帳告警一致），並處理「時鐘往前跳會把告警鎖死到那個未來時間」的同款坑。

## 本機驗證
- ruff 0 / pyright 0 errors / 型別債無新增
- pytest **3762 passed, 2 skipped**
- 突變驗證 **8 個全部轉紅**：登入時不記建立時間、不比對建立時間、
  讀不到當成不一樣（重登迴圈）、讀不到時編一個值、terminate 前不重驗、
  WM_CLOSE 仍送給被回收的 PID、告警不節流、時鐘往前跳鎖死告警

> 有兩個突變第一輪**沒有**轉紅：
> 「編一個值出來」——我的測試只走到「pid 是空的」那條 early return，根本沒進例外處理；
> 「WM_CLOSE 仍送給被回收的 PID」——我只斷言「有沒有送出」，而不過濾的版本照樣送。
> 補成「強制例外」與「斷言實際被拿去找視窗的那個集合」才紅。

---

# v2026.08.05.15 — 批次AB：multiset 識別 ＋ 基準唯一入口 ＋ 門檻執行期驗證（外審第 6 輪）

## P1-01 —— 兩張一模一樣的會診仍會被 set 吸收（上一批只修一半）
批次Y 把 radio 去重從文字改成控制項，第二列已經能活到簽章層 —— 但
`_consult_signature_from_roster` 回傳 `set`，兩張 `病歷號|日期|時間` 完全相同的
會診仍然只剩一筆 → 差集看不到第二張 → **漏寄，而且無聲**。

★上一輪拒絕、這一輪接受的原因★：上一輪的提案是「清單位置(occurrence)進識別」
—— 位置相依，任何一張會診離開清單就全體位移 → 整份誤判成新 → 重寄整份。
這一輪的設計**以識別字串為鍵、各自計數**（第 2 份加 `#2` 序號）：
```
1 張 → {"A"}          2 張 → {"A", "A#2"}
```
與位置無關、檔案格式仍是字串集合、舊基準零遷移、單張識別完全不變。
`_chart_of_consult_id` 同步剝序號（否則舊格式降級比對在升級輪整份重寄）。

## P2-01 —— eligibility 只守住一個寫入點
上一批的 `_may_update_baseline` 只擋「寄信成功後」那一處；首次安裝建基準與
無新會診剪枝仍直接呼叫 `_save_notified`。剪枝那條的後果：未經回讀確認的
**短**清單把還在的會診剪掉 → 之後又變「新」→ 重寄。
→ 統一入口 `_save_notified_if_eligible(roster_texts, ids, reason=...)`，
`_do_full_job` 內不再允許裸的 `_save_notified`（接線測試從「存在一個受保護」
升級成「逐一盤點、零裸呼叫」—— 上一輪那支「存在一個」的測試正是漏洞所在）。

## P2-04 —— 設定頁驗證擋不住已存在的壞值
舊版曾存下的 0、手改 JSON 的 -1，從 `build_doctor_threshold_map` 直接生效
（門檻 0 = 提醒恆真）。→ 執行期套同一組 `MIN/MAX_THRESHOLD`，不合法視為
「本診次未設門檻」並記 log（不靜默改成別的數字）。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增；pytest **3774 passed, 2 skipped**
- 突變驗證 **7 個全部轉紅**：簽章退回 set、單張也帶序號（會讓舊基準全變新）、
  取病歷號不剝序號、首次安裝繞過入口、剪枝繞過入口、入口永不擋、執行期範圍檢查移除

---

# v2026.08.05.16 — 批次AC：外審第 6 輪 P1-02/P1-06/P1-07 + P2-02/03/06/08

## P1-02 —— Delphi 會重用 form:同一個 hwnd 由隱藏轉可見
批次U 的會診視窗 generation 只認「命令後【新出現】的 hwnd」。上一輪被 Hide 的
那張會診單若被 HIS 重新 Show(同一個 hwnd),永遠不會被採認 → 每輪等滿 60 秒
失敗 → session 被殺掉重登。改成記下命令前每個 hwnd 的【可見狀態】:
採認 = 新 hwnd,或「原本不可見、命令後轉可見」的同一個 hwnd。
命令前就可見的(上一輪沒退乾淨/醫師的)仍不採認。

## P1-06 —— 閘門 15 分鐘後變成永久開閘
我上一版寫的是「超過上限就放行」——之後**每一輪**都放行,閘門只擋前 15 分鐘,
掛帳照樣增長,告警信裡「不會再登入」成了謊話。改成:放行【一次】恢復嘗試,
然後 `_unmanaged_since` 重新起算、重新擋滿一個窗口。告警信措辭同步改掉。

## P1-07 —— Outlook 逾時的 worker 沒有被終止
它可能稍後仍 Send 成功;把逾時當可重試錯誤 → 下一個 attempt 啟動第二個 worker
→ 收件人收到兩封。新增 `DeliveryOutcomeUnknown`,列為 fatal(不重試、只告警)。

## P2-02 —— 兩層重試相乘(外 3 × 內 3 = 最多 9 次提交)
會診寄信改 `max_retries=0`,只留外層的 3 次 attempt(重送同一份 artifact)。

## P2-03 —— SMTP 逾時不退配額
timeout 可能發生在伺服器已收下 DATA 之後(信可能已送達)。退回配額會低估
實際寄件量;結果不明時保留 reservation —— 最壞是少一封額度,不會超發。

## P2-06 —— 告警寄失敗不可沉默 6 小時
時間戳回撥成「10 分鐘後可再試」。

## P2-08 —— 截圖檔名微秒+pid
秒級檔名在同秒兩個 delivery 時互相覆蓋,`_discard_undelivered_shot` 還可能刪到
別人正在用的那張。

## 刻意不做的(給下一輪外審)
* **P1-03/P1-04/P1-05(hidden-desktop actor / teardown 狀態機 / modal owner-chain)**:
  大型重構,而且今天已經發生過「多個改動疊加成全停」的實機事故 —— 這一組要等
  v.13+ 在診間跑過完整循環(6 小時重啟+休息時段)之後單獨做,不跟其他批次疊。
  現況的緩解:閘門有上限、modal 判準已用 enabled 訊號、不盲按。
* **P2-02 的持久化 delivery ledger(PENDING/SUBMITTED/...)與 IMAP Sent 查證**:
  同理,等實機穩定後做。現況已把重複寄信的面縮到「單層重試+固定 Message-ID+
  逾時不重試」。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增;pytest **3785 passed, 2 skipped**
- 突變驗證 **9 個全部轉紅**(P2-03 第一個突變方向寫反,改對方向後紅)

---

# v2026.08.07.8 — 會診視窗採認:可見性三態（外審第 7 輪 P1-05／第 8 輪 §七.1）

## 同步說明
本機原有一個未推的批次AD commit，內容與另一台已推上去的工作**大量重疊**
（delivery ledger、被拒收件人、session 閘門位置）。遠端那份**更完整**
（帳本 + 逐位收件人狀態 vs 我的例外類別），所以：
* 以遠端為準 `git reset --hard origin/main`（原 commit 留在分支 `batch-ad-local-backup`
  與 scratchpad 的 patch 檔，沒有遺失）
* 只把遠端**沒有**的那一項重新套上去 —— 就是這個 P1-05

## P1-05：可見性是三態，不是布林
批次AC 讓「命令前隱藏、命令後轉可見」的同一個 hwnd 也能被採認（Delphi 常重用
form）。但那一版有兩個缺口：

1. `_visible()` 的例外回 `False`（＝**明確隱藏**）。於是一張【命令前就可見】的
   舊表單，只要那一刻查詢失敗，就會在命令後被誤判成「hidden→visible」而被採認
   —— 那可能是醫師自己開的、或上一輪沒退乾淨的舊資料。
   ★「讀不到」被當成某個確定的答案★ 是這幾天一路在修的同一個病灶，我又犯了一次。
2. 命令後新出現的 hwnd 沒有要求「現在明確可見」。Delphi 常「先建立 form →
   建 children → 載入資料 → 最後才 Show」——在 Show 之前就開始 roster settle／
   截圖，會拿到一張還沒填好的表單。

判準：兩種候選都要求 `_visible(h) is True`；命令前已可見、或可見性 UNKNOWN 的
一律不採認。連帶把「命令前已有會診視窗」那句警告的判準改成 `is not False`，
否則可見性未知的既有視窗不會被提到 —— 而那正是最該讓人知道的狀態。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增；pytest **3926 passed, 2 skipped**
- 突變驗證 **4 個全部轉紅**：查不到當成隱藏、新視窗還沒 Show 就採認、
  命令前可見也採認、警告判準退回 truthy

---

# v2026.08.07.9 — 外審第 8 輪：帳本跨 process 安全（P1-01）＋ 每筆都要有終局狀態（P1-03）

## 先講清楚風險等級
`has_live_delivery()` **目前沒有任何生產端呼叫** —— 帳本現在是純寫入的稽核紀錄，
不是寄送閘門。所以帳本這一組 finding **現在都不會造成臨床後果**；它們會在
「帳本接上寄送決策」那一刻才變成真的。這一輪先把地基修對，順序才不會反過來。

## P1-01：`threading.RLock` 鎖不住另一個 process
這本帳是【主程式與會診程式共用】的 —— 兩個不同的 process。舊寫法把本 process
記憶體裡的整份紀錄覆蓋整個檔案：
```
main    讀到 {A}          consult 讀到 {A}
main    寫回 {A,B}
consult 寫回 {A,C}        ← B 永久消失
```
`os.replace` 是原子的，但它保證的是「不會寫出半個 JSON」，**擋不住 lost update**。

★修法不必動到 SQLite★：紀錄是以**全域唯一的 delivery_id** 為鍵，所以兩個 process
不可能改到同一筆 —— 衝突只會是「用我手上的舊副本蓋掉對方的新版本」。
所以：**檔案鎖 + 鎖內重讀 + 只寫自己動過的（`_dirty`）+ 原子寫**，
就精確地消掉這個衝突，順帶讓本 process 看得到對方新增的紀錄。

* 鎖不到 → **仍然寫**（fail-open）。鎖不到就不寫＝為了避免「可能覆蓋」而造成
  「一定丟失」。退化成舊行為 + 一行警告。
* 寫回前讀不到磁碟 → **不寫**（不能拿記憶體整份去蓋別的程式的紀錄）。
* ★只實作 Windows★：本產品與 CI 都只跑 Windows。原本我加了一條 POSIX(fcntl)
  分支「以防萬一」—— 那是永遠不會被執行的死路，無法被任何測試涵蓋，
  只帶進 4 筆型別債與假的安心感。移除。

## P1-03：確定失敗會永遠停在 SUBMITTING
舊寫法只在【成功】與【結果不明】兩條路 settle。連不上、認證失敗、5xx、
SMTP 未設定這些「確定沒送出去」的錯誤直接往上拋 → 那一筆永遠停在 SUBMITTING，
而 prune 不清 SUBMITTING。三次 attempt 就留下兩筆假的「可能送到一半」。
→ **不變式：`begin()` 之後每一條出口都必須 settle**（成功/unknown/failed 三條）。

## 本機驗證
- ruff 0 / pyright 0 / 型別債無新增 / pytest **3933 passed, 2 skipped**
  （四道關卡各自獨立看 exit code —— 這次差點又用 `| tail` 把 `$?` 讀成 tail 的）
- 突變驗證 **8 個全部轉紅**

> ★突變驗證抓到我兩支測試根本沒測到東西★：
> 「A 載入 → B 新增 → A 再寫」抓不到 lost update，因為 `dict.update()` 只會新增；
> 真正的形狀是「A 手上有一份【B 後來改過的那筆】的舊副本」。
> 「讀不到磁碟不覆寫」也一樣 —— 要有【別的 process 寫的、而我記憶體裡沒有的】
> 那一筆，才看得出差別。兩支都改寫過才轉紅。

## 本輪其餘 finding 的處置
* **P1-02（帳本沒當閘門、business key 不穩定）**：正確，但那是「接上閘門」那一批的
  事。★先修 P1-03 是有順序意義的★ —— 帳本裡有假 SUBMITTING 時就接閘門，
  會直接把會診永久擋住。
* **P1-04/P1-05（止掛 Message-ID、實際補寄）、P1-06（SMTP phase）**：確認屬實，
  屬於 reconciliation/outbox 那一批。
* **P1-07/08/09（trigger 預設、durable inbox、settings transaction）**：carryover，
  維持既有排程。

---

## 外審第 9 輪（HEAD 現況・session 生命週期主題）2026-08-08

**審查方式改變**：先用歷史快照 `ad9e814..fdac832` 審過一輪，5 個 finding
**全部是「後續 commit 已修」**（`mains[0]`／閘門位置／沈冠宇預設 True／重試重跑
HIS／門檻可輸入 0）。審一個已經不存在的中間狀態，只會反覆挖出已修項，
所以改成審 `ad9e814..HEAD`（TIP 預設就是 HEAD），用 ctx 指定主題聚焦。

### 三個 CONFIRMED finding（皆對 HEAD 現況成立）

**P1-01 掉線恢復繞過 UNMANAGED 閘門**
`_automation_on_hidden` 的 except 分支直接呼叫 `_cold_start_session(cfg)`。
閘門只接在 `_acquire_session` 上 —— 而恢復路徑那一刻正是「剛剛才關不掉一個
session」，是閘門最該生效的時候。啟動器 handle 又終止不了實際 UI，於是登入
會累積、撞上 systemftp「最多兩個」上限。
→ 閘門移進 `_cold_start_session`（唯一冷啟動入口），`_acquire_session` 那一次
**移除**：15 分鐘上限的分支是「放行一次、重新起算」，查兩次的話第二次
`blocked_for≈0` 會把剛放行的那次擋回去。

**P1-02 SW_HIDE 後備模式用「比我們晚出現的 pid」冒充所有權**
兩條路徑的關鍵不對稱：隱藏桌面上 `find_windows` 只列舉我們自己的桌面，被污染
的 pid 貢獻不出視窗；**後備模式跑在醫師桌面上，污染的 pid 會真的貢獻出醫師的
視窗**。三個傷害面：
1. 登入視窗挑選 —— 醫師在那 120 秒內自己開的住院系統完全符合「比我們晚出現」，
   會被打進自動化帳密、移到螢幕外、開會診單、擷取全院病人資料。
2. 隱形執行緒 —— 每 80 毫秒把「比我們晚出現的」視窗藏起來，醫師根本沒辦法用。
3. 收尾 fallback —— 提早中止時拿全機差集去 `close_pids`，會關掉醫師剛開的實例。
→ 三處都改成只認 `spawned_root_pid`；並加上「我們的行程還活著」前提
（`_spawned.poll() is None`），因為行程一結束 pid 就可能被回收給別人。
啟動器把工作交給既有實例時 → 無法證明身分 → 本輪放棄。
（原本能正常運作的情況只有「沒有委派」那一種，這個改動完整保留它，
移除的全是危險分支。）

**P2-01 送查詢命令前沒有確認主畫面 enabled**
`_session_death_reason` 只看 hwnd 還在、身分相符。主畫面被閒置提示
（實機見過的 `TFMTimeOut_1`）擋住時是 disabled 的 —— 判活會過，但 disabled 的
視窗不會處理 WM_COMMAND。命令像是送出去了 → 等滿 60 秒 → 走恢復路徑拆掉一個
【按一下「繼續使用」就好】的 session。**與 2026-08-05 實機事故同一個形狀，
只是從退場換到進場。** 退場已有「按掉 modal → 輪詢重新 enabled」，進場沒有。
→ 改用 `_main_ready_for_next_cycle` + `_dismiss_blocking_modals`，
且在**送命令之前**就中止。

### 突變驗證（7/7 全紅）
M1 閘門不放在 `_cold_start_session` ／ M2 `_acquire_session` 也查一次 ／
M3 送命令前只判活 ／ M4 登入視窗改回「晚出現就算我們的」 ／
M5 拿掉「行程還活著」前提 ／ M6 隱形執行緒改回全機差集 ／
M7 收尾 fallback 改回全機差集。

★M4 第一版沒被抓到★：反例用「行程已結束」，被 `poll()` 前置守衛先擋掉，
量不到被測的那條規則。改成「行程還活著 + 醫師的 pid 是新出現的」才成立。
（同一個病灶：反例要只靠被測那條規則分勝負。）

### 既有測試被這次改動打回原形的兩處（都已修正，不是遷就）
* `test_no_cold_start_while_the_ledger_is_dirty` 等 stub 掉整個
  `_cold_start_session` —— 閘門移進去之後，那等於把閘門一起 stub 掉，
  測試變成同義反覆。改 stub `_cold_start_session_impl`（真的去登入的那一層）。
* `test_it_refuses_to_run_when_the_session_is_not_ours` 比對外層訊息字串。
  改成比對**原因本身**（"hwnd 已被回收給別的視窗"），外層措辭改了不該轉紅。

### 本機驗證
ruff 0 ／ pyright 0 errors ／ pytest **3945 passed, 2 skipped**。

---

## 外審第 10 輪（HEAD 現況・寄送帳本／SMTP／截圖）2026-08-08 — 7 輪後 APPROVE

貫穿整個主題的是同一句話：**我們宣稱知道「信到底送出去了沒有」，但其實不知道。**

### 第 1 回（8 項）
* **P1-03 `_submit` 包住整個 `send_message()`** —— 它內部是 MAIL → RCPT → DATA，
  例外拋出來時分不出走到哪一步，卻對【任何】例外都蓋上「已提交」的章。
  MAIL/RCPT 階段的逾時（伺服器確定還沒收到內容）→「結果不明」→ 止掛提醒
  「視為已寄」永久去重 → 那則提醒這輩子都不會寄出。
  （main.py 當時的註解還寫著「UNKNOWN 只在 DATA 已提交時才成立」——**那句是假的**。）
* **P1-02 DATA 之後的非逾時斷線根本沒被看** → 重送 → 醫師收到兩封。
* **P1-01 部分收件人被拒只記帳**，然後照樣更新已通知基準 → 永久漏寄。
* **P1-04 截圖寫到一半失敗留下殘檔**，而 artifact 還沒建立、收尾不知道路徑。
* **P2-05/06/07/08** 帳本：查詢只看啟動快照、寫回失敗沒有下文、
  business_key 用主旨、陳舊 PREPARED 沒有 API 看得到。

### 第 2 回（7 項）★其中兩個 P1 是第 1 回的修正自己開的洞★
`smtplib.SMTP.data()` 有三種結局，形狀不一樣，第 1 回只處理了一種：
| 結局 | 形狀 | 第 1 回的處理 | 後果 |
|---|---|---|---|
| ① DATA 指令回應非 354 | raise `SMTPDataError`，**內容還沒送出** | 蓋「已提交」 | 確定沒寄出的信被當成已寄、永久去重 |
| ② 送完內容最終回應非 250 | **回傳** tuple，不 raise | 回傳值被丟掉 | 伺服器明確拒絕，我們回報成功還推進基準 |
| ③ 傳輸中斷 | 其他例外 | 正確 | — |
另五項：鎖序反轉（`settle` 是 self._lock→檔案鎖，`_refresh_locked` 反過來）／
`flush()` 與 `converge_stale_prepared()` 只有測試在呼叫（「有 API ≠ 會發生」，
而註解寫著「程式結束時會再試一次」——那句當時是假的）／未簽章的退化 key 只有
收件人雜湊／補寄結果不明時毀掉已知結果。

### 第 3 回（4 項）
持續 4xx 兩次立即補寄必然失敗（→ 退避排程 2/10/30 分）／`os._exit` 出口沒補寫
（health 加 `pre_exit_callback`；main.py **三條**結束路徑）／`flush()` 在鎖外存檔／
**陳舊 PREPARED 被推論成「確定沒寄出」——前提不成立**（寫回是 fail-open，
`mark_submitting` 只改到記憶體、信卻寄出去了，磁碟上留下的一樣是 PREPARED）。
→ `begin()` 直接落地 SUBMITTING，舊紀錄收斂成 **UNKNOWN**。

### 第 4 回（2 項）
補寄佇列跨不過重啟（→ 用**已落地的帳本** `needs_recipient_retry()` 接回另一半：
掛超過一小時就明確結案 + 開發者告警。**不持久化信件內文**，因為那是病人清單）／
`pre_exit_callback` 那個分支永遠走不到（主程式有傳 `restart_callback`，它成功時
自己 `os._exit`）。

### 第 5 回（1 項）★上一回的修正自己開的洞，方向相反★
為了「不漏寄」加的 close-out，製造了一條「**誤報漏寄**」的路：補寄成功只結案新的
retry 紀錄，初次紀錄仍掛著暫時被拒 → 一小時後判成始終沒收到 → 寄告警 →
人工照著告警轉寄 = **重複的臨床通知**。
→ 帳本記 `parent_id`；補寄送達回寫初次；`needs_recipient_retry()` 只認沒有 parent 的紀錄。

### 第 6 回（1 項）★同一條路換成大小寫又走了一次★
`confirm_recipients()` 的正規化沒跟 `begin()` 一致（帳上 `strip().lower()`，
補寄拿到的是設定檔原樣、只 strip 沒 lower）。收件人有一個大寫字母就回寫不到。

### 第 7 回 → **APPROVE**

### 自己犯的錯（都被測試擋下來）
* `.part` 副檔名讓 PIL 直接 `ValueError` —— **每一張截圖都會存不了**，比原本的 bug 嚴重得多。
* 補寄判斷寫死字面值 `"transient"`，而 `classify_refusal` 回 `"transient_refused"`
  —— **補寄那段程式碼一次都不會執行**。
* `pre_exit_callback` 第一版把 `os._exit(1)` 縮進 `if` 裡 —— 沒帶回呼的行程
  **永遠不會被重啟**，RAM 洩漏防護整個失效。
* 測試自己**只檢查第一條結束路徑就 return** —— main.py 其實有三條，
  而更新重啟那條最頻繁。

### 突變驗證：**55 項全紅**
兩個第一版沒抓到的，都是反例沒真的重現缺陷：截圖 stub 在建檔前就拋（沒有殘檔可留）、
事件識別只測 helper 沒測接線。

### 本機驗證
ruff 0 ／ pyright 0 errors ／ pytest **3999 passed, 2 skipped**。

---

## 外審第 11 輪（HEAD 現況・IMAP 觸發授權／設定交易／止掛門檻）2026-08-08 — APPROVE

### 一項 REJECTED（有證據）
Codex 連兩輪要求「移除黃建仁、謝佳陵」。查 `git log -S` 得 `624fb39`（2026-08-06,
訊息「止掛提醒新增黃建仁/謝佳陵」）是**使用者在另一台機器上刻意新增的功能**。
是我寫的 ctx 停在 8/5 的狀態而寫錯，不是程式偏離契約。
→ 把沿革**寫進 `threshold_policy.py`**（決策該住在程式碼旁邊），並用更正後的
ctx **重開一輪**（resume 不重讀 ctx，錯誤敘述會鎖在 session 裡）。重開後就沒再出現。
★目前止掛對象：陳駿升、沈冠宇、黃建仁、謝佳陵 四位。★

### 兩個可被外部觸發的安全缺陷
* **`require_authenticated_trigger` 預設 False**，且 From 解析不出來時連白名單都繞過
  → **任何能寄信到那個信箱的人都能遠端啟動 HIS 查詢、讓全院會診清單被寄出**。
  改為預設 True + 既有設定檔一次性遷移（marker 寫回磁碟，之後可明確 opt-out）；
  無 From 那條 fallback 整條移除。
  ★「不敢打開」的理由是怕靜默失效 → 解法不是把門開著，而是不讓它靜默★：
  白名單來信被擋時寄開發者告警。
* **`Authentication-Results` 可被偽造**：攻擊者在自己的信裡塞一段
  `mx.google.com; dmarc=pass`，與 Gmail 真正那段（fail）串在一起搜就通過。
  改為只採信**最上方**那一段、authserv-id 完全相同；下方若有第二段冒用即整封不採信。
  另外**驗證結果原本聚合到寄件人** → 攻擊者持續寄較早的偽造信就能讓那位醫師的
  授權觸發長期失效。改為 auth 綁在每一封信上。

### 觸發信的持久化接手（`\Seen` 早於可靠接手）
journal 落地 → 才標已讀 → 才觸發；**正常完成才結案**（例外／被排進補跑佇列都不結案）；
開機補跑、過時的不補；被拒絕／未通過驗證／去重略過／無法解析 From 四類都是終局處置。

### 多檔設定交易
commit 失敗回滾／備份失敗就不開始／manifest 原子寫入且失敗就不開始／
行程被砍時開機還原／還原失敗保留 manifest 與備份／舊格式 manifest 不可當成
「全部都不存在」／殘留但救不回來的 manifest 不可永久擋住存檔／
**完成要持久標記 committed**（不能從「還有沒有備份」推斷）／
**收尾失敗要據實回報失敗，不可假成功**。

### ★連續踩到的同一類問題：修正自己開的洞★
第 2、3、4、5 回的 finding **幾乎全部是前一回修正引入的**：
* 「不漏寄」的 close-out 造出「誤報漏寄 → 人工轉寄 → 重複臨床通知」；
* 「journal 落地才標已讀」與「去重是終局處置」各自正確，**組合起來讓信永久消失**；
* 回滾用 `backups.get() is None` 推斷「原本不存在」→ **會刪掉使用者的設定檔**；
* 舊格式 manifest 沒有 `existed` → **復原刪掉全部三個設定檔**；
* commit 成功但 manifest 刪不掉 → **把剛存好的新設定檔刪掉**、或**永久擋住之後所有存檔**；
* `pre_exit_callback` 把 `os._exit` 縮進 if 裡 → **RAM 洩漏防護整個失效**。

**共同教訓：一個修正的正確性不能只看它自己，要看它與既有機制組合之後的狀態。**

### 自己在測試上犯的
* 兩次用「字元視窗」找程式碼（視窗長度是猜的，剛好切在中間就誤報）——第二次是在
  剛修完第一次之後。都改成 AST。
* 突變改到**同名的另一處**（`replace(...,1)` 命中先出現的函式），量到的不是目標。
* 測試只找到第一條結束路徑就 `return`——main.py 其實有三條。
* 突變驗證揭露**我加的守衛不承載任何性質**（`pop` 在空 dict 上本來就不會寫入）。
  沒有編造假突變，把這件事誠實寫進 docstring。

### 突變驗證：**50 項全紅**
### 本機驗證
ruff 0 ／ pyright 0 errors ／ pytest **4056 passed, 2 skipped**。

---

## 外審第 12 輪（HEAD 現況・打卡／HIS 契約）2026-08-08 — 2 輪後 APPROVE

**唯一一項 P2：刪光最後一個帳號後，打卡程式會整個消失。**

`restart_program()` 靠 `_machine_has_clock_accounts()` 決定要不要帶
`--configure-if-empty`。但那個 predicate 回答的是「**現在**設定檔裡有沒有帳號」，
而它是在 `save_config()` 把帳號寫成 `[]` **之後**才求值的 —— 讀到的是刪除
**後**的狀態。於是：刪光最後一個帳號 → 存檔 → 重啟（沒帶旗標）→ 新行程讀到
空設定就靜默結束。**設定視窗與 tray 一起消失，使用者以為打卡還在跑。**

同檔第 2275–2278 行的註解明訂「刪光最後一個帳號後應重新開啟設定視窗」——
又是宣稱與實作不符：那個 predicate 答的是別的問題。

★修法不是去修 predicate★：兩條離開設定視窗的路徑【自己明講】帶旗標
（它們本來就知道自己從哪來），predicate 留給自動更新／health 重啟用 ——
那些情境沒有人剛在編輯設定，「現在有沒有帳號」正好就是要問的問題。
反方向也釘住：沒有帳號的機器不可以被自動更新彈出打卡設定視窗
（使用者 2026-08-06 回報過的事故）。

突變驗證 4/4 全紅。ruff 0 ／ pyright 0 ／ pytest **4061 passed, 2 skipped**。

---

# 補審全案完成（2026-08-08）

`ad9e814..HEAD` 全部未審 commit 已分四個主題補審完畢，**每一主題都跑到 APPROVE**：

| 主題 | 輪數 | finding | 上線 |
|---|---|---|---|
| 1. session 生命週期／視窗辨識 | 2 | 3 | v2026.08.08.2 |
| 2. 寄送帳本／SMTP／截圖 | 7 | 23 | v2026.08.08.3 |
| 3. IMAP 觸發授權／設定交易／止掛門檻 | 5 | 29 | v2026.08.08.4 |
| 4. 打卡／HIS 契約 | 2 | 1 | (本次) |

**總計 56 項 finding、突變驗證 ~110 項全紅、1 項 REJECTED（有證據）。**

## 這次補審最該記住的三件事

1. **一個修正的正確性不能只看它自己。** 主題 2、3 有一半以上的 finding 是
   前一回修正引入的，包括兩個各自正確的機制組合起來讓觸發信永久消失、
   三個會刪掉使用者設定檔的回滾缺陷、以及一個讓 RAM 洩漏防護整個失效的縮排。
2. **「讀不到 / 沒寫成 / 現在的狀態」不可以被當成某個確定的答案。**
   這一句涵蓋了本輪至少八個 finding：journal 讀不到就覆寫、
   舊格式 manifest 沒有欄位就當成「全部都不存在」、
   commit 完成與否從「還有沒有備份」推斷、
   `_machine_has_clock_accounts()` 答的是刪除後的狀態。
3. **假的成功比失敗嚴重。** 失敗使用者會再存一次、會再寄一次；
   假成功他不會。

---

## ★補漏：`feaed5f..ad9e814`（28 個 commit）2026-08-08 — 7 輪後 APPROVE★

### 為什麼會漏
本檔記載那批的 base 是 **`feaed5f`**，但補審時我用的 base 是 **`ad9e814`**（更晚）。
中間 28 個 commit、約 914 行（`consult_query.py` 788、`pidfile.py` 210、
`imap_reader.py` 26）**從來沒有進入任何一次外審**。
成因：照記憶裡的「31 個未審 commit、base = ad9e814」直接開工，
**沒有回頭核對 repo 裡的待補審文件**——那份文件才是權威。

### 三個原始 P1（都在後續沒被改寫的區域）
1. **BDE 倒數最後 5 秒沒有人在看** —— 下 `/t 60` 卻只監測 55 秒。
   醫師在那時回座打字，診間電腦照樣重開。
2. **標記寫入失敗 → 基準遺失被當成首次安裝** —— 而首次安裝是【把當下所有
   未回覆會診靜默記成已通知然後不寄】。那批會診從此沒有人收到通知。
3. **50ms 建立時間容差擋不住快速 PID 重用** —— 本專案多支程式都是同一個
   `pythonw.exe`，名稱與 executable 檢查都會通過，最後分辨身分的只有這個數字。
   下游是 `taskkill /F /T`：認錯的代價是強殺一支無關的自家程式連同其子行程。

### 我的修法被打回來四次，每一次都是「只做到一半」或「修法本身更糟」
* **盲區搬家**：把 OS 倒數拉長到 65 秒、監測 60 秒 —— 第 60~65 秒一樣沒人看。
  → 監測期滿【先取消】排定的重開，再取最後一次 idle 樣本，確認才下立即重開。
* **產物痕跡當證據**：用 log／截圖目錄推斷「這台跑過沒有」。**兩個方向都會錯**：
  `--configure` 開個設定就留下 log（全新機器被誤判成跑過）；email 觸發會建
  去重檔與截圖，但它明確不更新團隊基準（沒有基準卻被當成有過）。
  → 整個啟發式拿掉；補救放在【建立基準的那一刻】。
* **釘住 PID 卻關掉整條救援路徑**：不管來源一律要求釘住，於是「PID 檔不可用」
  時（正是 cmdline 後備啟用的條件）PID 永遠殺不掉，**半死救援整個停擺**。
  → 來源要【帶著走】（查詢當下記下），不可以事後重讀 PID 檔去推
  （重讀回 None 會被誤讀成「後備來的」而直接殺掉已被換掉的 PID）。
* **基準在寄送成功之前就 commit**：標記失敗 → fail-open 寄信，但基準已經
  前進；信若寄不出去，下一輪認為全部通知過而什麼都不寄 —— 那批會診永遠送不出去。
  → 順序改成「先確認清單合格 → 再確認標記能落地 → 才建基準」。
* **標記在清單不合格時也被建出來** → 留下「有標記、沒基準」→ 下一輪假的遺失告警。

### 我自己犯的其他錯
* `rollback()` 漏傳引數（生產是 `_rollback_ts(why, ...)`），TypeError 被吞掉 →
  時間戳沒回滾 → 自動修復被壓住 24 小時。**而我的測試用 `lambda: None` 把它蓋掉了。**
* 改 `_baseline_absence_reason()` 時**把 `_INSTALL_MARKER.exists()` 整行一起刪掉**
  （那是這個功能的核心判斷）—— 測試立刻抓到。
* 測試「找到第一個就 return」兩次（shutdown 指令有三條、kill 點有兩個）。

### 突變驗證：**24 項全紅**
### 本機驗證
ruff 0 ／ pyright 0 ／ 型別債 0 新增 ／ pytest **4090 passed, 2 skipped**。

---

# 批次 U（2026-08-08 下午）：型別債棘輪重構過敏 + BDE 退出競態 + UNKNOWN 回查

三件事各自獨立，共同點是**「宣稱與實作不符」或「關卡自己失效」**。

## ① 型別債棘輪把 extract-function 當成新債（任務 #52）

指紋是 `檔案|所在函式|訊息|那一行的原始碼`。`所在函式` 是為了分辨同一檔案裡
兩行字面相同的程式碼而加的，副作用是：把一段程式碼搬進新函式時，裡面每一筆
**既有**診斷的 scope 都變了 → 棘輪同時報「消失 N 筆 + 新增 N 筆」→ 紅燈。
2026-08-08 就發生過：`_final` 抽成 `_collect_final_uids` 之後 **CI 連紅兩版
（v.4/v.5）**。

**修法**：`diff_counts` 把「消失側找得到【同檔案/同訊息/同一行】」的新增對消掉，
並印出「這幾筆只是換了所在函式」。對消量以消失量為上限 —— 所以「修一個、
別處又寫一個一模一樣的」照樣抓得到。

## ② 棘輪本機跑不起來 → 推上去才知道紅（同一個 #52 的另一半）

`push_helper` 刻意不跑棘輪，理由寫在註解：「逐條跑 11 次 pyright，本機約 10 分鐘」。
代價是實測到的：v.4/v.5 兩次 CI 紅燈，本機四道關卡全綠，**唯一沒跑的那道就是
紅的那道**。

**量過才改**（不是推理）：
| | 逐條 11 趟 | 一次打開 |
| --- | --- | --- |
| 時間 | 70.5 秒（不是 10 分鐘） | **6.3 秒** |
| 11 條規則的指紋計數 | — | **逐一比對完全一致** |

新增 `--fast`（一次打開所有規則、只跑一趟 pyright；pyright 每筆診斷都帶 `rule`
欄位，一趟就能分回各規則），接進 `push_helper` 的關卡清單。
**CI 仍走逐條那條路**：一次打開理論上有規則互相遮蔽的風險，那會讓診斷【變少】
—— 而變少在這道關卡只是警告，會**安靜地變弱**。同理 `--fast` 禁止與 `--update`
併用（基線必須由權威路徑產生）。

## ③ BDE 立即重開與「退出時取消」會交錯（外審 P2-01）

`_finish_bde_reboot("elapsed")` 是：① `shutdown /a` 取消排定的那次 → ② 取最後一次
idle 樣本 → ③ 仍然沒人才 `shutdown /r /t 0`。

①→③ 之間**作業系統沒有排定中的重開機**。使用者若在這個窗口按退出：
`_abort_bde_shutdown_on_exit` 看到 `pending=True`、跑一個取消不到任何東西的 `/a`
（rc!=0，log 寫「機器仍將重開」），然後 ③ 照樣執行 ——
**使用者剛把程式關掉，診間電腦立刻重開。**

**修法**：③ 之前拿 `_bde_watch_lock` 並**再看一次** `_bde_reboot_cancel`；退出那側
整段（含它的 `/a`）也在同一把鎖裡。旗標先設、鎖後拿，兩者不可能交錯。
拿不到鎖也不重開（保守方向），並回滾時間戳讓下一輪重新判定 —— 不是永久放棄。
★回滾必須在放鎖之後呼叫★：`_bde_rollback_after_abort` 自己會拿同一把鎖，
`threading.Lock` 不可重入，在鎖裡呼叫會**安靜地死鎖**（沒有任何 log）。

## ④ UNKNOWN 回查：帳本宣稱、但一個呼叫端都沒有（任務 #57 outbox）

`delivery_ledger` 模組 docstring：「UNKNOWN 就誠實地記成 UNKNOWN，之後用
Message-ID 回查寄件備份把它收斂成 CONFIRMED 或 FAILED」。
而 `resolve_unknown()` 在生產程式碼裡**沒有任何呼叫端**。後果不是少一個功能：
`LIVE_STATES` 含 UNKNOWN → `has_live_delivery()` 永遠回 True →
**同一批會診永遠不會再寄**，而且沒有人會知道那封信到底送到了沒有。

新增：
* `imap_reader.find_message_in_sent(msgid)` → **三態** True／False／**None（查不出來）**。
  選不到寄件備份、SEARCH 沒回 OK、連線失敗 —— 全部是 None，不是 False。
  Message-ID 進 IMAP 指令列，含 `"` / `\` / CR / LF 或過長 → 連線都不開。
* `consult_query._reconcile_unknown_deliveries()` —— 接在 `_do_full_job` 的補寄段，
  排在結案之前（結案要用收斂後的狀態）。自帶節流（10 分鐘）、最小年齡
  （10 分鐘，寄件備份要時間才出現）、每輪上限 5 筆。
  **★沒事做的輪次不推進節流時間戳★**：推進了的話，每 3 分鐘一輪會讓剛成熟
  的那一筆幾乎永遠等不到。

## 本機驗證
- ruff 0 / pyright 0 / 型別債（--fast）0 —— **各自看 exit code，不接 pipe**
- 突變驗證 **38 個全部轉紅**（棘輪 5、--fast/push_helper 7、BDE 7、outbox 12，
  另加重跑的 M1；含反方向：「沒有取消令就要照樣重開」「節流不可以變成永不回查」）
- ★自己犯的錯★：第一次跑 BDE 突變時加了 `--timeout=60`，而這個 repo 沒裝
  pytest-timeout —— pytest 直接參數錯誤退出，**七個突變全部被記成「紅」**。
  那正是這一批在修的同一個病灶：關卡自己失效卻看起來通過。重跑（改用
  subprocess timeout）才是真的全紅。

## 外審第 1 輪（批次 U・deep）—— 1 個 finding，CONFIRMED

**P2 `unresolved()` 只讀自己 process 的記憶體快照**
帳本是【跨 process 共用】的：`main.py` 也會把 UNKNOWN 寫進同一個檔。
`has_live_delivery()` 會先 `_refresh_locked()` 重讀磁碟，而 `unresolved()` 不會 ——
於是別的 process 建立的 UNKNOWN **永遠不會被挑去回查**，一直停在 UNKNOWN；
而 UNKNOWN 屬於 `LIVE_STATES`，那個 business_key 也就**永遠不會再寄**。
我新加的回查流程正好完全依賴這個方法，等於接了一條在某些情況下必然空轉的路。

**驗證**：逐行比對 `delivery_ledger.py` 的 `has_live_delivery()`（有 refresh、
讀不到會 `raise LedgerUnavailable`）與 `unresolved()`（兩者皆無）→ CONFIRMED。
既有測試用靜態假帳本，IPC 測試只驗 `has_live_delivery()`，這個交錯確實沒被涵蓋。

**修法**：`unresolved()` 比照 `has_live_delivery()` 先 `_refresh_locked()`，
讀不到就 `raise LedgerUnavailable` —— **不可以回空清單**（那是把「讀不到」講成
「沒有待回查的」，往安靜地什麼都不做的方向）。呼叫端 `_reconcile_unknown_deliveries`
本來就把例外接住並回 0（這一輪不做、下一輪再試）。

**新增測試**（三條，皆按外審建議用兩個帳本實例）：
A 先初始化 → B 之後才寫入 UNKNOWN → A 必須看得到；重讀失敗要拋；
拋出去要被回查流程接住而不是照樣去收斂。突變 2 個轉紅。

**沒有一併改的**：`stuck_submitting()` 與 `needs_recipient_retry()` 有同樣的形狀，
但不在本次 finding 內、且 `needs_recipient_retry()` 改成會拋會動到既有結案路徑的
語意 —— 另開待辦，不在這一批夾帶。

---

# 批次 V（2026-08-08 晚）：本機的錯不可以記在遠端頭上（任務 #49 P2-01）

## 問題
`reg52_contract.classify_*_html` 在 `from bs4 import BeautifulSoup` 失敗
（lxml wheel 沒裝好／相依衝突／換 Python）時回 `parser_unavailable`。
那個分支的註解**自己就寫著**「解析器壞掉不代表頁面壞掉」，而它回的是
`SEMANTIC_INVALID` —— 呼叫端據此：

```
_source_backoff_fail(source_key)   → 指數退避，把主機擋掉數十分鐘
_circuit_record_fail(source)       → 連續幾次就熔斷跳閘
```

**我們自己的環境壞掉，卻去懲罰一台完全健康的遠端主機。** 而且是四個來源
一起被擋，整個 reg52 功能變暗，log 上寫的是「東區主機連續失敗」——
查的人往遠端查，查不到東西。宣稱與實作不符，而且指向錯的地方。

## 修法
第四態 `LOCAL_ERROR` + `FetchOutcome.blames_remote`：
* `ok` 仍是 False、`usable_html` 仍是空字串 —— **絕對不可以拿那份 html 去用**
  （我們根本沒能確認它是不是掛號表）；
* 只是不記 backoff／熔斷，並用 `logging.error` 明講「這是本機環境的問題」。

★兩個方向都守★：真正的遠端失敗（維護頁、連不上）照樣記 —— 不記的話 P1-02
修過的那個洞（每一輪都再打一次同一個壞頁）會復活。

## #49 其餘三項：查證為【已完成】，不是漏做
| 項目 | 現況 |
| --- | --- |
| P2-03 wake token | `screen_blackout.WAKE_GRACE_SEC` 已是**一次性 token**（第 3 輪外審改的），不是時間窗 |
| P2-04 audit metadata 封閉集 | `action_ledger` 已強制 `value/detail` 必須是 `audit_events` 的型別，傳字串記成 violation 且內容不落地 |
| P2-06 IMAP 驗證 | v2026.08.08.4 已做：只信最上層 `Authentication-Results`、authserv-id 完全相同、驗證綁每一封信、`require_authenticated_trigger` 預設 True |

## 兩個自己踩到的坑（都被測試抓到）
1. **測到一條不存在的路**：我為亞大寫了「解析器壞掉不可以懲罰遠端」，但
   `classify_auh_html` 是**純文字標記**判定，從頭到尾沒碰 BeautifulSoup ——
   那條測試通過與否都不代表任何事。改成釘住真正的性質，並加一條
   「哪天亞大開始解析就要補 LOCAL_ERROR 測試」的守衛。
2. **反例被前置條件擋住**：`usable_html` 放寬成「只擋 TRANSPORT_ERROR」時
   突變仍然綠 —— 因為生產路徑建 LOCAL_ERROR 時**根本不帶 html**。
   補一條直接構造帶 html 的非成功結果，勝負只由這條規則決定，突變才轉紅。

## 照實記下的既有不對稱
`_circuit_record_fail` 只出現在 east／huisheng／auh —— **惠和沒有熔斷器**。
那是本批之前就存在的（四段複製的程式碼漏抄一段的樣子）。測試裡用
`_HAS_CIRCUIT` 表照實寫成期望值、並加一條「覆蓋範圍變了就紅」的守衛，
另開待辦追（#67）。**不假裝它有** —— 寫成「應該有」會讓測試第一天就紅，
然後被加 skip，那道守衛就永遠不會再看一眼。

## 本機驗證
- ruff 0 / pyright 0 / 型別債（--fast）0 —— 各自看 exit code
- 突變驗證 6 個全部轉紅（含反方向：維護頁不再被記＝P1-02 的洞復活）

## 外審第 1 輪（批次 V・targeted）—— 1 個 P1，CONFIRMED

**我的修法只做了一半：`main.py` 的三個判定點沒改到。**
`reg52_fetch` 的四個分院抓取改好了，但 `check_appointment_count` 裡休診表兩處、
主院一處仍然是「`not outcome.ok` 就記退避」—— `LOCAL_ERROR` 在那三處等同不存在。
**契約說「不算在遠端頭上」，實作照樣算**，而且是我自己的修正造出來的落差。

外審同時指出更前面的一刀：`soup_main = BeautifulSoup(html_main, 'lxml')` 原本
**無條件、而且排在分類之前** —— lxml 壞掉時它先丟例外，LOCAL_ERROR 判定與
「這是本機問題」的 log 一個都跑不到，例外往上被當成一般抓取失敗（會記退避）。

**驗證**：逐一讀 `main.py` 7302–7311／7386–7395／7428–7440／7416 → 全部屬實。

### 修法
* 三個判定點都改成 `if _do.blames_remote:` 才記退避，否則 `logging.error` 明講本機。
* `BeautifulSoup(html_main, 'lxml')` 包 try；壞掉時 `soup_main = None`，
  兩個使用點（主院解析、東區失敗時的內嵌週表）各自守住。
* **順手修的同一個形狀**：`_parse_doctor_info_dayoff(BeautifulSoup(html_dayoff…))`
  也是裸的。休診表【走快取進來】的輪次不會再分類，所以 LOCAL_ERROR 擋不到它。
  休診覆蓋靜默消失＝★停診的診次被顯示成正常門診★。
* 遙測誠實：解析器不可用那一支**不可以**記成 `cache_hit_parse`（沒命中也沒解析）。

### 新增測試：★真的執行 `check_appointment_count`★
外審點名既有測試只驗 `reg52_fetch`、只用 AST 看回傳值，從來沒跑過這條主路徑。
新檔 `tests/test_main_local_fault_2026_08_09.py` 把外部世界換掉後真的跑它。

### 自己踩到的三個（都被測試本身抓出來）
1. **`builtins.__import__` 攔截在這裡不管用**：契約層照樣 import 成功，回的是
   `missing_schedule_table`（維護頁）—— 測試量到的是「壞頁」而不是「本機壞掉」。
   改用 seam 注入 LOCAL_ERROR，並另加一條測試證明契約層真的會回那個狀態
   （否則就是自己編一個生產不存在的輸入，再證明程式處理得了它）。
2. **fixture 的基準狀態是錯的**：`main.requests` / `main.BeautifulSoup` 在測試
   環境都是 `None`（生產由 `_ensure_network_imports` 補上）。不補的話每個測試
   都死在 AttributeError／TypeError，或是在「解析器本來就壞」的狀態下跑，
   反方向那幾條完全沒有意義。
3. **突變 M6 第一次是綠的**：把休診表那一行的 `try` 拿掉仍然全綠 —— 因為那幾條
   測試裡 `html_dayoff` 都是空字串（stale fallback 回 `""`），那一行連跑都沒跑到。
   ★沒跑到的程式碼，突變當然不會轉紅★。補一條「休診表走快取進來」的測試才打得到。

### 突變驗證：6 個全紅（M1/M6 用更精確的錨點重跑過）

## 外審第 2 輪（批次 V）—— 1 個 P1，CONFIRMED：★我的修正造出來的新問題★

上一輪我把「休診表解析失敗」改成「log 一句就往下走」，`dayoff_data` 留在 `{}`。
於是結果照樣以 `is_live_final=True` 發布 —— **★停診的診次被顯示成正常門診★，
而且取得止掛提醒的寄信資格**。模組註解本來就寫著「只有併過休診覆蓋的才算完整的
即時資料」。**把誤罰遠端換成了假平安，比原本的缺陷更糟。**

### 修法（用既有機制，不發明新的）
`degraded_sources` 這套本來就在做這件事（`live_final = not degraded_sources`）。
五個本機失效點全部補上標記：休診 3 處（分類 2 + 解析 1）、主院 2 處（分類 + 解析）。
休診解析失敗時再多一步：**有上一份【併過休診覆蓋】的完整快取就用它**
（`_emit_cached_appointments`），不要拿半套的東西覆蓋畫面。

### 自己踩到的兩個（突變驗證抓出來的，兩條測試都「因為別的原因」通過）
1. **M8 綠**：拿掉「退回完整快取」的早退仍然全綠 —— 因為流程往下走會撞上
   「無任何可用門診資料」，三次重試失敗後**在失敗路徑上又呼叫了一次
   `_emit_cached_appointments`**，最後一則訊息剛好還是那份快照。
2. **M10 綠**：`live_final = True` 也全綠 —— 因為那條測試裡 `_break_bs4` 把整個
   `BeautifulSoup` 弄壞，主表也解析不出東西，**根本走不到發布點**，
   `finals == []` 是「一則訊息都沒發」而不是「發了但不是 live_final」。

兩個都是同一個病灶：**反例沒有隔離出被測的規則**。改成只讓
`_parse_doctor_info_dayoff` 炸掉（畸形休診表就是這個形狀）、主表資料保持完整，
真的走到發布點，勝負只由「有沒有休診覆蓋」決定 —— 兩個突變才轉紅。
並補一條「斷言退回快取的原因」，擋掉「三次重試失敗後才退回快取」那條假路。

### 突變驗證：M7/M9/M11 紅、M8/M10 修正測試後轉紅
M12（把 `_emit_cached_appointments` 的理由字串改掉）維持綠 —— 那個字串與我的
`logging.error` 同文，斷言分不出來，而且它不承載任何性質。**沒有編造假突變。**

## 外審第 3 輪（批次 V）—— 1 個 P1，CONFIRMED：只擋寄信資格還不夠

第 2 輪的修正只做到「不取得止掛寄信資格」。**沒有完整快取時仍然往下走，
把【沒有休診覆蓋】的班表發到畫面上** —— 醫師看到的還是「停診被顯示成正常門診」。

### 查證時發現的關鍵：`[CACHE_PROTECT]` 讓「補送錯誤結果」沒有用
UI 端（main.py:14712 附近）明訂：錯誤結果遇到既有資料就**略過**，保留舊快取。
所以只要**先**送出那份半套班表，後面再送錯誤結果也蓋不掉它 ——
半套的東西就留在畫面上了。**在半套已經送出之後才補救，補不回來。**

### 修法：把休診表解析搬到【所有漸進式送出之前】
`check_appointment_count` 有兩處漸進式送出（主院一處、分院／亞大一處），
兩處都在休診解析之前。休診表 HTML 那時候其實**早就抓回來了**，解析是純本機
運算 —— 所以把整段解析搬到第一處送出之前，補不完的那一輪在
**還沒發布任何東西**時就收手：
* 有上一份【併過休診覆蓋】的完整快取 → 送它；
* 沒有 → 送 `{"error": …}`，不發布這一輪的班表。

### 這一批連續三輪都是同一個病灶
P2-01 原始缺陷（誤罰遠端）→ 我的修正造出「靜默當成沒有休診」→
再修成「只擋寄信資格」→ 仍然把半套班表送上畫面。
**每一次修正都要問：這個修法新開了什麼？** 三輪的 finding 全部是前一輪修正引入的。

### 突變驗證
M13（沒有快取時仍然發布半套班表）、M14（連錯誤結果都不送＝靜默）皆轉紅。

---

# 批次 W（2026-08-09）：帳本另外兩個讀取方法也要跨 process ＋ 惠和熔斷器 REJECTED

## ① `needs_recipient_retry()` / `stuck_submitting()`（自查，與批次 U 的 P2 同形狀）
批次 U 修好了 `unresolved()`（先 `_refresh_locked()`、讀不到就 `raise`），
同一個檔的另外兩個讀取方法還是只讀本 process 的快照。

`needs_recipient_retry()` 被 `_close_out_stale_recipient_retries()` 用來找
「還沒收到的收件人」—— 只讀自己的快照的話，**`main.py` 記下的待補寄收件人
永遠不會被結案、也永遠不會告警**，而那正是那個方法存在的理由。

（在會診程式裡它剛好排在 `unresolved()` 之後而「碰巧」是新的 —— 但那是靠
**另一個方法的副作用**，不是它自己的性質。這種「碰巧對」的東西一重排就壞。）

修法與 `unresolved()` 完全一致。呼叫端本來就有 try/except，拋出去＝這一輪不動作。

**順手抓到一個 fixture 缺陷**：`test_a_stale_pending_retry_is_closed_out_and_alerted`
是直接改記憶體裡的 `created_at` 來「老化」那筆紀錄 —— 加了重讀之後會被磁碟蓋回去，
於是那個 fixture 等於沒有老化。改成 `_dirty.add()` + `flush()` 讓狀態真的落地。
★測試要用生產看得到的狀態，不是只在記憶體裡擺一個樣子★。

## ② 惠和沒有熔斷器 —— **REJECTED（有證據），不是漏抄**
我原本把它記成「四段複製的程式碼漏抄一段」。查 `git log` 與 URL 常數之後推翻：

| 來源 | host |
|---|---|
| 東區 | `61.66.117.10/cgi-bin/fh1` |
| 惠盛 | `61.66.117.10/cgi-bin/hs1`（與東區同一台） |
| 亞大 | `appointment.auh.org.tw` |
| **惠和** | **`appointment.cmuh.org.tw`（＝主院自己那台）** |

熔斷器來自 `d555f5c`「加 O36 **院外**熔斷器」，涵蓋的正好是三台**非主院**主機。
惠和跑在主院自己的伺服器上，而主院已經有自己的 `sk_main` 退避 —— 給惠和加熔斷器
等於讓一條 CGI 路徑去壓制主院自家主機的請求。**不改程式**，把理由寫進測試註解
（原本那段寫「看起來是漏抄」是錯的，已更正）。

## 本機驗證
- ruff 0 / pyright 0 / 型別債（--fast）0；突變 4 個全紅（不重讀／讀不到回空，各兩個方法）

## ③ 浮動視窗沒有「快取／即時」的概念（任務 #22）

主表格早就有這個概念：`result["from_cache"]` → 狀態列寫「已載入上次快取，等待更新」
（main.py:9467 / 11752）。**浮動視窗把快取的燈號畫得跟即時的一模一樣。**
醫師瞥一眼看到的是一個【看起來就是現在】的號碼，實際可能是幾分鐘前、甚至是上一節
門診留下的。浮窗就是「瞥一眼」用的 —— 那正是最不該說謊的地方。

`RoomStatus` 加 `stale`；`room_card_view` 帶出去；卡片上加一行灰色小字
「· 上次快取，等待更新」，燈號本身改用暗色（不用「看診中」的飽和色）。

**兩個刻意的方向**：
* **標記 ≠ 藏起來**：舊號碼仍然比沒有號碼有用，只是不可以假裝它是新的。
* **只有在真的宣稱一個號碼時才標**：離線／未開診／關診本來就沒有在講一個即時
  號碼，再標一次只是雜訊（`bool(s.stale) and state == "open"`）。

★接上去了才算數★ 這個欄位預設 False —— 上游若沒傳，它就是一句永遠為真的謊。
測試用 AST 檢查主程式真的有 `stale=` 而且值取自 `from_cache`，
並檢查畫卡片的地方真的讀了它（資料模型對了、畫面照舊說謊也是失敗）。
突變 7 個全紅（含反方向：stale 不可以把燈號整個藏掉）。

## 外審第 1 輪（批次 W）—— 1 個 P2，CONFIRMED：警語會被燈號蓋掉

我把「· 上次快取，等待更新」畫在 `(pad, 74)`，而 32pt 的燈號畫在 `(pad, 58)`
—— 同一個 x、字框重疊，而且燈號**後畫**會蓋掉警語開頭；高 DPI 只會更糟。
**★正好在最需要它的時候被蓋掉★。** 我原本的測試只驗 view-model 旗標與 AST
下標次數，沒有任何一條碰排版，所以量不到。

### 修法：排版變成【宣告式的算式】＋卡片為它長高
```
_LIGHT_ROW_Y   = 58                                   # 燈號垂直中心
_LIGHT_HALF_H  = 34                                   # 32pt 半高上界（涵蓋 150% DPI）
_STALE_ROW_Y   = _LIGHT_ROW_Y + _LIGHT_HALF_H + 4     # 警語自己一列
_CARD_H_OPEN_STALE = _STALE_ROW_Y + 14
```
測試環境沒有 Tk 顯示、量不到字，所以不去量字，改成**把不變量寫成可檢查的算式**：
警語列必須在燈號字框之外、半高上界必須涵蓋高 DPI、卡片必須裝得下多的那一列。

**順手消掉一個會漂的地方**：卡片高以前在 `_build_card` 與 `_content_height`
各寫一次三元式。抽成單一的 `_card_h(v)` —— 漂掉的樣子是「視窗高度與卡片總高
對不上」：最後一張被裁掉或多一塊黑。測試釘住「除了 `_card_h` 沒有別的函式
自己算高」。

### 又一次「反例沒有隔離出規則」
`test_the_note_string_is_shared_between_measuring_and_drawing` 第一版只檢查
`_STALE_NOTE` 這個名字**有出現** —— 把量寬度那側換成別的字串仍然全綠（畫的那側
還在用它）。突變 M12 綠燈抓到之後，改成要求 `_content_width` 與 `_build_card`
**各自**都引用它，才轉紅。

### 突變驗證：M8–M12 五個全紅（M12 修正測試後轉紅）

---

# 批次 L・L1（2026-08-09）：版本化目錄的【讀取】能力

依 `docs/批次L_版本化目錄與原子切換_設計_2026-08-03.md` §5 的三階段，只做 **L1**。
**驗收條件就一句：`current.txt` 不存在時，行為與今天完全相同。** 更新流程一個字沒動。

## 做了什麼
* 新增 `version_pointer.py`（**app 根目錄，不在 `src/` 底下**）。
  放進 `src/` 就是雞生蛋：要先知道版本才 import 得到它。純標準庫（有 AST 測試釘住）。
* 六支 `.pyw` 改成 `_SRC = _resolve_src()`，用
  `importlib.util.spec_from_file_location` 直接讀那支檔 ——
  **不把 app 根目錄塞進 `sys.path`**（那會永久參與所有 import 解析）。
* `version_pointer.py` 加進 `sync_manifest.py` 的 `extra_files`。
  ★沒加的話它永遠不會送到診間★，stub 每次都走 fallback，功能等於不存在，
  而且**沒有任何地方會說出來**（fallback 本來就是安靜的正常狀態）。

## ★三種情況不可以摺成一種★
| 情況 | 意思 | 行為 |
| --- | --- | --- |
| 沒有 `current.txt` | **過渡期的正常狀態** | 安靜走 `<app>/src` |
| 指標讀不出來／不安全 | 指標壞了 | 走 `<app>/src` + **寫 `version_pointer.log`** |
| 版本目錄不存在／沒有 `.complete` | 半成品或被清掉 | 同上 |

後兩種若安靜回退，就是「跑舊版而讓人以為更新成功了」—— 這一整批要消滅的正是這個。
`reason` 是封閉集合，呼叫端可以分流，不必比對字串長相。

## 安全與韌性
* **路徑穿越**：`current.txt` 的內容會被拼進路徑。只允許 `[0-9A-Za-z._-]`、
  長度 ≤64、不得是 `.`／`..` —— `../../evil`、`C:\windows`、含空白或 NUL 一律拒絕。
* **這一段是六支程式的共同單點**（切版本救不回來的檔）：
  `version_pointer` 載不進來、磁碟壞掉、log 寫不進去 —— 全部都有出口，
  一律回退 `<app>/src`，絕不讓六支程式一起起不來。半套更新（stub 換了但
  `version_pointer.py` 還沒到）有專門的測試。
* **六份複製品逐字相同**：有測試比對六支 stub 的解析區塊完全一致。
  分岔的樣子是「兩支跑舊版、四支跑新版」，而且沒人會知道。

## 沒做（L2/L3 等使用者定案）
設計文件 §7 的兩件事還沒有答案：① 保留幾個舊版本（預設 3）
② L2 第一次切換挑哪台機器、什麼時段。**L2 的第一次切換必須在診間電腦人工驗證**
（六支各啟動一次、確認版本號、settings 讀得到、排程觸發正常）。

## 自己踩到的
測試 `sys.path 不可以進根目錄` 第一版是掃字串，結果**紅在自己的 docstring 上**
（那段註解正在解釋「為什麼不用 sys.path」）。改用 AST。**同一個病灶這個 session 第三次。**

## 本機驗證
ruff 0 / pyright 0 / 型別債（--fast）0；突變 8 個全紅
（含反方向：「沒有指標」不可以大聲吵 —— 久了就沒人看 log）。

## 外審第 1 輪（批次 L・L1・deep）—— 2 個 P1，皆 CONFIRMED

★兩個都建立在同一個前提上，而我原本會【錯誤地駁回】它們★
外審說「`runpy.run_path` 之後 `sys.argv[0]` 是版本化的入口路徑」。我以為
`run_path` 不動 `sys.argv` —— **實測推翻了我的假設**（CPython 用 `_ModifiedArgv0`）。
先驗證再判斷，不然這一輪會以「前提錯誤」駁回兩個真缺陷。

### P1-1：版本化之後 `get_app_dir()` 會歪掉
`_looks_like_src_dir` → 回「src 的父層」。版本化之後那是 `<app>/versions/<V>`
—— **settings／log／assets 全部跑進版本目錄**，而設計明訂 settings 不隨版本走。
後果：**切一次版＝帳密、門檻、已寄紀錄全部不見**。

### P1-2：`restart_self()` 會鎖死在舊版本
它用 `os.path.abspath(sys.argv[0])` 組命令 —— 那是 `versions/<V1>/src/xxx.py`。
新行程仍跑 V1，而且**永遠不會再讀一次 `current.txt`**：更新後的重啟停在舊版。

### 修法（照外審的 minimal correction）
把兩個值**釘在固定的啟動器邊界**：六支 `.pyw` 在 `runpy` 之前寫入
`CMUH_APP_DIR` / `CMUH_LAUNCHER`（子行程繼承，watchdog 啟動的那些也算）。
`get_app_dir()` 與新抽出的 `build_restart_command()` 優先採用它們，
**但只信真的存在的目錄／檔案** —— 一個壞掉的環境變數不可以讓程式找不到設定。

這兩個缺陷在 L1 不會發作（`current.txt` 不存在），但不修就是給 L2 埋地雷。

### 自己踩到的三個
1. **測試把 pytest 自己殺掉**：第一版直接呼叫 `restart_self()`，它結尾是
   `os._exit()` —— 輸出停在 `FF.F` 之後什麼都沒有。把「組指令」抽成
   `build_restart_command()` 純函式來測，並加一條測試釘住 `restart_self`
   真的走它（否則測的是一條沒人走的路）。
2. **量錯對象**：conftest 有 autouse 夾具把 `get_app_dir()` 整個導向
   per-test 的 `tmp/_cmuh_app`。不拿 `real_get_app_dir` 的話，這幾條測的是那個
   假的 —— 而外審指出的正是「真正的推導邏輯會歪掉」。
3. **fixture 不像生產**：`_looks_like_src_dir` 認的是 `cmuh_common/version.py`。
   少了它述詞不成立，走的是另一條分支。

### 突變驗證：7 個全紅（M6/M7 用更精確的錨點重跑過）

## 外審第 2 輪（批次 L・L1）—— 1 個 P1，CONFIRMED：★我上一輪的修正只做了一半★

我把 app 根目錄釘住了，卻**只讓 `paths.py` 讀它**。`watchdog_core` 與
`watchdog_runner` 仍然用 `__file__` 推根目錄 —— 版本化之後推出來的是
`<app>/versions/<V>`：watchdog 讀到【另一份】設定與鎖，而且到版本目錄底下找
六支 `.pyw`（那裡沒有）。**★watchdog 再也救不回任何一支臨床程式★，
而它正是最後一道防線。**

### 修法
把 pin 的讀取收斂成單一的 `paths.pinned_app_dir()`（驗證邏輯只寫一次，
不會有「某一處忘了驗存在」那種漂移），兩支 watchdog 都走它。
`watchdog_runner` 跑在 `cmuh_common` 可 import 之前，只能寫死字串 ——
加一條測試**逐字比對**它與 `paths.APP_DIR_ENV`：打錯字的樣子是「釘了但沒人讀」。

### 突變驗證：M8–M11 全紅（累計 11 個）
含反方向：`pinned_app_dir()` 不驗目錄存在 → 一個壞掉的環境變數就能讓程式找不到設定。

---

# 批次 X（2026-08-09）：任何「再開一次自己」都要走固定 launcher

外審（第二份，2026-08-09）三個新 P1 + 一個 P2，**共同病灶只有一個**：
`runpy.run_path` 會把 `sys.argv[0]` 換成被執行的那支源碼（`_ModifiedArgv0`）。
版本化之後那是 `versions/<V1>/src/xxx.py` —— 任何拿它去重新啟動的地方都會
**鎖在舊版本，而且不再讀 `current.txt`**。L1 只修了 `restart_self()` 一處。

| 路徑 | 後果 | 狀態 |
| --- | --- | --- |
| `restart_self()` | 更新後的重啟停在舊版 | L1 已修 |
| `run_as_admin()`（P1-02） | UAC 提權後跑舊版、不重跑開機復原 | **本批修** |
| 托盤「設定」（P2-01） | **舊版 UI 寫新版 settings**，舊預設值覆寫新欄位 | **本批修** |

## 單一真相來源
新增 `paths.self_entry_path()`：有通過驗證的固定 launcher 就用它，
否則照舊用 `sys.argv[0]`（過渡期、直接跑 src 不可以壞掉）。三處全部走它。
**並加一條逐一盤點的守衛**：AST 掃 `src/` 所有把 `sys.argv[0]` 當成「要執行的檔」
傳給 `launch_python_script`／`Popen`／`os.exec*` 的呼叫，清單必須是空的 ——
只修看得到的那幾處，下一個新增的地方又會踩同一個坑。

## P2-02：環境變數不可以只驗「存在」
`pinned_launcher()` 現在要求它 **realpath 之後就在釘住的 app 根目錄第一層**
（六支 launcher 真正的位置）。環境變數是會被繼承的 —— 只驗存在的話，
一個指向別處的值就能讓我們去重啟**另一支程式**。

## P1-03 的一半：resolver 自己壞掉不可以是靜默的
六支 stub 的 `_resolve_src()` 以前是 `except Exception: return fallback`，
**一個字都沒留**。而我在 `version_pointer.py` 的文件裡寫著
「『沒有指標』與『指標壞了』不可以摺成一種」—— 卻在 stub 把「resolver 載不進來」
摺進了那個安靜的正常狀態。★宣稱與實作不符，而且是我自己的原則★。
現在分成：`FileNotFoundError`（過渡期正常 → 安靜）vs 其他任何失敗
（→ 寫 `version_pointer.log`，明講「這一輪不是新版本」）。

## 自己踩到的三個
1. **無條件覆寫 `args[0]`**：把既有測試（明確傳 argv 驗引號處理）弄紅了。
   那不是測試的問題，是我改壞了語意 —— 改成只有拿到【通過驗證的】pin 才替換。
2. **`_stub_block()` 的擷取範圍沒跟著新函式走** → exec 出來 NameError，
   測到的是我的擷取範圍而不是行為。
3. **`monkeypatch` 攔 `builtins.open` 會連 `exec_module` 讀原始碼一起弄壞** ——
   改用真實的檔案系統條件（把 log 路徑做成目錄）。

## 沒做（下一批）
* 復原排到版本解析之前（P1-03 的另一半）—— 要動六支的開機順序，單獨一批。
* watchdog 排程改走 `.pyw --once` + 舊排程 migration（批次 Y）。

## 本機驗證
突變 6 個全紅（含反方向：沒釘住不可以覆寫呼叫端指定的 argv）。

## 外審第 1 輪（批次 X・deep）—— 2 個 finding，皆 CONFIRMED，且都是本批寫出來的

### P1：用 `except FileNotFoundError` 當「還沒送到」的判準是錯的
resolver **存在但執行時**自己去開別的檔失敗，丟的也是 `FileNotFoundError` ——
於是一個【壞掉的】resolver 被當成【還沒送到】而**靜默跑舊版**，
正是我這一批要消滅的東西。改成**先問 `os.path.isfile(_p)`**：
只有「檔案不存在」才安靜，其餘任何例外一律留紀錄。

### P2：`if root and ...` 讓守衛可以 no-op
沒有（或無效的）`CMUH_APP_DIR` 時，整個 containment 檢查被跳過 ——
**任何存在的檔都被接受**：繼承來的陳舊 `CMUH_LAUNCHER` 就能讓我們去重啟
別的程式，而 UAC 那條路還會把它**提權**。
兩個值是【一組】的：沒有可信的根，就沒有可信的 launcher。改成 fail-closed。

### ★而且我自己的舊測試把這個缺陷釘成了通過條件★
`test_restart_goes_through_the_fixed_launcher` 只設 `CMUH_LAUNCHER` 就期待被接受。
守衛改成 fail-closed 之後，紅的是**那條測試**而不是程式 —— 這就是
「測試的期望值也是一種宣稱」。已改成兩個值都設，並補一條
「沒有可信的根要拒絕」的反向測試。

### 突變驗證：M7–M9 全紅（含反方向：檔案不存在時不可以吵）

## 外審第 2 輪（批次 X）—— 1 個 P1，CONFIRMED：★又是我上一輪的修法新開的★

第 1 輪我把「用 `except FileNotFoundError` 判斷還沒送到」改成 `os.path.isfile(_p)`。
但 `isfile()` 對 **同名目錄**、**壞掉的連結** 一律回 False —— 而那些正是
**部署失敗留下的痕跡**，卻同樣被摺進「這個檔還沒送到」那個安靜的正常狀態。
★又一次把看得見的壞變成安靜的壞。★

改用 `os.stat(_p)`：
* `FileNotFoundError` → 安靜（真的還沒送到）
* 其他 `OSError`（權限、同名目錄的存取錯誤…）→ 留紀錄
* 能 stat 到就往下走，讓載入器去失敗、由 `_note_resolver_failure` 記下來

**連續三輪，每一輪的 finding 都是前一輪修法引入的**：
`except FileNotFoundError` → `isfile()` → `stat()`。
共同形狀是**用一個「便利的判斷式」去代表一個「語意上的狀態」**，
而那個判斷式在別的原因下也會成立。

突變 M10/M11 全紅（含反方向：真的沒送到時不可以吵）。

## 批次 Y（同一輪工作區）：watchdog 排程改走 root `.pyw`
* `安裝開機自動啟動.ps1` 的 `$scriptFullPath` 由 `src\watchdog_runner.py`
  改為 `$pywPath`。
* 新增 `cmuh_common/watchdog_task.py`，接在 `--once` 路徑：
  已部署的電腦不會再跑安裝腳本，舊 task 會一直指著 src 底下那支。
* **改完一定回讀**（`/Change` 回 0 不代表寫進去了）；
  **「查不到」是獨立出口**，不觸發改寫也不宣稱沒問題；
  **launcher 不在就不改**（改了會指向不存在的檔，比現況更糟）。
* 16 條測試；`watchdog_task.py` 已加進 manifest
  —— ★同一道守衛第二次救我★（L1 的 `version_pointer.py`、這次的 `watchdog_task.py`），
  失效樣子都是「程式寫好、測試全綠，但那個檔永遠不會送到診間」。

## 外審第 3 輪（批次 X）—— 1 個 P1，CONFIRMED：`os.stat()` 會跟隨符號連結

壞掉的連結照樣丟 `FileNotFoundError` → **又被當成「還沒送到」而靜默**，
而上一輪的註解才剛把壞連結歸類成【部署失敗的痕跡】。宣稱與實作不符。
改用 `os.lstat()`：只有「目錄項目真的不存在」才安靜。

### ★連續四輪，每一輪的 finding 都是我前一輪修法引入的★
```
except FileNotFoundError  →  isfile()  →  stat()  →  lstat()
```
| 判斷式 | 會被誤判成「還沒送到」的東西 |
| --- | --- |
| `except FileNotFoundError` | resolver 存在但內部開檔失敗 |
| `isfile()` | 同名目錄、壞掉的連結 |
| `stat()` | 壞掉的連結（它會跟隨連結） |
| `lstat()` | 只有「目錄項目真的不存在」 |

**共同形狀：用一個「便利的判斷式」去代表一個「語意上的狀態」，
而那個判斷式在別的原因下也會成立。** 每一次都把看得見的壞變成安靜的壞。

### 測試刻意用結構性斷言
釘住 `lstat`、禁止 `stat`，不真的建 symlink ——
Windows 建 symlink 要權限，沒權限的機器上那條會 skip，
而**一條會 skip 的測試守不住任何東西**。突變 M12 轉紅。

---

# 批次 Z（第一半）：卡住的 SUBMITTING 也要進回查（外審 P1-04）

`begin()` 已把 SUBMITTING 落地；**SMTP 送出之後、settle 之前 crash**，那一筆就
永久停在 SUBMITTING。`stuck_submitting()` 這個 API 早就存在，但**沒有任何生產
呼叫端** —— 那個 docstring 的宣稱一直是假的。

★這是接寄送閘門的前置條件★ SUBMITTING 屬於 `LIVE_STATES`：一旦接上閘門，
沒有回查的 SUBMITTING 會**永久擋住同一批會診**。所以順序必須是
「先讓 SUBMITTING 收斂得掉，才能接閘門」。

回查 worker 現在同時消費兩個來源，走同一條 Message-ID 回查；
`_STUCK_SUBMITTING_AFTER_SEC = 900`（正常的 SUBMITTING 只存在幾秒鐘）。

## 順手修的測試缺陷
假帳本 `_Led` 沒有 `stuck_submitting` —— worker 一改，六條測試測到的是
AttributeError 而不是被測的行為。假帳本要跟上生產介面。

## 外審第 1 輪 P2（CONFIRMED）：兩個來源接起來直接切上限 → 餓死
`unresolved() + stuck_submitting()` 然後 `[:5]`：只要有 5 筆以上的 UNKNOWN，
**SUBMITTING 永遠輪不到**（UNKNOWN 產生的速度可能超過每 10 分鐘 5 筆的消化率）。
餓死的那一筆接上閘門後會永久擋住它的 business key。
★而我自己的上限測試只放 UNKNOWN，量不到這件事。★

改成**合併後依 `created_at` 全域排序**再切：最久沒收斂的先查，兩個來源自然公平。
突變 3 個全紅（含反方向：排序方向相反＝最新的先查）。

**教訓：加上限本身是對的，但「加上限」與「兩個來源」組合起來產生了新的失效模式**
—— 又一次「修正的正確性不能只看它自己」。

## 刻意沒做：UNKNOWN 仍寫 stop-alert 的 legacy marker
外審點名它是缺陷，方向同意，但**不做半套**。現行回 True 的理由是
「回 False → 呼叫端釋放寄送權 → 下一輪掃描再寄 → 醫師每輪收到重複提醒」。
只把 marker 拿掉＝把「少數情況漏一則」換成「每輪重複寄」。
需要**暫時性抑制**機制（獨立待辦 #71），設計已定：
另開 `alert_email_pending.json`（不動既有格式），
UNKNOWN → 寫 pending；回查查到 → 升級成永久；查無 → 清掉允許重寄；
★pending 超過 N 小時仍無答案 → 告警並清掉★（IMAP 長期不可用時
不可以安靜地永遠不寄也不說 —— 那就是 8/5 事故那個「沒有出口的 fail-closed」）。

---

## 批次 AA（2026-08-09）— 外部 review 的 P1-02/03/04 + P2-01/02/03

上線 **v2026.08.09.7**（Codex deep 3 輪 APPROVE）、**v2026.08.09.8**（CI 修正，tests-only）。
兩版 CI 皆 4/4 綠。

### 修了什麼

| # | 缺陷 | 修法 |
|---|---|---|
| P1-03a | `_run()` capture 了 stderr 卻不回傳 → 權限不足被判成「這台沒有 task」 | 回三值；只有明確的 not-found 字樣才 `NO_TASK` |
| P1-03b | 解析 `/FO LIST /V`（給人看的在地化輸出）當機器契約 | 改 `/XML` + local-name 比對 `<Command>`/`<Arguments>` |
| P1-03c | 遷移驗證只判「不含 legacy」→ 指到錯的根／缺 `--once` 都算成功 | 與 `desired_action()` 精確比對 |
| P1-02 | resolver 不在一律安靜 | 只有「resolver 與 `current.txt` **都**不在」才安靜 |
| P1-04 | 回查只存在於會診程式 | 抽出 `cmuh_common/delivery_reconcile.py`，主程式也驅動；節流改跨 process |
| P2-01 | 一筆壞時間戳打掉整輪 | `_created_at()` / `_as_epoch()` |
| P2-02 | launcher 只驗「存在 + 在 app 根第一層」 | 加 `paths.LAUNCHER_NAMES` 封閉集合 |
| P2-03 | 沒有 Message-ID 的紀錄永遠進不了 `ripe` | 逾 24h 明確結案 + 告警 |

### 外審第 2 輪又抓到 5 個（全部 CONFIRMED，都是本批引入的）

1. **`schtasks /XML` 被寫死用 UTF-8 解**。★實機量到的比外審描述的更糟★：
   本機（GetACP=GetOEMCP=936）導到 pipe 的輸出是**單位元組 cp936**，不是
   BOM 標示的 UTF-16。中文全變 U+FFFD → action 永遠對不上 → 每兩分鐘重寫一次
   排程且回讀永遠失敗，舊排程一次也遷移不成功。
   修法：capture bytes，BOM→UTF-16、否則 utf-8 嚴格→主機 OEM/ACP code page。
   **建了真實排程做端到端驗證**：中文完好、`_same_action` True。
2. **`unresolved()` 直接拿原始值當 sort key** → 一筆字串 `created_at` 讓
   `sorted()` 拋 TypeError，整份清單列不出來（`Reconciler` 的容錯在它**之後**才跑）。
3. **回查同步串在刷新 worker 上** → IMAP 不可達時掛號資料先卡 60 秒以上。
   改成 single-flight 的背景 daemon 執行緒。
4. **XML `<Command>` 不含引號** → Python 裝在 `C:\Program Files\...` 的機器上
   永遠對不上 quoted 的 `desired_action()`。改成重新加引號。
5. **NaN/Infinity 不會拋例外**（`json.load` 接受、`float()` 也接受），而 NaN
   參與的比較永遠是 False → 同一個壞值在兩條路上往**相反**方向出錯：
   年齡門檻永遠不成立（永不回查）、放棄門檻也永遠不成立（**立刻**被結案）。
   兩處都加 `math.isfinite()`。

### 第 3 輪：manifest 雜湊

外審指「修完沒重新同步 manifest → updater 會拒絕整批更新」。
**部分駁回**：`push_helper` step4 每次 push 都重跑 `sync_manifest`、step5 逐檔
驗雜湊，所以發布路徑不會發生那個後果。但工作區確實不同步，已重新同步
（134/134 相符，用生產的 LF-normalize 算法驗過）。→ APPROVE。

### CI 教的一課（v2026.08.09.7 紅、.8 修好）

我把 **cp936 寫死在測試裡**，而 CI runner 是 **cp437**（US-English Windows）。
那等於「拿 A 機器的位元組去用 B 機器的解碼器解」——**生產不會發生**（位元組與
解碼器都來自同一台機器）。而且**單位元組 code page 對任何位元組都解得出東西、
永遠不會拋 UnicodeDecodeError**，所以錯得很安靜（只是一堆亂碼）。
改成用 `_decode_candidates()` 取主機自己的 code page 取樣，測真正的性質。

### 突變驗證

P1-03 六條、P1-02 三條、P1-04 七條、P2 群七條、第 2 輪九條 —— **全部轉紅**，
還原後與原檔逐字相同。其中兩次抓到我自己的假綠燈：
* `_run` 的 stderr 修正沒有任何測試踩得到（所有測試都 stub 掉 `_run` 本身）；
* 「壞時間戳」測試用 `None`，但 `x or 0` 先把它變成 0，根本走不到 except。

### 仍未做

* 外審 P1-01（開機復原早於指標解析）、P2-04 的部分整合測試缺口。
* 批次 Z 下半（#71，UNKNOWN 暫時性抑制）——設計文件已寫，未實作。
* **實機驗證自 8/5 起仍為空白**（本批只有 schtasks 那一項做了實機探針）。

---

## 批次 AB（#71 批次 Z 下半，2026-08-09）— UNKNOWN 的**暫時性**抑制

上線 **v2026.08.09.9**（Codex deep 3 輪 APPROVE，CI 4/4 綠）。

### 問題

`_send_alert_email_via_smtp` 遇到 `DeliveryOutcomeUnknown` 回 `True`，
呼叫端寫下**永久**去重記號。批次 U 把回查接上去之後：
回查查無 → 帳本說「可以重寄」，而 `alert_email_sent.json` 說「已寄過」，
**marker 贏** → 那一則止掛提醒永遠不會再寄。
兩個真相來源互相矛盾，而錯的那個贏。

不能只把 marker 拿掉：回 `False` 會讓醫師**每輪**收到重複提醒。需要第三種狀態。

### 做法

| 檔 | 內容 |
| --- | --- |
| `alert_state.py` | `PENDING_*` 封閉集合、`new_pending_entry` / `pending_age` / `is_suppressing` / `decide_pending` / `same_pending_generation` / `load_alert_pending` / `pending_for_save` |
| `delivery_ledger.py` | `state_of(did)`：**先重讀磁碟**（收斂那筆的可能是別支程式）；查不到回空字串＝「不知道」 |
| `main.py` | `SendResult`（`__bool__` 維持舊語意 + `unknown`）、`_mark_alert_email_pending` / `_sweep_alert_pending` / `_promote_alert_pending` / `_release_alert_pending` / `_retry_alert_pending_save`；去重述詞與 claim 都改看「永久記號 **或** 未逾期的 pending」 |

### ★三個刻意的設計決定★

1. **逾期一律解除 + 告警**，不是延長抑制。IMAP 長期不可用時回查永遠沒答案；
   沒有出口就會從「防重複」變成**永久靜默漏寄**（2026-08-05 事故形狀）。
2. **`is_suppressing()` 自己就會過期**，不依賴掃描跑過 ——
   解除若只發生在掃描裡，掃描壞掉就等於永久抑制。
3. **壞掉的 `since` 當成 `inf`（很舊）**，不是「剛寫的」。

### 外審三輪抓到 5 個（全部 CONFIRMED，全是我當輪引入的）

**第 1 輪**
1. **舊快照刪掉新的那一筆**：掃描是「取快照 → I/O → 再改」，空窗裡同一個
   notify_key 可能已逾期、被重寄、又寫了新的一筆。照 key 無條件刪 → 刪到新的
   → 抑制消失 → 一直循環重寄。修法：每筆帶 `gen`，動它之前比對世代。
   （正是本 repo 踩過的 generation lease 形狀。）
2. **落地失敗永不重試**：抑制只活在記憶體，重啟後既無永久記號也無 pending
   → 重寄。修法：`_alert_pending_dirty` + 每輪掃描重試。
3. **未來的時間戳繞過 6 小時上限**：`since > now` 時年齡是負的 → 判成「很新」
   → 抑制被延長整個時鐘偏移量（往回調 30 年就是抑制 30 年）。
   修法：容忍 `PENDING_CLOCK_SKEW_SEC`(300s)，超過當壞值。

**第 2 輪（都是第 1 輪修正引入的）**
4. **升級時出現空窗**：上一版「先刪 pending（上鎖一次）→ 再寫永久記號（再上鎖）」。
   兩次之間掃描緒 `_claim_alert_email()` 會**兩個來源都看不到** → 重複寄一封
   帳本已確認送達的信。修好漏寄卻換來重複寄送。
   修法：同一個臨界區、**先立記號再刪抑制**（抽出 `_mark_alert_email_sent_locked`）。
5. **關機／更新重啟不補寫**：只在掃描裡重試 → 「寫失敗 → 磁碟恢復 → 還沒輪到
   掃描就重啟」這條路上抑制憑空消失。修法：兩條退出路徑都補寫。

### 自己抓到的兩個測試品質問題

* **反例沒有隔離被測規則**：`test_a_small_clock_skew_is_tolerated` 用
  `now + SKEW/2` 建資料 —— 把容忍度突變成 0，測試資料也跟著變 0，突變照樣全綠。
  改成絕對值 30 秒（診間 NTP 抖動量級）。
* **守衛被重構打敗**：`test_the_writer_asks_before_overwriting` 掃死
  `_mark_alert_email_sent`。本體抽成 `_..._locked` 之後它掃到一個只剩兩行的殼，
  **靜默失效**。改成「找出所有真的寫這個檔的方法逐一檢查 + 空集合不算通過」。
  （另外第一次驗這條守衛時我的突變錨點縮排寫錯，根本沒套用 → 誤判成「沒轉紅」。
  **突變驗證要先確認突變真的被套用**。）

### 突變驗證

12 + 7 + 5 條全部轉紅，還原後逐字相同。四道 gate 全綠（4503 passed, 2 skipped）。

### 仍未做

* 外審 P1-01「開機復原早於指標解析」。
* SQLite atomic claim（架構投資，需使用者決定）。
* **實機驗證自 8/5 起仍為空白**。

---

## 批次 AC（外審 P1-01，2026-08-09）— 復原模組的來源

上線 **v2026.08.09.11**（Codex deep 1 輪 APPROVE、無 finding，CI 4/4 綠）。

### 問題

六支 `.pyw` 的 `_recover_incomplete_update()` 用裸的 `import bootstrap_recovery`，
而那一行之前 `sys.path` 開頭已經是**版本解析後**的 `_SRC`。
→ **復原模組本身來自那棵可能正壞掉的樹**（它存在的理由正是「更新沒走完、
新舊混版」）。拿壞掉的東西去修壞掉的東西 —— 與 2026-07-27 會診事故同一個病灶。

L1 階段 `current.txt` 通常不存在（`_SRC == <app>/src`）所以看不出來；
**L2 版本化目錄一落地就會分歧**。

### 改法

六支逐字相同的解析區塊裡新增 `_load_bootstrap_recovery()`：
只從固定的 `<app>/src/bootstrap_recovery.py` 走
`spec_from_file_location` 載入，**完全不碰 `sys.path`**。
★L2 落地時那個檔必須繼續存在★ —— 找不到就走各支既有的「復原模組不可用」
路徑（主程式問人、背景程式放行），不可以偷偷改用版本樹裡那一份。

**載不進來拋例外、不回 None**：呼叫端本來就是 `try/except`，而
`_report_startup_crash()` 靠「當下有活的例外」才寫得出 traceback。
回 None 會讓 log 記成 `NoneType: None`、對話框變成「啟動失敗：None」——
診斷資訊比修正前更差。（這是我第一版寫錯、自己抓回來的。）

### 突變驗證的一課

「載入器改讀 `_resolve_src()`」那條**第一次沒轉紅**：我的反例只把版本樹塞進
`sys.path`，卻沒有 `current.txt`／沒有 `version_pointer.py`，
於是 `_resolve_src()` 本來就回 `<app>/src` —— **兩條路答案一樣，反例量不到差別**。
改成用真的指標機制造出分歧（兩棵樹各放一份內容不同的模組），
並先斷言「測試前提確實成立」，才測得到。
（同一個教訓今天出現三次：反例必須真的隔離被測的那條規則。）

---

## 穩定性總體檢（2026-08-10 起）— 批次 SA / SA-2 / SB

盤點方法：自己的系統性掃描（AST 掃自我重排 after 迴圈、容器上限、資源
配對、timeout 覆蓋）＋ 併行的 GPT-5.6 獨立穩定性分析（10 條 finding，
`.codex-review/stability_2nd_opinion_out.txt`）。

### 批次 SA（v2026.08.10.1，外審 2 輪 APPROVE）
* 新模組 `tk_stability.py`：`ThrottledExceptionLog`（每簽名 60s 節流）、
  `pump_log_records`（單筆 try + 截 500 行）。
* **`tk_exception.py` 內部委派節流實作**（第 1 輪外審抓到我沒查先例、
  另立一套還被舊的蓋掉 —— check-prior-art 違規實例）。
* `_update_clinic_lights_loop`（596 行、零 try、重排最後一行）拆駕駛座：
  例外不再永久凍結 reg64 管線。
* consult/autoclock log 幫浦統一（原本無行數上限、一筆壞紀錄殺全部）。
* `_show_notice` 關閉鈕走 cleanup（死 Toplevel 把新通知推出螢幕）。

### 批次 SA-2（v2026.08.10.2，外審 1 輪 APPROVE）
* 帳本檔案鎖 LK_LOCK 有界重試 + pid 相位錯開（CI IPC flake 根因：
  1 秒輪詢 10 次全撲空 → fail-open 無鎖寫 → 互相覆蓋）。
* 未來日期守衛比對基準改台灣時間（UTC runner 上台灣早上的標記被判成未來）。

### 批次 SB（v2026.08.10.3，外審 6 輪 APPROVE）
外部 #3/#4/#6 + 五輪加深：
* **refresh age takeover**（900s）+ 世代擁有權（殭屍不清狀態/不接力/
  完成 callback 不執行）；接管合併同簽名排隊。
* **殭屍世代資料丟棄**：`UiClinicDataMessage.refresh_gen` 全 5 個送出點
  帶戳、接收端世代不符整筆丟棄。
* **win32_safe 放生 thread 上限**：原子佔位（檢查+入列同臨界區）、
  未 start 佔位以 `ident is None` 判定、start 失敗釋放佔位。
* **liveness 全面 monotonic**（autoclock/consult 寫入端+比較端同步換）。
* **reg52 fetch 容量**：有界取得(45s)→等不到拋 `Reg52BackoffActive`
  （本地問題不進遠端 backoff）；被抱死 >300s → 世代隔離換新 semaphore；
  epoch 上限 3；到頂升級到 `_restart_when_hotkey_idle(force_after_max=
  False)` —— 熱鍵活著就無限期等，**絕不腰斬 HIS 寫入**。

### 這一批的教訓
* 外審 6 輪把「加一個上限」逼成完整的資源生命週期設計：
  check-then-act 原子化 → 未 start 的佔位 → start 失敗釋放 →
  換代上限 → 重啟升級 → 升級不得強制。**每一輪都是我前一輪修正的洞**。
* 說明註解裡的字面救活突變：今天第四次。負向斷言一律先剝註解。
* 突變還原被防毒佔檔擋掉一次：還原也要有重試，且要逐字驗證。
* 測試自己鎖死一次：到頂後 semaphore 不再換，阻塞式 acquire 等於自殺。

### 剩餘（批次 SC/SD 待做）
* 外部 #1 autoclock WebDriver wedge 持有 clock_lock（Critical、需設計）
* 外部 #2 consult 隱藏桌面 worker 遺棄（Critical、需設計）
* 外部 #7 logging.shutdown 卡死（port consult 的既有解法）
* 外部 #8 HICON 洩漏、#9 Outlook COM worker、#10 clinic_history 保留期
* 實機驗證自 8/5 起仍為空白

### 批次 SC（v2026.08.10.6，外審 2 輪 APPROVE）— 兩條 Critical
* **autoclock `clock_lock` 有界化**：一個打卡任務整段持有全域鎖，而
  Selenium transport command 在 chromedriver wedge 時永久不返回 →
  之後每個 key、每次 gate 逾時接管的 worker 全堵在無界 acquire 上；
  而 scheduler 照常 tick → 兩層 watchdog 都以為正常 →
  **打卡永久失效且無聲**。改 `bounded_clock_lock`（600s）+ 連續 3 次
  等不到 → `_autoclock_hard_exit`（打卡是冪等的，重啟不會重複打卡）。
  ★零重排★：只換掉 `with` 的第一個 context manager。
* **consult 隱藏桌面 worker single-flight**：240s `join()` 只是放棄等待，
  worker 沒被取消（`CloseDesktop` 只有它自己走到 finally 才執行）。
  外審第 1 輪抓到我把守衛擺在 `_ensure_hidden_desktop()` **之後** ——
  每輪照樣先開一個 HDESK 才拋（洩漏一樣），而 SW_HIDE 後備路徑
  **完全繞過守衛**。守衛保護的是「不可以再開一輪自動化」，
  位置要在所有路徑的共同上游。

### 批次 SE（v2026.08.10.7，外審 2 輪 APPROVE）— 外部 #9 收尾
* Outlook COM worker single-flight。外審抓到 check-then-set 無鎖
  （`_flow_lock` 與 `_test_email_gate` 是不同守衛，可真的併行），
  且清除無條件。改原子佔位 + 條件式釋放 + start 失敗釋放。
* ★我又踩一次剛學過的坑★：佔位述詞只看 `is_alive()`，
  但佔位在 `start()` 之前 —— 未 start 的 thread `is_alive()` 也是 False，
  併發測試量到 8 條同時開出。改 `ident is None or is_alive()`。

### ★外部第二意見 10 條全數處理完★
| # | 內容 | 批次 |
|---|---|---|
| 1 | autoclock WebDriver wedge 持有 clock_lock | SC |
| 2 | consult 隱藏桌面 worker 遺棄 | SC |
| 3 | refresh 單飛旗標無 age takeover | SB |
| 4 | win32_safe 放生 thread 堆積 | SB |
| 5 | 通知關閉鈕繞過 cleanup | SA |
| 6 | liveness 用 wall clock | SB |
| 7 | 退出路徑 logging.shutdown 卡死 | SD |
| 8 | HICON 洩漏 | SD |
| 9 | Outlook COM worker | SE |
| 10 | clinic_history 無保留期 | SD |

### 這一整輪的共同形狀（值得記住的）
**「卡住的東西不可以拖垮整個子系統」** 在這個 codebase 出現了七次，
每次的正確解法都是同一組件：
1. **有界取得**（無界的 acquire/join 就是把別人的故障變成自己的故障）
2. **原子佔位**（check-then-act 必須在同一個臨界區）
3. **未 start 的佔位也要算數**（`ident is None`，`is_alive()` 騙人）
4. **條件式釋放**（只清自己那一條，別清別人的）
5. **失敗要釋放**（start 失敗的佔位＝永久停用）
6. **上限 + 升級**（放生要有數量上限；到頂就重啟，那是唯一能終結
   native-wedged thread 的手段）
7. **升級不得腰斬臨床寫入**（等閒置，絕不強制）
8. **守衛要在所有路徑的共同上游**（不然後備路徑繞過去）

### 批次 SF（v2026.08.10.9，外審 3 輪 APPROVE）— 會診查詢穩定性第二輪
自掃 3 條 + 獨立 GPT-5.6 分析 15 條中的 3 條：
* 退出路徑加「保證退場」硬性期限（收尾卡在凍結 systemftp 的 raw
  `GetWindowText` 時，托盤回呼卡死 → `_shutdown` 從未建立 → 行程永遠不死 →
  watchdog 的救援被單例 mutex 靜默擋退 → 會診查詢停擺且無任何 log）。
* `_outlook_available` 改走 `call_with_timeout` —— ★它是批次SE single-flight
  的「門」★，Outlook 卡住時 `_do_full_job` 在這裡就 return，SE 的守衛根本
  走不到，而每輪 poll 堆一個 COM apartment。
* `_flow_lock` 卡死判定 + 升級重啟（non-blocking acquire 的失效完全無聲：
  每輪只印一行 INFO，heartbeat/tick 全正常）。
* ★批次SC 的守衛自帶出口★ —— 外部分析指出：那個 single-flight 把「資源爆炸」
  換成了「永久服務拒絕」，而且鎖每輪都正常釋放 → 上一條剛加的判定對它無效。
  **這是我同日上午自己開的洞。**
* 旗標檔要「取得」不是「看到」（刪不掉會造成寄信風暴；RELOAD 更嚴重：
  排程被反覆重建 → 輪詢 job 永遠不會到期 → ★輪詢永不觸發★）。
* journal 讀不到不得當成「沒有待辦」（那些觸發信已標已讀，IMAP 掃不到了）。

外審兩輪都打在同一件事上：**我的「保證退場」自己會卡住**（升級點先
`logging.critical` 要拿 handler lock、`_hard_exit` 會無界等帳本 RLock）。
定型成 `_exit_now`（函式本體只有 `os._exit`）+ `_force_exit`（先掛保險絲、
才做會等別人的事；掛不上就立刻硬退）。裸的 `os._exit` 收斂成兩個原語，
並加 AST 守衛。突變 31 條全紅。

★順帶抓到我自己的測試會產生假綠★：`_arm_exit_deadline`/`_force_exit` 會真的
建一條「睡飽就 `os._exit`」的緒，monkeypatch 在測試結束就還原了而那條緒是
之後才醒 —— 它會在 pytest 跑到一半把行程殺掉，**而且以 exit code 0 殺掉**。
實測一次量到 `rc=0` 但其實三個測試是紅的。加了 autouse fixture 擋住。

### 批次 SG（v2026.08.10.10，外審 3 輪 APPROVE）— ★實機事故驅動★
A01-11106-001 連續失敗六小時，告警信分不出「密碼被改／帳號被鎖／字沒打進去」。
補上兩半證據：登入階段對話框的文字（記在按掉之前）＋輸入欄位焦點是否確認。
* 證據排在診斷**最前面** —— 告警信是 `str(reason)[:300]`，接在視窗清單後面
  正好被剪掉，整批修正歸零。（我原本的測試全都只驗函式回傳值，
  沒有一條把它送進真正的告警格式量過。）
* ★隱形執行緒不再把擋路對話框藏起來★ —— SW_HIDE 後備模式每 80ms 把新視窗
  SW_HIDE，而偵測只認可見視窗：**它把偵測要找的東西藏掉了**。判準抽成
  `_is_blocking_dialog`，偵測端與隱形執行緒共用，兩邊不可能漂移。
  第 1 輪我只把診斷改誠實就交出去，外審駁回：★把診斷改誠實不等於把證據拿回來★。
* 隱私邊界由結構保證：`record_text` 預設 False，AST 測試斷言傳 True 的呼叫端
  恰好一個（登入等待），且會診單那條路完全不出現這個參數。
突變 22 條全紅。

### 批次 SH（v2026.08.11.1，外審 3 輪 APPROVE）
使用者定案「只加資源耗盡類重開機」。①`_login_cooldown_until` 落地
（重啟不再清掉 15 分鐘防鎖定保護 —— 而我這兩天才自己新增兩條會重啟行程的
升級路徑）；②隱藏桌面連續 3 輪建不出來**且本行程曾成功過**（＝USER object
耗盡，重開機可修；從沒成功過＝群組原則，不重開）→ 走既有閒置 30 分／24 小時
看守。外審兩輪都抓到設計錯誤：**站崗原因做成單一槽位**（BDE 與資源耗盡可同時
存在，後到的覆寫先到的 → 一邊恢復把另一邊還沒好的解除；且錯過排程就永不重排）
→ 改成原因集合；**修第一次時新開一個競態**（取消令設定在鎖外，新事故擠在中間
→ 事故掛著卻沒人看守）→ 合併成單一臨界區，用「非重入鎖持有時再取一定失敗」
做決定性測試。突變 27 條全紅。

### 批次 SI（v2026.08.11.2，外審共 10 輪 APPROVE）— 遠端指令
`[皮膚科遠端指令] <動作> <機器>`，動作＝重啟會診／重啟打卡／重開機。
起因是實機證實 A01-11106-001 停在好幾天前的版本（更新只在啟動時檢查一次、
heartbeat 讓 log 不過期所以 watchdog 也不重啟它）→ 修正到不了那台。

安全立場：指令**一律強制驗證**（不吃 `require_authenticated_trigger`）；
機器必填、剛好三段、一次一台；**先標已讀再執行**（標不掉就不做，否則每 20 秒
重啟一次的無限迴圈）；終局處置（格式錯／未授權／過期）要結案 —— 不結案是一個
**免驗證**的資源與 log DoS，但**別台的不可結案**；授權比對排在目標比對**之前**
（授權是全域確定的）；標已讀批次化並走有界 single-flight 路徑；指令掃描有
自己的 worker，**不與臨床觸發共用**（共用的話一條卡住的掃描會把信件觸發永久
關掉）；打卡由**自己**挑沒在打卡的時點重啟；重開機走既有閒置看守。

★這一批外審打回 9 次，其中 5 次是我自己修正時新開的洞★（共用 worker 吃掉
臨床觸發的結果、共用 single-flight 擋住臨床觸發、批次化把回信的 gate 弄丟…）。
突變累計 50+ 條全紅；測試被突變抓到 8 個真缺口（負向斷言被 docstring／回信
內文／目標回聲打敗共四次、有傳參數但沒證明真的接上、把生產函式假掉、
worker→呼叫端沒有端到端測試、兩種情況都回 False 分不出來）。

★方法論教訓★：外審的 ctx **只在第 1 輪注入**，第 1 輪寫錯的規格會鎖死整個
session。我在 ctx 裡自己加了「或 `全部`」（廣播），使用者定案根本沒有它，
而它用共用信箱做不出來（`\Seen` 是信箱全域的）。連改三輪文件都拉不回來，
最後改文件 + **重跑一輪完整外審**才解開。→ 寫 ctx 要把「範圍定案」放最前面、
引用使用者原文、寫明刻意不做什麼。

### 批次 SJ（v2026.08.11.4，deep 外審 3 輪 APPROVE）— 「皮膚科會診重開」

使用者定案（原話）：
> 我只要能夠 讀取email 指令 "皮膚科會診重開" 就把正在執行會診程式的電腦重開
> 即可 沒有執行會診程式的電腦不用重開

所以把 SI 的「機器必填」改回**選填**，主旨開頭固定三句：
`皮膚科會診重開`／`皮膚科會診重啟`／`皮膚科打卡重啟`。
**不填機器不是廣播** —— 會去收這個信箱的只有會診查詢那一支，
「收得到指令」與「正在執行會診程式」是同一件事，沒在跑的機器根本看不到。

★這一改的必然結果：去重不能再用已讀旗標★。`\Seen` 是**信箱全域**的，
第一台標掉別台就再也搜不到 → 只有一台會動作，「不填機器」名存實亡。
改成**本機收據**（鍵＝`{UIDVALIDITY}:{uid}`）、**claim-before-execute**；
收據必須落地，因為★重開機之後那封信通常還沒過期★，沒落地就變重開機迴圈。

外審三輪共 5 條 finding，全部 CONFIRMED：
1. 執行過的信刻意不標已讀 → 30 分鐘後走「已過期」回信說沒人執行（使用者已先
   收到成功信 → 再寄一次 → 多一次重開機）。
2. `safe_load_json` **把讀取錯誤吞掉回 default**，那個 `except` 永遠不會進去 ——
   「讀不到就不執行」從未生效；而我的測試把它 patch 成會拋例外，
   ★是生產函式不會有的行為★。改用會回報狀態的 `safe_load_json_ex`。
3. 取不到 UIDVALIDITY 時鍵變成 `":<uid>"` → 下一輪鍵不同 → 重複執行。改 fail-closed。
4. 掃描端 `startswith` vs 解析端 `find` → `Re:` 開頭在掃描端就被濾掉，
   ★契約明明允許★。抽出共用的 `imap_reader.normalize_subject()`。
5. **收據存在 ≠ 執行過**：收據是執行【之前】落地的，claim 到執行之間掛掉就
   會被講成做過、連過期通知也吃掉。且一台機器**沒有全域資訊** ——
   不指定機器的信永遠 UNSEEN，晚開機那一台第一眼就是「已過期」、身上當然
   沒收據，照樣會代表全體發言。→ 收據拆兩段（claim／done）+
   **過期通知只講本機**並加上「已收到成功信就請忽略，不要再寄一次」。

突變累計 32 條全紅；★三條第一次沒轉紅★，都是同一種病：
* `startswith`→`in` 被後面「短語後必須是空白」那條守衛擋掉 —— 量到的是別條規則，
  要**忠實還原修正前的 `find` + `i + len(phrase)`** 才分得出勝負；
* 剪枝把別筆收據誤刪**當場看不出來**，要等下一封指令把檔案重寫成只剩它自己 ——
  反例得是 claim A → claim B → A 必須仍被擋住；
* （第 1 輪）測試把生產 helper 假成會拋例外，測了一個不存在的情境。

### 批次 SK(v2026.08.12.1,deep 外審 2 輪 APPROVE)— 登入對話框截圖附告警信

使用者原話(2026-08-12):「截那個對話框的圖附在告警信裡」。
背景:Delphi 訊息框內文是 TLabel(TGraphicControl,沒有 HWND),
`_window_texts` 拿不到 —— 實機告警只看得到「[TMessageForm] 住院醫囑系統 / OK」。

設計:`_note_login_dialog`(唯一呼叫端是登入階段的 record_text=True 路徑)
在所有 early-return **之前**呼叫截圖;PrintWindow 只截該 hwnd 的視窗範圍;
一輪一張、reset 重新武裝、沒存成不佔名額;.tmp + os.replace 原子落地;
告警端 mtime >12h 或在未來一律不附,附件與「已附上…」那句由同一個變數決定;
恢復時清掉截圖。隱私:與 SG 文字證據同一條結構邊界(登入階段畫面不可能有
病人資料)+ 尺寸上限 1600x1200 當第二道防線(太大=抓錯視窗,寧可不存)。

外審 1 條 P2(CONFIRMED):worker 決定「要附」到 SMTP 開檔之間,正式檔可能
被恢復清理刪掉 → 信「說有附卻沒附」,或整封告警弄丟(告警 6 小時才一封)。
修法:寄信前 copy 成不可變快照 `login_dialog_evidence_sending.png`、附快照,
finally 清掉;快照失敗=不附也不宣稱,告警照寄。★教訓:mock 掉 send_mail 的
測試量不到「決定要附」與「真正開檔」之間的併發交錯 —— 改成在 fake 裡當場
刪正式檔再驗附件仍在。★

突變 13 條全紅(10 主體 + 3 修正)。#82(TLabel 內文)以此結案。

### 批次 AD-1〜AD-5(v2026.08.13.1〜.5)— 2026-08-12 GPT-5.6 全審五批結案

全審(24 commits 增量)8 條 P1 全 CONFIRMED 全關閉、7 條 P2 關 6 條:

* **AD-1 L2 結案**(deep 3 輪):六支 stub 改「復原→解析→執行」+整合測試
  (同一次啟動要跑到修復後的版本);current.txt 恰好一行;版本樹禁任何
  reparse point(os.walk 帶 onerror,查不動=拒絕)。
* **AD-2 帳本遷 SQLite WAL**(deep 2 輪,使用者定案):BEGIN IMMEDIATE 交易
  +synchronous=FULL(begin 回傳=已 fsync);begin_if_no_live 原子 claim;
  舊 JSON 每啟動 INSERT OR IGNORE 併入;_txn 任何例外一律 ROLLBACK
  (非 SQL 例外會把整本帳拖下水)。
* **AD-3 SMTP 三段化+文字 outbox**(deep 3 輪,使用者定案只落地文字):
  DATA 拆 354 前/內容中/等最終回應(354 前逾時=可安全重試,不再冤成
  UNKNOWN);Sent 查無→用落地文字自動補寄一次(原子 claim_resend_child、
  回寫原信、PHI 附件不落地、body 獨立 3 天保留期+回查輪掃除)。
* **AD-4 IMAP identity 統一**(deep 3 輪):journal/收據鍵=帳號指紋|世代|uid;
  身分由掃描連線自己的設定算出隨結果傳遞;mark_uids_seen 驗世代+帳號;
  ★舊收據不改綁★(換帳號邊界不安全)→ 曖昧 fail-closed 等 24h TTL;
  replay 接 OverflowError/非有限/未來時間戳。
* **AD-5 HIS safety**(deep 2 輪):會診視窗採認=恰好一個+連續三次穩定
  確認(_sole_stable_window 共用;第一眼的單一候選不算數 —— A 先出現
  B 晚一拍的競態);force_close_active 改 tag 分域(指令掃描逾時不誤砍
  Sent 回查)。P2-06(modal owner-chain)刻意轉觀測版待辦(#89):
  沒有實機 owner 鏈數據就 enforce 會重演 8/5 fail-closed 無出口事故。

方法論:五批合計突變 60+ 條全紅;8 條第一次沒轉紅(同型例外不釘訊息、
殊途同歸的序列、不忠實的突變、沒先 resolve 的傳播測試…)全數修反例。

### 批次 AE-1 / AE-2(v2026.08.13.7〜.8)— 2026-08-13 全審第二輪結案

第二輪全審(6 commits 增量)確認上一輪 16 個大項全數 FIXED、L2 正式
通過;新開 4 P1 + 3 P2,全 CONFIRMED 全關閉(P1-04 閘門政策=使用者
決策,依審查要求維持未接線):

* **AE-1 durable resend 狀態機**(deep 2 輪):核心不變式=「欠補寄」由
  資料庫回答(resends_owed),只要還欠著 payload 就必須在 —— body 保留
  到鏈關閉(全數送達/明確放棄/3 天隱私天花板),不再「終局即清」;
  claim_resend_child 改「in-flight 互斥+auto 上限 2 次」(crash 過的
  claim 不再永久擋路);PARTIAL 接進同一條 durable 路(重啟後補漏的
  那位,不再只剩告警);結案讓路給 durable 補寄;schema v3(kind 欄)
  +前向版本守衛(比程式新的資料庫拒開、不降版)。審查中再抓 4 條
  (同 key 多條鏈各自補寄→canonical 只留最新/sibling 查詢失敗≠有較新
  (結鏈不可逆)/結案不看 in-flight/佇列子紀錄留 PHI 副本)全修。
  審查點名的 6 個 crash 注入測試全數對應;突變 19 條全紅。
* **AE-2 identity v2 + modal 觀測版**(deep 2 輪):身分指紋含
  host/port/username/mailbox 整組(同帳號兩台伺服器不再互認);
  改鍵後果自收 —— 舊命名空間收據納入曖昧 fail-closed(24h TTL 出口),
  部署交接不重複執行遠端指令。#89 P2-06 觀測版:owner 鏈證據
  (GW_OWNER 逐跳 class/vis/en + main_in_chain/main_disabled 兩個候選
  判準)記進 log,只記不擋;成環/超長鏈有界、不讀視窗文字、同形狀
  節流(main 狀態進形狀鍵:unknown→known 是新證據不可壓)。
  突變 8 條全紅。

剩餘追蹤:P1-04 寄送閘門政策(availability vs integrity)待使用者定案;
enforcement 等觀測版實機數據;#48 批次L、#23 DPAPI 不變。

### 批次 AE-3(v2026.08.17.1)— 2026-08-13 全審第三輪結案(deep 6 輪 APPROVE)

第三輪確認 AE-1/AE-2 方向正確(L2/SQLite/SMTP/IMAP/HIS 全數維持 FIXED),
但點出 durable outbox 仍缺兩個 primitive。三條 P1 + 續審再抓 7 條,全關:

* **claim ≠ attempt**:額度只數★真正跨過 SMTP 邊界★的嘗試
  (`mark_submitting`,已 fsync);claim 後 send 前 crash 的子紀錄
  attempts=0,不扣額度(舊版連續兩次這種 crash 就能在【零次 SMTP】
  的情況下 abandon 一封臨床通知);另設 claim 總數硬背擋 6 次
  (反覆在同一點中斷=機器壞了 → 放棄+告警)。
* **兩個 executor、一個仲裁**:退避佇列補寄也走 `claim_resend_child`
  (kind=queue_retry)+ 劃 SMTP 邊界,佇列降級成 scheduler;claim 交易內
  重驗「此刻仍 TRANSIENT」∩「同 business_key 底下沒有任何紀錄已送達過」
  —— 子紀錄/sibling 送達了但還沒回寫親紀錄的瞬間也擋得住。
* **takeover 要有本錢**:較新同 key 紀錄「已送達或自己有 body」才能接走
  補寄義務;bodyless 且未定 → wait;bodyless 已失敗 → 義務交還舊鏈
  (混版部署匯入的 legacy 紀錄天生無 body,舊版規則會刪掉唯一的 payload
  = 永久漏寄)。
* **逐位拒收要搶在整封回查之前落地**:`record_refusals`(只覆蓋
  R_UNKNOWN —— CONFIRMED/PERMANENT 都是已有結論的終局)+ 回查每輪
  先重新落地 + ★跨 process 寄存處★ `settings/pending_refusals/`
  (每筆一個檔,避免共用 JSON 的無鎖讀-改-寫丟更新);補不上的那幾筆
  ★誰都不准收斂★,七天/一天(讀不懂的檔)有出口並大聲講。

方法論:突變 26 條全紅;其中 3 條第一次沒轉紅(反例沒跨過回查門檻、
被後來的新機制遮蔽、突變不忠實)—— 全部照「反例要只靠被測那條規則
分勝負」修正。

### 批次 AE-4(v2026.08.17.3)— 2026-08-17 全審第四輪結案(deep 3 輪 APPROVE)

第四輪確認 AE-3 三條 P1 的核心已修對,但把 blocker 往下推到更細的
★SMTP recipient-level evidence durability★。審查建議的「下一批只做 4 件事」
全做,續審再抓 2 條,全關:

* **逐位 RCPT 結果在 DATA 之前 durable**:`send_mail` 新增
  `on_rcpt_result` callback(RCPT 全部回答完、DATA 之前呼叫,
  ★`accepted=[]` 也呼叫★)+ `require_durable_rcpt`(補寄路徑落不了地
  就不送,內容尚未送出可安全重試)。★這同時是真正的嘗試邊界★ ——
  attempts 只在跨過 RCPT 後遞增(連不上/認證失敗不再算一次嘗試,
  那類無限中斷由 claim 背擋收)。四條寄送路徑全接上並有 AST 接線測試。
* **回寫用收斂後的狀態**:`_settle_one` 找到子紀錄在 Sent 之後,只
  propagate 子紀錄裡真的 `R_CONFIRMED` 的人 —— 拿「當初嘗試的名單」
  整包確認會把被 4xx/5xx 明確拒收的人洗成已送達。
* **supersede 是顯式狀態(schema v4)**:takeover 不再只清 body ——
  `body_text == ""` 本來同時代表【已送達】【已放棄】【已被接手】三件事,
  結案路徑因此對「剛剛才由較新紀錄送到」的事寄出「始終沒收到,請人工
  轉寄」(誘導人工重寄=重複通知)。
* **permanent 單調上傳**:補寄時的 550 往上傳到親紀錄;
  `SMTPRecipientsRefused` 的逐位碼不再摺成 generic failed
  (★只在涵蓋全部收件人時才用★ —— 殘缺 map 會把不在名單上的人宣稱成
  已送達)。
* **P1-02 最低限度**:claim 交易內擋「同 business_key 還在飛的較新初次
  紀錄」(新一輪剛 begin 全是 UNKNOWN,只看已送達擋不住)。完整的
  event-level 寄送閘門仍是待定案的政策題。
* 續審再抓:全部收件人被拒時 callback 完全沒跑(最重要的那條路)/
  補寄用預設 `max_retries=2` 讓一次 claim 做三次 RCPT、額度被乘三。

方法論:突變 15 條全紅;其中 3 條第一次沒轉紅(反例沒經過生產呼叫點、
突變對測試形狀不忠實、縱深層無獨立反例 → 改量它的危害方向)。
既有測試替身 4 檔跟上生產呼叫形狀(假的不呼叫 callback = 額度永不遞增,
量到的是生產不會發生的情境);回傳型別守衛改成只算函式自己的 return。
